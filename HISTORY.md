# Development history

Per-session development log for the OniARM64 port, newest first. For the project overview, current status, and milestone progress, see [README.md](README.md). For per-session handoffs with full investigative detail, see `../docs/handoff-*.md` in the parent repo. For active bugs and investigations, see [GitHub Issues](https://github.com/andiyar/OniARM64/issues).

This file is updated per behaviour-changing commit (the workflow contract in `../CLAUDE.md` mandates it as part of the same commit as the code change).

---

### 2026-09-19 — Session 75: `-nodamage` player health lock

- **feat(character): `-nodamage` launch flag locks the player's health** (3e700a2): a `UUtBool noDamage` on `ONtCommandLine` ([Oni.h:45](OniProj/OniGameSource/Oni.h)), its default and a `-nodamage` arm in `OniParseCommandLine` ([Oni.c:103](OniProj/OniGameSource/Oni.c), [Oni.c:122](OniProj/OniGameSource/Oni.c)), the condition in `ONrCharacter_IsInvincible` ([Oni_Character.c:10666](OniProj/OniGameSource/Oni_Character.c)), a guard refusing script and console writes in `ONrCharacter_SetHitPoints` ([Oni_Character.c:12411](OniProj/OniGameSource/Oni_Character.c)), and a startup line reporting the resulting state ([Oni.c:285](OniProj/OniGameSource/Oni.c)). Every HP loss in the game goes through the single gate at [Oni_Character.c:10788](OniProj/OniGameSource/Oni_Character.c), so widening the predicate covers melee, weapons and particle weapons, explosions, landing and falling, poison and self-inflicted damage in one edit. Nothing new was added to the damage path.

  It is the persistent form of behaviour the game already has. `ONgPlayerInvincible` — the `liveforever` cheat — answers the same predicate the same way for the same character type, but `ONrCharacter_LevelBegin` clears it at every level start (`Oni_Character.c:2247-2253`), so it has to be retyped each chapter. `ONgCommandLine` is written once during parsing and never re-initialised, so the flag lasts a session with no lifecycle handling at all.

  **The predicate rather than the subtraction is load-bearing for the health bar, not just tidier.** The bar is `hitPoints + inventory.hypoRemaining` (`Oni_InGameUI.c:4345`), and the hypo drain sits inside the same gate (`Oni_Character.c:10839-10848`) — so a narrower guard around `hitPoints -= inDamage` alone would leave hypos draining and the bar visibly falling on a character who is taking no damage. Knockback, the damage compass and the attacker's `damageInflicted`/`numKills` sit outside the gate (`10891-10912`), as do the stun resets, `ONrGameState_TauntEnable` and `AI2rFight_AdjustForDamage` above it — deliberate, so the AI still reads the hit as a hit and stays aggressive rather than losing interest in a player it cannot hurt, and so the semantics stay exactly `liveforever`'s rather than becoming a new set. `ONrCharacter_IsInvincible` has one call site repo-wide, and neither the AI nor anything else reads it or `ONcCharacterFlag_Invincible`, so widening it cannot move AI behaviour.

  **The lock is on the variable, not only on damage.** `ONrCharacter_SetHitPoints` is guarded as well ([Oni_Character.c:12411](OniProj/OniGameSource/Oni_Character.c)), so the console's `chr_health` ([Oni_Character.c:2142](OniProj/OniGameSource/Oni_Character.c)) and the scripted `chr_full_health` ([Oni_AI2_Script.c:2613](OniProj/OniGameSource/Oni_AI/Oni_AI2_Script.c)) and `chr_set_health` ([Oni_AI2_Script.c:2631](OniProj/OniGameSource/Oni_AI2_Script.c)) are refused for the player while the flag is on. That is the difference between being unable to be hurt and having the value held. The guard is `iNoDamageLockApplies` ([Oni_Character.c:12398](OniProj/OniGameSource/Oni_Character.c)), a file-local predicate holding the `charType == ONcChar_Player && ONgCommandLine.noDamage` test once and naming it, so the guard reads as the rule it enforces rather than as a bare field comparison, and it only ever returns early: it never assigns a value, so health freezes where it stands rather than being pinned to a number the flag picked. `ONrCharacter_Heal` is left alone deliberately — it only moves the value up, and the pickups and AI2 repair paths depend on it. Only the player is affected; scripts set AI health constantly and guarding those would break the game.

  That same guard closes the last route to death. `ONrCharacter_Die` is called from exactly one place — the `inHitPoints <= 0` branch of the function now being guarded — and every damage route to death sits inside the invincibility gate, so a locked player cannot be killed at all. This is what keeps the guard safe rather than a soft-lock: the revive path at `Oni_GameState.c:3608` that it refuses is unreachable, not broken.

  The startup line was added after the flag cost two debugging rounds, both of them silent. `OniParseCommandLine` is a bare `if`/`else if` chain with no final `else`, so an unrecognised parameter is discarded without a word — and because the flag changes nothing visible on screen until something hits you, a flag that was never parsed is indistinguishable from a flag that failed. The first round was a stale bundle: `open OniARM64.app` from `build/` resolves to a hand-moved Sep 17 copy that predates the feature, and the log would have shown `off` immediately. The second was the verification script itself, where zsh does not word-split an unquoted `$flags`, so `Oni $flags` passed `"-nodamage -noswitch"` as one argument with a space in it; the log showed `off` and named the fault in one line. It reports the resolved state rather than only the flag's presence, so it distinguishes "parsed and off by default" from "never seen", and it sits beside the renderer line that already does this for the same reason.

Verified: clean build of both `Oni` and `OniSweep` with no new warnings in the edited ranges, and a differential end-to-end run through the real damage path. A temporary probe in `ONiRunGame`'s loop (removed before commit) called `ONrCharacter_TakeDamage(player, 25, ...)` once at frame 11 of an `ONI_AUTOSTART=1` level-1 run and printed the player's health either side. With `-nodamage` the hit went `200 -> 200` and the probe read `flag=1`; without it the same hit went `200 -> 175`. That is the feature in one comparison: the flag is parsed, the gate answers, and the default path is untouched. Two earlier attempts at this probe reported nothing, and both reasons are worth recording because each looked like a passing test. `intro.bik` (17 MB) plays before the loop and ate most of a 22-second window, so the loop had not reached frame 11 before the run was killed — the probe simply never ran. And the probe printed to a block-buffered stdout that SIGKILL discarded, so its first working run would have read as a second silent failure without an `fflush`. A `SWEEP_LEVELS="1 19" SWEEP_RENDERERS="gl"` sweep run reports `0 regression(s), 0 abort(s)` with both cells `ok` — level 19 because it is the melee-sensitive level in this fork's history, level 1 as the baseline case. The 441 stale lines are the unswept levels and fall straight out of running a subset; they are not drift, and the run exits `2` because of them rather than `1` — the script tests broken, then regression, then stale, so an exit of 2 is itself proof that the first two were zero. The same subset run before the lock existed reported the same `0 regression(s), 441 stale`, which is the point: defaulting the flag off is what keeps a CI gate that never passes it unable to move. The startup line was then confirmed to discriminate correctly, which is the cheapest check in this entry and the one that would have saved the most time: `Oni -nodamage -noswitch` logs `no damage: on`, the same binary with no arguments logs `no damage: off`, and the malformed single-argument form above logs `off` — three launches, three correct answers, including the wrong one. Note which file the log lands in: `iOpenLogFile` opens the path relative to the current working directory first and only falls back to `~/Library/Logs/OniARM64/`, so the same binary writes to different files depending on where it was started, which is what made the first attempt at this check read as empty output.

The lock itself was proved the same way as the damage gate, with a temporary probe (removed before commit) in `ONiRunGame`'s loop calling `ONrCharacter_SetHitPoints(player, 50)` once at frame 11 of an `ONI_AUTOSTART=1` run and printing the value either side. With `-nodamage`: `flag=1 charType=0 before=200` then `after=200` — the write was refused. Without it, the identical call: `flag=0 charType=0 before=200` then `after=50` — applied. Same character, same starting health, same call, one variable. The control had to be run twice: the second launch of a back-to-back pair produced no output at all, zero bytes and not even the particle warnings, so the control was re-run on its own rather than counted from a run that never started.

Not verified, and recorded rather than papered over: everything under *What it does not do* in [EXTENSIONS.md](EXTENSIONS.md) is read from the code rather than observed. Knockback and knock-downs still landing under the flag, the shield meter draining while health holds, and the out-of-bounds fall no longer killing all follow from where the gate sits relative to those paths; the two probes above exercise the two gates themselves and none of them. Playing a level with the flag on and taking a punch, a fall and a bullet is still a human's job.

---

### 2026-09-18 — Session 74: held-key combat block

- **fix(ai): zero the technique weight at the two throw refusals** (b9dbebb): `AI2iMelee_WeightTechnique` refuses a throw technique in several places, and they disagreed with each other. The two wrong-side cases at ~4284 and the missing-active-target case at ~4305 already do `ioTechnique->weight = 0; return;`; the two below them — `try_animation == NULL` on the target-animation lookup at ~4314, and `!AI2iMelee_TargetIsThrowable` at ~4326 — filled the on-screen verbose string and fell through, leaving full weight on a technique the AI could not execute. The second is load-bearing for the block feature rather than incidental: the new gate makes AI throw refusals routine, and without the zero the AI selects the throw, aborts at execution, blacklists it, re-selects it, on roughly a one-second cycle. Landed standalone and before the feature so it bisects and sweeps on its own.
- **fix(character): stop passing a NULL animation to TMrInstance_GetInstanceName** (5b74172): the `"AI %s tried to throw %s (%s/%s) but was unable to"` printf passed `specific_throw.dstThrow`, which is provably NULL on that branch — `srcThrow` and `dstThrow` are assigned together at ~11812, and reaching the branch requires `srcThrow == NULL`. `TMrInstance_GetInstanceName` dereferences its argument, so the "was unable to" path was a fault near address 0 rather than a log line. Now passes `desiredtargetanim`, which the sibling printf at ~11977 already does and which `Oni_AI2_Executor.c:383` asserts non-NULL.
- **feat(character): player held-key combat block** (58edffe): the feature, in three parts. `UUtBool blocking` joins `ONtActiveCharacter` rather than the character flags, because `ONtCharacterFlags` is full — all 32 bits named — and because the value is recomputed from input every tick in `ONrCharacter_HandleHeartbeatInput`, so it needs no lifecycle handling: the active-character struct is cleared wholesale on activation, deactivation and level begin. It is written before that function's two early exits so a release during an animation lock is still seen, and gated on being the local player, alive, not in a film (`DoFilmUpdate` overwrites `buttonIsDown` from the recorded frame) and not currently thrown. `ONrCharacter_IsBlocking` restates the `charType` test so no future caller can make an AI unthrowable, and mirrors `CouldBlock`'s own `hitStun` rule so the block and throw answers cannot disagree. `HandleBlock` is inserted between `HandleStun` and `HandleLeaveStun` and claims only from a neutral stance; that placement does two jobs with one edit — claiming only from neutral is what stops the guard interrupting your own punch or jump, and sitting above `HandleLeaveStun` is what stops `blockStun` expiring from kicking a held guard back to standing, since that function's `Block` case forces Stand or Crouch. No edit to `HandleLeaveStun` was needed. `ONrCharacter_FightMode` is re-issued every held tick as `HandleStun` does it, because `fightFramesRemaining` decrements once per animation frame and `DoAnimation` strips the Fight varient at zero, which would drop the guard out of the fight collection mid-hold. The handler returns its post-condition rather than assuming the block animation was found, so a class with no block animation degrades to normal movement instead of freezing. `ONrCharacter_CouldBlock` answers a held block early — both height flags true, facing test skipped — and leaves the authored path below untouched. Skipping `ONrCharacter_IsDefensive` for an actively blocking player is load-bearing: that function computes `toIsCrouchOrStand` from `fromState` instead of `toState`, so once a block is held the animation's own states stop matching the crouch/stand list and defensiveness goes false for the whole hold. **That typo was deliberately left alone** — correcting it would test `Blocking1`/`Crouch_Blocking1`, which are not in that list either, and would break the existing reactive block and AI blocking with it.
- **feat(character): a blocking player cannot be thrown** (e8840e9): throws ignored block state entirely; both live initiation paths reach `ONrPerformSpecificThrow` without asking. Three gates, one predicate. `AI2iMelee_TargetIsThrowable` is the primary one, consulted both at weighting (~4330) and at execution (~8004), so one edit stops the AI choosing a throw it cannot land — which is what makes the refusal read as a decision rather than a failure. `AttemptThrow` carries the same refusal next to the existing `ONrCharacter_IsKnockdownResistant` check, this repo's vocabulary for "cannot be thrown". The third is the AI commit point in `RemapAnimationHook`, and it is not redundant: the executor sets the throw override and then still has to land a punch, and the atomic-animation delay can hold that override for many frames, so there is a real window in which the player raises the guard after the AI has committed. That gate is an `else if` on the existing facing test, so a refused throw falls through to the "no throw was found" return and the AI plays its original punch — the same path a bad facing delta already took. Gating inside `ONrPerformSpecificThrow` was rejected: it returns `void` and both callers infer success from `specificThrow.srcThrow`, so a refusal means changing the signature and restructuring both callers, and it writes `thrownBy` and `throwTarget` at different points while the per-frame consumer keys off the second, so an early return desyncs the pair.
- **feat(input): default the block key to Z, and give existing configs one too** (893643e): the action name `"block"` and the `LIc_Bit_Block` bit both already existed and `bind z to block` already parsed — nothing bound one. Fresh installs get the bind in the default list; existing installs need the fallback because `key_config.txt` is only written when absent and its shipped first line is `unbindall`, which clears every default that follows it, so a config predating this feature could never produce the bit. The fallback enumerates live bindings after the config file is read and installs the default only if the action is unbound, so an explicit `bind q to block` still wins. Two traps are recorded in the commit because both fail quietly. `LIrBindings_Enumerate` stops on `UUcFalse`, not `UUcTrue` — the first cut's callback answered `UUcFalse` unconditionally and therefore inspected exactly one binding, which looked like success in the case with no bind and was wrong in the case with one. And the search asks a second question, whether `z` is already in use, because `LIrBinding_Add` reassigns the action of a key that is already bound rather than allocating a fresh slot, so installing blindly would silently convert whatever the user had put there; if `z` is taken the fallback declines and logs that block has no key, so the failure is visible instead of silent.
- **diag(character): env-gated trace for the held block** (4d6613f): `ONI_BLOCK_TRACE`, set to anything but `0`, appends `[block]` lines to `startup.txt` — guard raise with the state it raised from, guard drop tagged with whether the key was still held, every hit a held guard absorbed with both height flags and the angle, and every refused throw with its site. It exists because none of the feature is reachable from the sweep harness. Every line is a state change or a once-per-attempt event, never per-tick. `AI2iMelee_TargetIsThrowable` is deliberately *not* instrumented even though it is where the AI decides not to throw, because the planner consults it while weighting every technique every tick. The damage-site line is the only end-to-end witness of the 360 requirement in the code: `CouldBlock` is also called by the AI planner as a prediction, so a line there reports intent rather than outcome.

Verified: clean build of both `oni_app` and `OniSweep` with no new warnings in any edited range; the binding fallback headlessly through `ONI_AUTOSTART`, four configs — no block bind with `z` free installs the default, `bind z to block` is left alone and silent, `z` bound to something else declines and says so, and this machine's own `key_config.txt` installs the default, which is the case the fallback exists for; and the level sweep, which needed triage rather than a tick — see the next paragraph.

The sweep did not pass on the first run, and what it found was not the feature. It reported 8 regressions and 10 stale entries, all on levels 10, 12 and 19. The block feature cannot move a baseline: `-sweep` never runs the main loop body, so no key is pressed and no guarding player exists to change an outcome. Bisecting the six commits found a single cause — **b9dbebb, the AI technique-weight fix, reproduces all 8 regressions on its own.** `main`, with none of these commits, is clean on levels 10, 12 and 19, so this is not a stale baseline; and repeated runs of the same binary produced byte-identical reports for levels 10, 12 and 19, so it is not the nondeterminism of #106. The mechanism is the fix working: zeroing the weight on a throw the AI cannot run changes which technique it picks, the fight resolves differently, characters settle in a different order, and the settle-phase events move with them. Much of the diff is ordering rather than content — the diff tool compares sets, so a reordered event is reported as both a regression and a stale, and the same event appearing in both lists is the tell. No cell aborted and no new error-severity line appeared. One consequence is worth stating plainly rather than burying: **this branch changes AI melee behaviour on level 19**, and it does so by way of a fix the feature depends on rather than by way of the feature itself. The baselines were then updated by hand rather than with `--accept`: the 10 stale lines removed and the 8 new ones inserted in sorted position, each inheriting its reason from the file's own existing vocabulary for that kind of event. `--accept` would have been the wrong tool here — it rewrites a baseline from scratch out of the merged report and would have thrown away all 553 hand-written reasons in each file, replacing every one with a review-me placeholder. The gate then re-ran clean against the edited baselines, 0 regressions and 0 stale on both renderers, off the same run's data.

Not verified, and recorded rather than papered over. Every behaviour of the feature itself is a property of the heartbeat chain, `CouldBlock` and the throw remap, inside two engine files of 17k and 8k lines, and `-sweep` cannot reach any of it: it never runs the main loop body, so no key is ever pressed and no AI ever swings at a guarding player. No test was added, deliberately — linking any of that into a `tests/` binary would mean stubbing the template manager, animation collections, physics, AI2 and the console, a stub surface larger than the feature. What still needs a human at the keyboard: the guard raising on the bound key and the release returning to standing, and to crouching from a crouch; the hold reading as a hold, with no re-stitch per tick and no flicker at the moment `blockStun` expires after a blocked hit; standing with your back to a Striker and taking no damage, which is the only end-to-end proof of the 360 requirement; a refused throw reading as a punch rather than a frozen or hitching AI; and holding the key mid-punch to confirm the punch finishes first. The `ONI_BLOCK_TRACE` lines above are what to read while doing it. The AI weight fix is in the same position for a different reason: the sweep proves it changes which technique an AI picks on level 19, which is consistent with the churn it was written to remove, but consistency is not the same as watching an enemy stop changing its mind against a knockdown-resistant or unstoppable opponent, and that still needs a fight.

---

### 2026-09-17 — Session 73: quick save / quick load on the Data Pad (#112)

- **feat(ui): quick save and quick load buttons on the Data Pad** (addresses #112): mid-level quick save and quick load existed but were reachable only through a key binding the player had to add to `key_config.txt` themselves, or the dev console — nothing in the game pointed at them. The Data Pad (the **F1** pause screen, dialog template 70) is where a player looks, and it now carries **Quick Save** and **Quick Load** in its left column, under **Help**. The two controls are `ONcPS_Btn_HelpPrevious`/`HelpNext` (112/113), Bungie's multi-page-help buttons, art-complete in the template and dead since the feature was cut — their own comment said *"remove the UUcFalse if you want to have multiple pages for help"* — renamed to `ONcPS_Btn_QuickSave`/`QuickLoad`. The dialog template is binary game data that cannot be edited from source, so reusing shipped controls is the only route that doesn't mean creating them at runtime.

Placement continues the tab column rather than hardcoding coordinates: the row pitch is read from two adjacent tabs (`Help.top - Diary.top` = 60) and each button goes at `Help.top + pitch * row`, landing at 217,408 and 217,468. `WMrWindow_SetLocation` takes **parent-relative** coordinates and the WM has no getter for them, so the parent's screen rect (via `WMrWindow_GetParent` + `WMrWindow_GetRect`) is subtracted from the target's — the same screen-rect-delta idiom #89 used for the Options checkbox, arrived at from the other direction because these controls already exist and have a position to discard. Both buttons carry `WMcButtonStyle_HasTitle`, so `WMrWindow_SetTitle` paints. Label fit was measured rather than guessed: "Quick Save" is 76 px in the button's own font against a 130 px control, well clear of the symmetric clipping `DCrDraw_String`'s `TSc_HCenter` would cause. The measurement mirrors `WMiButton_Paint` — `WMrWindow_GetFontInfo` → `DCrText_SetFontInfo` → `DCrText_GetStringRect` — and deliberately not `title_width`, which is computed before the template font is applied and never recomputed.

Both buttons **defer**: the click closes the pad and sets a pending flag, and `Oni.c` does the work on the same iteration once the pad is destroyed. `pending_quick_load` already existed for the F9 key and the `quick_load` BSL command; `pending_quick_save` joins it. The deferral is not load-bearing for safety — every callee is a read-only walk writing into `ONgQuickSave`, and the modal loop has the world frozen, so an inline save would also be correct — it is about audio and about one idiom for both buttons: `ONrPauseScreen_Display` cuts volume to 50% for the pad's lifetime (`ONcPauseScreen_VolumeCut`), and an inline save would play its chime at half volume. The toast survives either way: `COrMessage_Print` does not check the disabled flag, and `COgMessagesTemporaryDisable` gates the fade-timer *update* as well as the display, so the counter only starts draining once the pad closes.

Quick Load greys out when there is no save to load. The predicate is `ONrQuickSave_Exists`, built on a shared header check factored out of `ReadFromPath` — the obvious implementation, calling `ONrQuickSave_Read`, is wrong twice over: `ONtQuickSave` is ~170 KB, and `ReadFromPath` memsets the whole struct before it decides anything. Both callers now go through `ONiQuickSave_ReadHeader`, so the version, swap-code and counts-fit guards cannot drift apart. The result is cached in `ONtPauseScreenData.quick_load_available`, computed once in `InitDialog`, because `ONiPS_EnableButtons` runs on every tab click and every Prev/Next press; it cannot go stale within a session since both buttons close the pad. It is assigned unconditionally, `ONgPauseScreenData` being a never-cleared file-static. Visibility is owned by `InitDialog` alone: the force-hide of 112/113 in `ONiPS_ShowHintArea` was **deleted**, not flipped to `inShow` as Bungie's comment suggests — that function is called with `inShow = !show_help` from `HandleCommand`, so following the comment would hide the buttons exactly when the Help tab is selected. The dead commented-out help-paging block in `ONiPS_EnableButtons` went with it: same cut feature, and it referenced the renamed identifiers.

Verified headlessly by a temporary `ONI_PSDUMP` hook (a frame-200 trigger on `pending_pause_screen`, a layout dump in `InitDialog`, and a synthetic click posting the same `WMcMessage_Command` that `WMiDialog_HandleKeyEvent` posts for Enter), removed before commit. Four runs: both buttons land at exactly 217,408,347,448 and 217,468,347,508, visible; Quick Load reports `enabled=0` with no save file and `enabled=1` after one exists; a synthetic Quick Save click wrote a 1796-byte `quick_save.dat`; and a synthetic Quick Load click logged `[quicksave] loaded level 1 save point 0 (1 quads; doors 103 applied, 0 forced, 45 locked; ...)`, so the deferral, the flag wiring and both engine paths are exercised end to end. The plan's fallback — creating the controls at runtime if 112/113 turned out to be absent, as #89 did for the Options checkbox — was **dropped** once the probe proved they ship in the template; a NULL guard that logs replaces it, with the reasoning that ~80 lines of unused UI construction is worse than a visible message for a case that does not occur. The `WMcButtonStyle_Default` hazard the plan flagged (Enter firing a button it shouldn't) was cleared by the style dump: 112/113 are `0x00030000`, push-button with no `Default` bit, and the only `Default` control is the pre-existing Close.

Not verified, and recorded rather than papered over: the synthetic click is not a mouse, so hit-testing, focus behaviour and the enter/escape round trip need a human; the on-screen look of the reused button art against the partspec (`psui_oniUI`) is a judgement call for a screenshot; and the two buttons sit outside `gPSHighlightableButtons`, which lists the five tabs only, so they get none of the tab font-shade treatment and render in the template's own shade.

### 2026-09-17 — Session 72: mid-level quick save / quick load (#112)

- **feat(save): quick save state file module** (01e6db2, addresses #112): `Oni_QuickSave.c/.h` owns `quick_save.dat` — a fixed header (level, save point, player record, record counts) followed by four variable-length runs (characters, doors, geometry quads, corpses), only the live records written so the file stays proportional to its contents rather than to the array maxima. Every record is fixed-size and pointer-free by construction, because each one has to survive `ONrLevel_Unload`: identity is a name, an ordinal or an ID, never an address. That is also why the module does not reuse `ONtContinue`, whose header drags in `Oni_GameState.h` + `Oni_Motoko.h` + `Oni_Weapon.h` and would cost the standalone test. Two details are load-bearing: character identity is `(name, ordinal)`, because shipped levels spawn the same name repeatedly on purpose (`respawn1` up to three times in `power`, `WH_Striker_C` four times in `EnvWarehouse`), so a name alone is not a key; and corpse records carry their character class as a *name*, never as the `ONtCharacterClass*` that `ONtCorpse_Data` actually holds, because that pointer dies with template memory and the corpse renderer dereferences it for `body` and `textures`, so restoring a corpse by byte copy would be a use-after-free. The reader rejects rather than parses — missing, empty, truncated, short, wrong version, wrong swap code, and counts that would overrun the fixed arrays, the last because a count read off disk indexes a fixed array. `tests/test_oni_quick_save.c` is standalone, 61 assertions, covering the empty and maximum-population round trips, every rejection path, and the resolver-backed wrappers via a stubbed `ONiBundlePath_ResolveStateFile`.
- **feat(save): quick save/load bindings, triggers, and player state** (db02dbd, addresses #112): the two reserved `LIc_Bit_Unused1/2` slots become `LIc_Bit_QuickSave`/`QuickLoad` so every existing input bit keeps its position; `quick_save`/`quick_load` join the action description table, and fresh installs get F5/F9 in the default config. F5 saves straight through; F9 sets `pending_quick_load`, consumed in the main loop beside the existing `pending_pause_screen` pattern. The reload deliberately does not run inside the heartbeat — `ONrGameState_HandleUtilityInput` runs inside `ONrGameState_ProcessHeartbeat`, and tearing the level down there would free state the caller is still using. Both key tests sit above the `ONgDeveloperAccess` gate so they work for a normal player, and `ZeroUserInput` already masks input down to Console+Escape+ScreenShot while user input is cleared, so both are inert during cutscenes for free. `quick_save`/`quick_load` BSL commands went in beside them, this port having no input automation; the `quick_load` handler sets the same pending flag rather than reloading inline, because `ONrLevel_Unload` frees the template memory the executing script lives in. `ONrGameState_MakeContinue` was two responsibilities in one function — capture the player, persist it into `persist.dat` — and quick save needs the first without the second or it collides with real save-point slots, so `ONiGameState_CaptureContinue`, `ONiGameState_NameContinue` and `ONiGameState_ApplyContinue` were extracted, with `MakeContinue`/`UseContinue` keeping their signatures and behaviour and quick load calling the apply half directly so it also works at save point 0, where no script branch calls `restore_game`. Quick load primes `ONtContinue` through `Continue_SetFromSave` *before* the unload, because `UseContinue` divides by `maxHitPoints` and a non-zero save point with an unprimed continue makes the level script divide by zero.
- **feat(save): restore world entities on quick load** (c50ef82, addresses #112): the four entity passes, applied after `ONrLevel_Load` returns because `AI2rLevelBegin` creates the load-time population during the load and `SLrScript_ExecuteOnce("main")` runs after that — applying earlier gets overwritten. Order is geometry quads → doors → characters → corpses. *Geometry quads* set or clear only `AKcGQ_Flag_BrokenGlass` per recorded index and then call `AKrEnvironment_GunkChanged()`; that bit sits in the collision, render, occlusion and AI-sightline skip masks, so without it a quick load can wall you out of an area you had opened. *Doors* go through `OBJrDoor_ForceOpen`/`ForceClose` rather than a `state` write — `state` is bookkeeping, and both functions also call `PHrNotifyDoorStateChange`, which pure field assignment skips, leaving pathfinding believing the old state; a door caught mid-animation is left alone and finishes its travel. Flags are written directly rather than through `OBJrDoor_Lock_ID`, because the lock path plays `ONcEventSound_Door_Lock` and a load should not chirp. *Characters* resolve by `(name, ordinal)` against the reloaded list, teleport and set health for those found alive, delete those found dead, and **force-spawn those not found at all** — `AI2rSpawnCharacter(name, UUcTrue)`, which is not optional, since `OBJcCharFlags_NotInitiallyPresent` is the editor default for `OBJcType_Character` and `AI2rLevelBegin` does not create most shipped enemies, so a quick load without the forced spawn lands you in a near-empty level. `hitPoints` is written as a field, never through `ONrCharacter_SetHitPoints`, which runs the entire death path when handed 0. Deletions run last so the next `ai2_spawn` cannot take a slot out from under the pass, and the player plus anything under `ONcCharacterFlag_ScriptControl` is skipped. *Corpses* are copied densely from `static_corpses` with `next` ring-wrapped; `node_list` is derived, so each record gets `ONrCorpse_Create_VisibleList` rather than a byte copy of a stale list — hence the new prototype in `Oni_Character.h`. `ONcQuickSave_MaxGeometryQuads` went 1024 → 4096, which is not an on-disk change and so needed no version bump.

The load line's counts are measured from the engine *after* the passes rather than echoed from the file, so a pass that silently no-ops reports zeroes. That mattered: the first cut incremented its "spawned" counter unconditionally, so it counted calls rather than effects — the same can't-fail verification the rest of the line was built to avoid. It now splits into `charactersSpawned`/`charactersSpawnRefused`, measured by counting recordable characters before and after the spawn, and `doorsLocked`/`charactersLive` are read from live engine state at the end. Two headless runs on level 1 driven by `ONI_AUTOSTART`, with `save_game`/`quick_save`/`quick_load` in the level script and differing only by a `door_unlock 15` before the save, report `45 locked` and `44 locked` respectively — the plan's own semantic, that a save where you unlocked the vault must not reload locked, demonstrated differentially. The `characters 0 restored, 2 spawned, 2 live of 3 saved` line is correct rather than a resolver failure: temporary instrumentation in the identity resolver showed the reloaded warehouse save-point-1 world contains only the player, so the force-spawn path is doing exactly the work it exists for. Sweep mode cannot host this test — `-sweep` ticks `ONrGameState_Update` but never runs `ONiMain`'s loop body, so `pending_quick_load` is never consumed; only the normal game path with `ONI_AUTOSTART` works.

Not verified, and recorded rather than papered over: the corpse engine pass, because level 1 has no corpses and no save point in it produces one (its file layer is unit-tested, its engine layer is covered by construction — dense write from `static_corpses`, derive `node_list`, wrap `next` — and needs eyes), and the visual fidelity of a restored world, which needs the interactive F5/F9 test. Also worth stating plainly, because they are what a player will actually notice, and they are properties of the engine rather than of this implementation: BSL script state cannot be restored (variables, trigger arming and the scheduler are all reset by `ONrScript_LevelBegin` and no export path exists anywhere in `BFW_ScriptLang/`, so a scripted event you already passed runs again); AI internal state resets (restored enemies keep position and health but re-derive goals, alert level and pursuit targets from scratch); `gameTime` returns to 0 and the RNG is reseeded by `ONrSky_LevelBegin`, so timed scripts restart; and a `chr_teleport` from a cutscene trigger can re-position a character you restored.

### 2026-09-10 — Session 73: OniMod Installer (#20)

- **fix(ui): Options renderer toggle painted under the dialog art** (addresses #89): the maintainer's Options visit with the 0200f3e diagnostics live showed the checkbox created, placed under the gamma slider and visible — just never seen. `WMrWindow_New` appends a child at the end of the parent's list and `WMiWindow_Draw` paints from the last child back to the first, so list order is z-order with item 0 on top; the Options template (WMDD 152) ends with `pict_options_background`, so the dialog art is itself the last child and everything appended after it paints underneath. The control is now moved to the head of the child list after placement (`WMrWindow_SetPosition` with `NoMove|NoSize`, insert-after NULL), which is where every template control already sits relative to the background. Diagnostics stay in; one more Options visit confirms.
- **feat(installer): OniMod Installer core, CLI mode and AppKit droplet** (0a89d5d, addresses #20): a small Swift droplet app that turns a depot download into an installed texture pack. Scope was cut back from the old #20 plan on the maintainer's call: no PNG ingest (depot mods arrive as `.oni`), no engine changes (overlay lookup already exists). The core walks the dropped zip or folder, keeps `TXMP*.oni` only (#62: any other template hijacks the engine), routes each file by the nearest `level<N>_final` path component (case-insensitive, nested to any depth, the doubled-dir shape in Realistic Skies included), names the pack from `Mod_Info.cfg` or the folder with the depot ID stripped, and runs the bundled `onipack import-sep` once per level into `TexturePacks/<Name>/`. The #63 alpha guard is built on the fly from the player's own `GameDataFolder` via the bundled `txmp-format-index`, same merge rule as `build-hd-overlays.sh`; when no game data is present it says so and installs anyway. A `--install` CLI mode drives the identical code for the fixture test (15 checks: zip + folder input, nesting, `Final` remap, ONCC ignored, replace refusal + `--replace`, guard on/off). Smoke-tested on four real depot zips including the 15-level HD Screens and the alpha-guarded Konoko-Cops retexture (2 skipped, the #63 class).
- **build(installer): bundle + sign; release pipeline** (b4f546c, b1089e7, 14dba3e): `txmp-format-index` gets a CMake target; `macos/build-installer.sh` compiles the Swift with `swiftc` (no Xcode project), copies both helpers beside the executable, stamps the game's version, and signs inside-out; `make onimod_installer` is the dev target. `oni_app_release` now also builds and notarizes the installer (`notarize-bundle.sh` took an app-path argument) and `package-dmg.sh` stages both apps into the DMG. The release path is syntax- and generator-verified only; the first real notarized run is the maintainer's next cut.

### 2026-09-01 — Session 72: macOS 26 glass app icon

- **feat(macos): Icon Composer glass icon**: the old `Oni.icns` was a transparent-background logo, which macOS 26 shrinks onto the white placeholder backdrop in the Dock. Added `macos/assets/Oni.icon` (hand-authored Icon Composer bundle: flat white "O" silhouette traced from the original art as a glass layer over a navy gradient fill) and taught `build-bundle.sh` to compile it with `actool` when the Xcode 26+ toolchain is present, emitting `Assets.car` + fallback `Oni.icns` and setting `CFBundleIconName`. Older toolchains fall back to the static `Oni.icns`, itself regenerated full-bleed on a dark background so it no longer hits the backdrop either. Verified end-to-end via `make oni_app`; the icon now renders in default/dark/clear/tinted styles.

### 2026-08-27 — Session 71: persisted renderer preference + Options toggle (#89, Metal M5)

- **feat(render): renderer preference state file module** (8ffdf86, addresses #89): a small pure-C module owning a one-token `renderer.txt` under App Support (or cwd for the bare-binary workflow, via `ONiBundlePath_ResolveStateFile`). Standalone unit test, 28 assertions, including the oversized-line case where parsing a truncation could turn `metalxxx…` into a valid choice.
- **feat(render): launch path honours the persisted preference** (2bfd28d, addresses #89): precedence is flag > `ONI_RENDERER` env > `renderer.txt` > default OpenGL, with the hold-Option chooser staying a transient per-launch override that never writes the file. Explicit choices are now tracked, which upgrades `ONI_RENDERER=gl` from silently ignored to a working force-GL. Sweep mode never reads the file — GL sweep cells pass no renderer flag and must stay on the default; verified by planting a `metal` preference and confirming a GL cell still logs `renderer selection: OpenGL (default)` with zero baseline regressions.
- **feat(ui): renderer toggle in Options** (02cde89, addresses #89): the shipping Options dialog template (152) can't gain controls from game data, so the checkbox is created at runtime in `InitDialog` — anchored under the gamma slider by screen-rect delta (the WM has no parent-relative getter), font borrowed from the subtitles checkbox, disabled when Metal is unavailable. Toggling writes the preference; a change from the running renderer shows the existing ChangeRestart dialog (156). First runtime-created control in a shipping dialog in this port, so on-screen appearance is the unverified half — awaiting a maintainer screenshot before the M5 milestone ticks.

### 2026-08-25 — Session 70: sweep baselines completed across all 15 levels (#103)

- **sweep: baselines now cover all 15 levels on both renderers** (addresses #103): full 30-cell run with the session-69 fixes in place — every cell completes, healthy wall times 2–27 s (down from 25–40 s in July). The eight newly-completing levels (1, 4, 6, 8, 9, 14, 18, 19) produced 416 new finding keys per renderer, byte-identical between GL and Metal; all 211 distinct keys triaged to accepted noise, none to new bugs. The new baseline classes: modal-dialog suppressions (the #109 tap doing its job), pool-exhaustion warnings (the #107/#108 warn-and-skip paths firing under the sweep's cumulative load), dry-pool script chatter downstream of those, and a handful of retail-data quirks verified against source before accepting (level 18 ships a character class with no animation collection; level 9's console references a screen texture that doesn't resolve, with Bungie's own warning present in their source drop; level 19's elite melee profile names attacks the class lacks). Baselines: 553 annotated findings per renderer. Per-cell watchdog default tightened 600 s to 120 s on the new timing data. One anomaly for the record: metal level 3 was SIGKILLed 12 s into its first attempt by something outside the driver (no jetsam record, no crash report, no unified-log trace) and re-ran clean with zero regressions against its July baseline; treated as a transient host kill, not a code defect.
- **fix(overlay): actionable log when a texture-pack dir holds no pack files** (addresses #110): the overlay scan only matches `level*.dat`, so a raw AE mod dropped into `TexturePacks/` (a tree of `.oni` files under `oni/common/level*_Final/`) logged `0 overlay file(s) registered` and nothing more, leaving the player in #110 to work out the pack-building step alone. The scan now counts every file the iterator yields and, when that count is zero, says the dir holds no packs and points at `onipack import-sep`. It gates on files-seen rather than files-registered: the dup-index and registry-headroom paths can legitimately register nothing from a dir that is full of packs, and both already log for themselves. README's TexturePacks note gained the matching build steps.
- **fix(instrumentation): gate the remaining event-frequency log families** (addresses #92): the #70 sweep caught the per-frame families but left nine event-frequency ones writing to `startup.txt` on every ambient start and stop, every super on/off edge, every AI combat entry, every room path prep and every 300th presented frame, so they now sit behind `UUrDiagVerbose()` (`ONI_DIAG_VERBOSE=1`) with the tags left in place — except the `[ALERT-DBG]` combat-entry line, which instead joins its four `Oni_AI2_Alert.c` siblings behind `ONI_AI_TRACE=1` so the whole family answers to one lever. Two of them were doing work as well as logging — the `[tex-diag]` dedup scan walked up to 1024 pointers per `M3rGeometry_Draw` call whether or not it could print, and the closed-hunt `super*`/`ap_wiz` classname filters in the particle code ran for the same reason — so the gate goes ahead of the scan rather than around the print; the port-added `ANIM_PREP` tracer also moved off stderr onto `startup.txt` where the rest of the diagnostics land.
- **fix(persist): back up persist.dat before a version-mismatch reset** (addresses #91): `ONrPersistance_Initialize` clears the in-memory struct whenever the file is unreadable or fails the version/swap-code check, and the next `ONrPersist()` (a volume change is enough) writes that cleared struct back over the file, taking every save point and unlock with it. The reset now copies the file's raw bytes to `persist.dat.v<N>.bak` first, or `persist.dat.unreadable.bak` when the read is what failed, and keeps any backup already sitting there rather than rotating it: a mismatched build gets launched more than once, and the second launch would otherwise back up its own reset file over the good copy. The guard has to ship ahead of the #73 v15→v16 bump rather than with it, because the binary doing the wiping is the older one, so a player who rolls back only has it if it was already there. Zero-length files are skipped, which keeps the #103 sweep's per-cell seeded `persist.dat` from leaving a backup in every cell, and the logging goes through `UUrStartupMessage` rather than the console taps.
- **#84 gamma no-op claim verified as fact; M3rSetGamma now says so** (addresses #84): a standalone SDL probe on this host (macOS 15, Apple Silicon, SDL 2.32.70) shows `SDL_SetWindowGammaRamp` returns -1 "That operation is not supported", and the readback stays the identity ramp — the options brightness slider does nothing, under either renderer, since both route through the same `M3rSetGamma`. The call site now checks the return and logs the failure once per session instead of discarding it. The fix proper (a final-pass gamma in the renderers, or an honestly inert slider) is scoped on the issue; per-fragment shader gamma was considered and set aside because blending happens after the fragment stage, which would put additive glows in #82 territory.
- **fix(input): key bindings go positional — AZERTY and other non-QWERTY layouts get working WASD** (addresses #93): the SDL input path translated `keysym.sym`, which is the character the active layout produces, so on AZERTY the W-position key reported 'z' and the default WASD binds landed on scattered keys, leaving the game unplayable for the French player who reported it. Both key-event sites and the polled-state path now translate `keysym.scancode` against a fixed QWERTY reference for the printable block, so "bind w to forward" means the W-position key on any layout; everything that doesn't move between layouts (F-keys, arrows, modifiers, keypad, space, enter, escape, tab, backspace) still goes through the existing keycode table. `LIrPlatform_TestKey` came along with a reverse map (the exact inverse table, the old layout lookup as its fallback), so the `Oni_KeyBindings.c` dev keys and the unit viewer land on the same physical keys as the gameplay bindings instead of half the input layer going positional. Accepted trade-off, recorded rather than fixed: the engine consumes no SDL_TEXTINPUT events (see the #77 comment in `Oni_Platform_SDL.c`), so raw-keycode text entry goes positional too and dev-console typing on a non-QWERTY layout produces QWERTY letters. `ONI_KEY_LAYOUT=1` puts every one of those paths back on the old symbol mapping.
- **fix(64bit): `-Werror=incompatible-pointer-types` fence landed after the `T**`/`void**` burn-down** (addresses #76): passing a `UUtUns32*` where a `uintptr_t*` is expected is now a build failure rather than a warning, so the callee can no longer write eight bytes into a four-byte stack slot and quietly corrupt the caller's frame. The July trial had already shown the tree clean of that class, so what blocked the fence was 135 benign same-width `T**`/`void**` mismatches across 44 files, mostly `TMrInstance_GetDataPtr`-style out-params; each was checked for a width change before being cast and none turned out to be a real bug. One actual width lie did surface on the way through: `gl_engine.c` stripped const with a `(long *)` cast that was free on 32-bit and a 4-to-8-byte claim on ARM64, so it now casts to `(UUtInt32 *)` instead.

### 2026-08-25 — Session 69: sweep branch landed on main; pool-exhaustion crashes fixed (#103, #107, #108, #105)

- **Sweep harness landed on main** (530829c, closes #103): the 18-commit `feature/sweep-harness-103` branch rebased clean onto main and fast-forwarded, all eight test suites green on the current toolchain (Xcode has been removed since July; the build now runs on CommandLineTools clang, which needed a fresh CMake configure). `check-sweep-byte-identity.sh` was retired in the same landing: its invariant ended when `Oni_Sweep.c` entered the shipping target (runtime-inert without `-sweep`), the reference build can no longer compile under current clang's implicit-declaration errors, and the script's own cleanup trap had a quoting bug that executed file paths as commands. Shipping inertness stays covered by `test_sweep_core.sh`'s 46 shipping-config assertions.
- **fix(objects+weapon+ai): NULL-check pool allocators at the five unchecked sites** (addresses #107, #108): the 40-slot object list and the physics context pool both return NULL on exhaustion, and five call sites fed that straight into an initialiser — the sweep's level-8 mass-kill inventory drop (#107) and the levels-14/19 `obj_create` overflow (#108) both crashed there. `OBrInit` now refuses a NULL object or a failed physics add and returns an error; `obj_create` warns once and stops; a door piece that can't be built is left NULL with the door falling back to its own animation context (the Busy open/close paths and the master/slave pairing grew matching guards); powerup drops without physics stay stationary instead of crashing, mirroring the weapon allocator's existing warning path. Audit-driven extras: `WPrPowerup_Drop` no longer marks a physicsless powerup as Moving (it would never fade out), and slave door physics is only armed together with its master (PHrPhysics_Update derefs `master` unconditionally for slaves).
- **fix(character+weapon): survive physics-pool exhaustion through the death cascade** (addresses #107): re-sweeping level 8 after the five-site fix confirmed the powerup warning firing at the old crash site, then exposed the next two layers in turn — `FindGroundCollision` deref'd the NULL physics `ONrCharacter_EnablePhysics` silently leaves behind when the pool is dry, and the weapon-drop path fed the missing context to `WPrNotThroughWall`. A physicsless weapon is now positioned by its own matrix (the fallback the display and pickup paths already use) and skips the knockaway arc; a two-agent audit of the remaining cascade and per-tick walks guarded eight more reachable sites (per-frame movement setter, jump-height reads, superninja fall handler, death-particle attach, the unstick and char-to-camera debug keys) and verified the rest of the walk surface is either already guarded or unreachable by construction. Enabling physics now warns on both failure modes instead of failing silently.
- **sweep: modal dialogs suppressed under `-sweep` via a window-manager tap** (addresses #109): the seven watchdog hangs root-caused — script commands like `text_console` reach `WMrDialog_ModalBegin`, whose event loop spins until a player dismisses the dialog, and the sweep has no player. Not an engine bug (a diary page waiting for its reader is correct in play), so the fix is sweep-side: `WMrDialog_SetModalTap` mirrors the existing warning tap, the sweep registers a tap that records the dialog id as a Warn finding and skips the dialog, and `ModalBegin` hands back `UUcError_Generic` with the out message zeroed. A twelve-site caller audit confirmed every live call site tolerates that return; nothing registers a tap in normal play. Level 8 joined the hang class the moment the #107 fix let it survive its outro (the cascade fix revealed `level_8a`'s diary page), so the tap covers it too. The verification run also re-hit the display-asleep constraint from the first-run record — a sleeping display watchdogged every GL cell in the load phase while all eight Metal cells passed — so the driver now re-fires the `caffeinate -u` wake before each GL cell instead of once per run.
- **fix(64bit): `UUrDebuggerMessage` formats with `vsnprintf`** (addresses #105): the last unbounded `vsprintf` on the diagnostic path, into a 2048-byte stack buffer with 191 call sites; same conversion `UUrPrintWarning` got during #103. Behaviour-identical for anything that fits.

---

### 2026-07-26 — Session 68: level sweep harness — built, first run, first baselines (#103)

- **Level sweep harness landed** (addresses #103, 17 commits): a `-sweep <level>` mode loads a level with drawing bypassed, then spawns every character setup, instantiates every particle class, settles the AI, and calls every zero-arity script-defined BSL function the level registers, recording everything the engine says to NDJSON with stable normalised keys. `scripts/sweep.sh` forks one `OniSweep` process per level per renderer inside a scratch working directory holding a throwaway `persist.dat` — the guard that keeps the corpus's 56 `save_game` calls off the player's real saves and pins persisted difficulty to the default (#106) — and `tools/sweep_diff` gates the merged findings against `tests/sweep-baseline-{gl,metal}.txt` with regression/stale/abort verdicts. Cells are judged by their reports, not exit status (`main()` returns 0 even when init fails); watchdog timeouts, report-size cap breaches, silent deaths and Metal→GL fallbacks are injected as gating abort records.
- **Sweep mode skips the pre-game dialogs** (28f687c): the crash-recovery prompt, update check and first-run picker are modal and share state with real play sessions (the App Support `.session-active` sentinel); a crashed cell would have hung every later cell on the recovery prompt. Same runtime-gate pattern as the intro movie.
- **First run results** (30 cells, ~90 min): seven of fifteen levels sweep clean on both renderers at ~25–40 s per cell with byte-identical GL/Metal finding sets, stable across repeat runs. The rest found real bugs: `ai2_smite` death-drop passes NULL physics into `PHrPhysics_Init` on level 8, both renderers (#107); BSL object creation passes NULL into `OBrInit` on levels 14/19 (#108); and five levels hang the script phase until the watchdog kills them, with `text_console` the leading suspect (#109). The maintainer's real `persist.dat` and crash sentinel were untouched across all cells.
- **GL cells block when the display is asleep** — every GL cell past level 0 hit the watchdog with the display off at exactly the point it passed display-on, while Metal was unaffected; the driver now wakes and holds the display with `caffeinate` for the run's lifetime.
- **Initial baselines committed** from the seven abort-free levels: 137 accepted findings per renderer, each annotated with its triage reason (retail data quirks, AI settle chatter, sandboxed `save_game` traffic, sweep-context artifacts). The crashing and hanging levels are deliberately excluded — aborted cells refuse `--accept` by design — and join as #107–#109 get fixed.

---

### 2026-07-25 — Session 67: warning-scan audit — uninitialised reads and dead guards

- **fix(ai): `ai2_panic` no longer crashes; the cancel form no longer re-enters panic** (addresses #104): the BSL handler passes a NULL knowledge entry to `AI2rCharacter_Panic`, which guards it at every use except the `panicked_by_hostile` priority check — so every `ai2_panic` call NULL-dereffed. The `ai2_panic <ai> 0` cancel form also fell through after `AI2rReturnToJob` and re-entered the panic it had just cancelled; it now returns early. Both inherited (`Oni_AI2_Panic.c` is byte-identical to BungieSource), and no retail BSL script calls `ai2_panic`, so this only ever fired from the dev console, mod scripts, or the planned #103 sweep.
- **fix(ui+ai+objects): warning-scan audit batch** (addresses #102): the build runs default warnings only, so a syntax-only clang pass with a high-signal warning set (uninitialised use, unsequenced modification, dead comparisons, precedence, format) was run over all 237 shipping translation units. Five reachable defects fixed: the scrollbar `WMcSBPart_Thumb` case set neither `param1` nor `param2` yet still sent the notify, so grabbing a listbox thumb shipped stack garbage as a scroll position (the listbox clamps, so the symptom was a random scroll jump, not an overrun); `WMrWindow_New`'s `goto cleanup` for an unregistered window type jumped past `window`'s assignment straight into `if (window) { ... UUrMemory_Block_Delete(window); }`, a wild-pointer free; `ai2_panic <ai> 0` never set `timer` before passing it to `AI2rCharacter_Panic`; the Directory-style listbox drew an unset `PStPartSpec *icon` for Volume items; and `OBrAnimation_GetMatrix` both dereferenced uninitialised `start`/`end` when `inFrame` sat past the last keyframe and computed `keyFrames + keyFrame - 1` when the first keyframe already exceeded `inFrame`. Six latent same-class inits landed with them (`WPrTryReload`'s return, `AMrRaySphereIntersection`'s optional-out-param coupling, material `breakable`, sound `sound_channel`, `M3rTextureMap_New`'s error code, `p3_spawn` velocity), plus three dead guards that never fired (`decal_block == NONE` comparing a `UUtUns16` against -1, `flags & FLAG == 0` precedence on the CharToCamera debug key, and six unsequenced `shade = cond ? shade = X : Y` writes in the editor ring draw). All byte-identical to BungieSource except `AMrRaySphereIntersection`, which is a fork half-fix: upstream never assigned `intersection` on the hit path at all, and the fork had already added the `UUcTrue` inside the `outIntersection != NULL` guard rather than outside it.
- **Non-finding recorded with evidence**: the 66 `%d`-with-`uintptr_t` format sites (mostly `Oni_AI2_Error.c`) are cosmetic on this target, not the #68/#95/#99 truncation class. Verified by test rather than inference — on Apple ARM64 each variadic argument occupies its own 8-byte slot, so `%d` reads the low half and every following argument including `%s` still lands correctly. The #95/#99 class is a pointer *cast* at the call site, which is a different thing. Also cleared: the `&&`-within-`||` collision tests are correct as written, `BFW_Physics`'s `collideA` chain is exhaustive, and the `error;`/`osd;` hits are Bungie's unused-variable idiom. Full list on #102.

### 2026-07-24 — Session 66: bug-hunt sweep — externally-spotted truncation fixed, queued hardening landed

- **fix(64bit): FileManager 'filename too long' path truncated the name pointer** (addresses #99): both `BFrFileRef_Set` and `BFrFileRef_SetName` cast the `char*` to `(UUtUns32)` before the `%s`, so a ≥31-char leaf filename (plausible with HD-pack files) crashed in the error report instead of printing it. Same class as #95 — call-site casts the #68 sink widening couldn't reach. Found by the session-66 truncation audit, which swept all 266 compiled translation units and otherwise came back clean (the latent TOOL_VERSION/Bink list is recorded on #100).
- **fix(objects+audio+console): session-66 audit minor batch** (addresses #98, plus the two #87 tool-path casts): console completion RightArrow guarded post-increment (read one past the names at the last entry; dev-console-reachable in shipping); `OSiPlayingAmbient_GetByID` runtime bound (assert-only before); `WPrDelete` now clears `HasParticles` (debug particle-nuke recreated particles on freed slots); melee technique `start_index`/`num_moves` validated at load (bad pair walked off the heap-sized move array); turret `attachment_count` clamped to the 16-entry arrays at class init; `Oni_Sound2.c`'s two `(UUtUns32)` enumerate-callback casts widened to `uintptr_t`.
- **fix(character): refuse a character class with no Stand animation** (addresses #97): both #56 guard sites (spawn path and `ONrCharacter_SetCharacterClass`) now also require the Stand/Standing lookup to succeed — a partial mod collection used to pass the NULL-collection check, leave `active_character->animation` NULL at activation, and crash on the first unguarded deref (`animation->actionFrame` et al.). Mod-data only; stock classes always carry Stand.
- **fix(film): validate interp distance before committing film state; clear stale interp on start** (addresses #96; also lands #87's `ONrFilm_GetFrame` edge guards): the interp-abort path in `ONrFilm_Start` used to run *after* `filmState.film`/`curFrame` and the facing snap were already written — a `playback <chr> <film> interp` from >80 units away (stock-reachable) aborted but left the character snapped and a still-playing film swapped mid-flight. The distance check now runs first. Starts also clear `ONcFilmFlag_Interp` so a Normal-mode film can't inherit a previous film's live drift countdown. `ONrFilm_GetFrame` additionally hands back neutral input for keyless films and holds the first key instead of stepping in front of it (stock films always key frame 0). From the session-66 stale-state audit — headline otherwise clean: every character/weapon/film/gamestate buffer full-clears on reuse, no AI2-class stale-pointer reads found.
- **fix(audio+cinematics): #87 batch, part 1** (addresses #87): `OCrCinematic_Stop` now clamps `inEnd` before it indexes `ONgIndexPositions[26]`, mirroring the guard `OCrCinematic_Start` already had (script-data-driven OOB read). The OpenAL `CHECK_AL_ERROR` helper polled only `alcGetError`, so per-source `al*` errors — most of its call sites — were invisible; it now polls both queues (the `alGetError` poll gated on a current context, since the init/teardown call sites run without one), and `alcCreateContext` gets `attrs` instead of `&attrs`. Remaining #87 items (Sound2 tool casts, Film_GetFrame edges, UI scroll-wheel) still open.
- **fix(bsl): fixed-array hardening across the script interpreter** (addresses #86): four unbounded fixed arrays, all previously "protected" only by asserts that compile to nothing in shipping builds. `symbolList[8]` now clamps every index (block nesting deeper than 7 parks symbols in the outermost slot with a one-shot warn — stock scripts peak at depth 6, one level below the old cliff); `statementEval[16]` keeps exact push/pop depth but clamps array access; `funcDefParams[8]` rejects a 9th formal with a proper parse error before the write (the old guard ran after it); `parseStack[64]` drops the push with a warn instead of overrunning. Folded in the same audit's class-5: the scheduler's engine-command branch computed an error then returned `UUcError_None` — it now propagates like the script branch. All inherited; matters because mod scripts are an explicit use case.
- **fix(ui): pause-screen hardening — help-page clamp + empty-diary guard** (addresses #85): `ONiPS_HelpPage_Init` copied up to 1024 HPge instances into the 10-slot `help[]` array with no clamp (its item/weapon/diary siblings all have one; `ONgPauseScreenData` is a static global, so the overrun corrupts adjacent globals); `ONiPS_Paint_Diary` dereferenced `diary[diary_page_num]` with no empty-list guard (the only unguarded `ONiPS_Paint_*`). Both inherited, mod/custom-data safety — stock data can't trigger either. Fixes mirror the sibling patterns.
- **fix(64bit): drop pointer-truncating cast in OT_Furniture error path** (addresses #95): `UUrDebuggerMessage("failed to locate instance %s\n", (UUtUns32) furn_geom_name, ...)` truncated the `char*` to 32 bits before the varargs `%s` read, so a furn-geom lookup failure would dereference a truncated address instead of printing the name. Spotted via an external review of a downstream fork that had already fixed it. Tree-wide sweep confirmed it's the only game-compiled instance of the class — the `UUrError_ReportP_Internal` sink was widened in #68, and the remaining call-site casts live in files that don't build into the Mac target (TemplateManager monolith, OGL_Common, tool importers).

### 2026-07-17 — Session 65: project audit; macOS quit/focus citizenship (addresses #83)

- **fix(platform): handle SDL_QUIT / window-close / focus events** (addresses #83): the SDL pump dropped every non-input event — Dock-icon Quit did nothing, Cmd-Q only worked where the WM key-command pump ran, and Cmd-Tab left the game simulating at full CPU with audio playing. SDL_QUIT and SDL_WINDOWEVENT_CLOSE now post the same `WMcMessage_Quit` the Cmd-Q key command uses (normal teardown, so the #74 crash sentinel clears); focus-loss/minimise sets a pending auto-pause that the heartbeat fires through the exact Escape gates (`OWrOniWindow_Toggle` behind `can_escape` + `CanEscapeKey` — never during cutscenes, cancelled if focus returns first; `ONI_AUTOPAUSE=0` disables). Deliberately does not touch LI mode from focus events — `LIrGameIsActive` flips are a live #78 candidate mechanism. Bonus: a 4th mouse button no longer aborts the event drain mid-poll. Design and the cutscene-gate verification are recorded on #83.
- **v1.3.0r5 cut and published** — signed, notarized, stapled DMG on GitHub Releases; the first release published without the pre-release flag (2026-07-09 policy, first applied here), so it shows as "Latest" and the r4-era update notifier offers it to every existing user. First release in 28 days: ~20 crash-class fixes, Metal feature-complete, HD texture-pack support (bring-your-own — nothing redistributed), crash-recovery dialog, macOS quit/focus citizenship (#83). Notes are maintainer-authored (`../docs/release-notes-r5.md`); #78 shipped as a known-issue line.
- **Five-agent project audit** (docs commit `ab4599f` + tracker pass): README/CHANGELOG/wave-plan synced to reality (chapters 1–9, Metal feature-complete, HD packs on the landing page), HISTORY session 52/53 headings restored, new tracking issues #89 (Metal M5), #90 (chapter 10–14 march), #91 (persist downgrade guard), #92 (log-gating batch), stale labels dropped (#43/#46/#63), #16/#49 bodies corrected, #78 record reconciled, controller branch pushed. Maintainer decision: the r5 cut moves ahead of Waves 2–3.

### 2026-07-11 — Session 64: onipack — native Mono-free texture-pack pipeline (#88)

- **feat(onipack): pipeline swap — pack + verify without OniSplit/Mono** (addresses #88): `build-hd-overlays.sh` now packs with `onipack import-sep` and verifies with the txmp-format-index + kanabo cross-parse harness; Mono and OniSplit are gone from the script. The #63 alpha guard lives in one place (the tool), fed a 3-column retail index whose cross-level format conflicts still resolve alpha-wins, and `--alpha-guard` hard-fails on an empty index — the silent no-op failure mode the shakedown caught. Full-run parity: zero mono processes, the same 5 ALPHA-SKIPs (now tool-side), output byte-identical to the A/B-verified pack, `verify-pack: OK (1242 named TXMPs)`.
- **feat(onipack): import-sep CLI + CMake target + alpha-downgrade guard** (addresses #88): drop-in for `mono OniSplit.exe -import:sep` — sorted dir scan, group ingest for every input, #62/#63 guards with per-item SKIP reporting, level/suffix parsed and validated from the output filename. `make onipack` builds `build/bin/onipack`; the CLI test covers fixtures including an animated pack.
- **feat(onipack): VR31 Mac-family pack writer** (addresses #88; hardening follow-up in the same session): emits the .dat/.raw/.sep triple with OniSplit's layout — aligned tables/records/blobs, preamble fileIDs, sorted name descriptors, reference-pack template checksums — via atomic .tmp+rename with .dat last. Animated groups pack whole: frames as unnamed Unique instances, TXAN records with maps[] remapped to pack indices. The review pass added uint64 size caps before allocation (the wire offsets are u32 by format), fclose checking so disk-full aborts before rename, and validate-before-mutate ownership semantics on both add paths.
- **feat(onipack): V32 .oni reader** (addresses #88; group ingest + hardening follow-ups in the same session): parses OniSplit single-texture exports and animated TXMP+TXAN groups — the 14 multi-instance mod files the original #62 guard would have dropped, which the proven CuratedHD pack includes (13 TXANs, 129 unnamed frames). All 1,382 staged HD-mod .oni files parse. Discovery en route, pinned in three sources: TXAN `maps[0]` is the literal 0 by convention, meaning "the base texture" (Bungie's own comment in Motoko_State_Draw.c; OniSplit's TextureImporter3.cs writes it; every proven-pack TXAN carries it) — the writer passes it through verbatim and remaps only nonzero entries.
- **feat(onipack): wire-format header + ground-truth-anchored unit tests** (addresses #88; review fixes same session): VR31/VR32 header layout, descriptor/TXMP/TXAN field offsets, fileID hash and texel-size math pinned against BungieSource, OniSplit source and on-disk retail + CuratedHD bytes — including the DXT1 sizing fork (OniSplit floors mip tails where the engine ceils to blocks; the reader slices with OniSplit semantics, engine expectations stay separate).
- **Stage 0 — Kanabo referee** (addresses #88): Iritscen's Kanabo added as an independent-reader reference plus the `verify-pack.sh` cross-parse harness (our txmp-format-index vs Kanabo, name-set diff). Kanabo's untested fmt-7 decode was empirically confirmed R/B-swapped, so the referee compares inventories only for fmt-7. Shakedown bonus: our pack eliminates the 56 Kanabo warnings the OniSplit-built pack triggers — those turned out to be 56 truncated 172-byte TXMP records OniSplit propagated from its inputs, which our writer normalizes to the full 192.

### 2026-07-11 — Session 63: crash-recovery UX (#74)

- **feat(ux): crash sentinel + next-launch report dialog** (addresses #74): `.session-active` is written to App Support once the pre-window dialogs pass and removed at the end of clean teardown; if it survives to the next launch, a native alert offers **Report on GitHub…** (browser opens the new-issue compose page pre-filled with version, build stamp, renderer, macOS version and the last ~30 lines of `startup.txt`, capped ~7 KB encoded; the newest recent `Oni-*.ips` is revealed in Finder for one-drag attaching), **Show Logs**, or **Dismiss**, with a don't-ask-again suppression preference. No tokens, no auto-filing — the user sees the whole report in the compose page. New `Oni_CrashReport_macOS.mm` mirrors the update-notifier shell; hooks in `Oni.c` order the startup so the data picker's and update check's clean `exit(0)` paths can never arm the sentinel. Sentinel lifecycle verified headless (5-check standalone harness); dialog + report path await an in-game crash to confirm.

### 2026-07-10 — Session 62: Metal fogged-additive FX parity (#82)

- **fix(render): Metal — force fog off for additive-blended draws** (addresses #82; user-verified in play next morning, level corrected to 10): the level-10 escape-shaft energy FX rendered as hard white rings + an opaque blocky "staircase" glow under Metal while the maintainer's same-install GL A/B showed the intended soft blue — GL forces fog off for every additive-blended primitive (`gl_utility.c:1901`, "force fog off!") and Metal bound its fog uniform unconditionally. The shader fogs RGB toward the 0.25-grey fog colour but leaves the alpha mask intact, so fogged additive glows became flat grey shapes whose stacked overlaps accumulated to stepped white — a one-line gate in `metal_select_textures` restores parity. A subagent GL-vs-Metal trace first refuted the tempting candidates (blend-factor plumbing, tint submission, converter failures, sampler filtering — all faithful to GL) before fog emerged as the sole divergence.

### 2026-07-10 — Session 61: stuck strafe investigation (#78); patrol-targeting SIGSEGV fixed (#79)

- **fix(ai): audit-residue hardening — melee pointer NULLs + combat-enter bail-path init** (addresses #80): the audit's final tranche cleared every remaining AI2 state (patrol, pursuit, alarm, panic, neutral, idle, guard, teamBattle, turret targeting — all init-before-read; full report on #80). Two inherited non-critical residues landed: `AI2rMeleeState_Clear` now NULLs `target`/`last_target`/`position_current_transition` (compared-before-init stale garbage, comparisons only today); `AI2rCombat_Enter`'s no-target bail path now initializes the union-resident sub-states before returning — it used to leave them stale with the state marked begun, so a later `AI2rCombat_Exit` would have called `callback->rSetAimSpeed` through a garbage table (unreachable today, defused anyway).
- **fix(ai): initialize `alarm_console` in `AI2rManeuverState_Clear`** (addresses #80): found by the post-#79 same-class audit (Opus agent sweep of every AI2 state-union struct). `AI2tManeuverState.alarm_console` is never assigned anywhere in the tree and the combat-entry clear skipped it — a disarmed guard choosing FindAlarm (RunForAlarm behaviour, or no-gun→alarm fallback) took the specific-console branch on stale union garbage and dereferenced it. NULL init routes it to the intended nearby-console search. Inherited from Bungie, port-exposed by the 64-bit union re-layout. Audit also confirmed the rest of combat+maneuver clean (behaviour-state union is pointer-free; melee sub-struct clean).
- **fix(ai): initialize `target` in `AI2rTargeting_Initialize`** (addresses #79): hard SIGSEGV during the maintainer's level-9 Metal playtest — `AI2iTargeting_UpdatePrediction` dereferenced `0x00000000d6e4d35e` (upper-32-zero truncated-pointer signature). The patrol shoot-at-flag path shoots at a *point* and relies on `AI2rTargeting_Update`'s `target == NULL` branch, but `Initialize` (byte-identical to BungieSource) never set `target`, and the targeting state lives in the AI state-buffer union — stale non-NULL bytes routed Update into character prediction. Benign under the 32-bit union layout; the 64-bit layout lands the 8-byte `target` on live stale data. One-line NULL init; combat/turret users set a real target explicitly and are unaffected.

### 2026-07-10 — Session 61 (earlier): stuck strafe during creep — investigation opened (#78)

- **diag(input) v2 — mode-transition + production-liveness tracing** (addresses #78): maintainer reports full keyboard release does NOT clear the stick and later input can't unstick it — that kills the simple lost-keyUp theory (a re-press would reset the scancode) and fits a **frozen input pipeline**: `ONiGameState_ScanButtons` replays the last `buttonIsDown` snapshot whenever a heartbeat tick has no action (`Oni_GameState.c:3954`), and BOTH the producer (`mac_get_input_in_game`) and consumer (`LIrActionBuffer_Get`) gate on `LIgMode_Internal == LIcMode_Game` — a spurious mode flip (console/Bink/dialogs/pause, or `LIrGameIsActive(false)` via SDL's mobile-oriented `SDL_APP_WILLENTERBACKGROUND`) freezes gameplay input at "whatever was held" while the world keeps running. Trace now logs LI mode transitions with caller address, production STOPPED/RESUMED edges, and a 600-poll liveness heartbeat.
- **diag(input) — `[input-trace]` instrumentation + `ONI_TEXTINPUT_KEEP` A/B lever** (addresses #78): maintainer repro on the first post-#77 build — hold crouch (shift) + sidestep (q/e), release the sidestep, character keeps strafing. Static analysis can't yet attribute it to #77 (SDL's keyUp path is outside the text-input gate; the newly-flowing auto-repeats are inert in game mode since `LIrInputEvent_Add` early-returns there and movement is rebuilt per poll). New env-gated diagnostics discriminate the two candidate mechanisms: `ONI_INPUT_TRACE=1` logs the raw SDL key-event stream (sym/scancode/repeat/mod — a lost keyUp shows as DOWN with no UP) and per-poll edges of the translated oni-key set (engine latch shows input going clean while the character keeps moving). `ONI_TEXTINPUT_KEEP=1` skips the #77 `SDL_StopTextInput()` for a one-run causal A/B.

### 2026-07-09 — Session 60: macOS accent picker over gameplay (addresses #77); #68 residual sweep; Metal march verdicts

- **User completed TCTF HQ (level 8) under Metal with CuratedHD** — first full level march under the Metal renderer (Apple M4 Max, 2560×1440). Log-verified: 512²-class HD textures converting (scratch grew to 1 MB, confirming #60's fix in action), zero `[metal-tex]` converter failures, pack overlay registered without level hijack or SIGSEGV through a level-8→9 transition (confirming #62), 93 deferred env-map refs healed by the session-58 bridge fixups (`envksface`/`strikerarmor` → `level8_Final.dat`), OpenAL cache 94.2% hit ratio, clean exit. Daodan spike flare renders correctly ("beautiful" — maintainer). Labels dropped on #60/#62.
- **fix(logging) — `[Metal] env-map draw` sampling gated** (addresses #70): the same run exposed a sixth ungated family — the M3-era env-map draw diagnostic's every-600th sampling still produced **42,343 lines** in one played level. Now `UUrDiagVerbose()`-gated; the first-draw one-shot stays unconditional.
- **fix(64bit) — last four truncating casts into the #68 error path** (addresses #68): the #68 widening fixed the *params*, but four compiled call sites explicitly cast their `char*` to `(UUtUns32)` before the call — truncating client-side where the widening can't help (`BFW_FileManager_Common.c:232`, `BFW_Group.c:679`, `BFW_ScriptLang_Token.c:610/618`). All now `(uintptr_t)`; the `-Wpointer-to-int-cast` warnings the #77 build surfaced are gone. Remaining grep hits are in files no target compiles (`BFW_TemplateManager.c`, dead `OGL_DrawGeom_Common.c`) — left as-is.
- **fix(input) — stop SDL text-input mode at init** (addresses #77): SDL2 enables text-input mode at video init and the game never turned it off, so the macOS backend fed every keyDown through the Cocoa text system (`interpretKeyEvents:`) for the whole session — holding a movement key popped the press-and-hold accent picker over gameplay, and its 1–9 selection row swallowed weapon/slot keys while up. SDL's own in-process `ApplePressAndHoldEnabled: NO` registered default demonstrably doesn't hold on macOS 15 (observed with no user override in any defaults domain). The engine consumes zero `SDL_TEXTINPUT` events (all text UI, edit fields and console included, is raw-keycode `SDL_KEYDOWN`), so `SDL_StopTextInput()` once after `SDL_Init` removes the popup path entirely with no input-behaviour change; SDL only re-arms text input on focus-gain if that event state is re-enabled, so the single stop holds for the session. Verified against the shipped SDL 2.32.10 source.

### 2026-07-09 — Session 59: release correctness wave (issues #66–#72 + #28)

- **fix(logging) — `[TM-SNDD]` gated too** (addresses #70): the first post-gating session log (user GL run, TCTF HQ) confirmed rotation (95 MB → `.old`) and all four families silent — but exposed a fifth ungated family, the `[TM-SNDD]` bridge-translate byte dumps (383 lines/session). Same `UUrDiagVerbose()` gate applied. Same run behaviourally confirmed the #72 banner (`v1.3.0r4 [main@a1c1ae2]`).
- **chore(release) — polish batch** (addresses #72): deployment target pinned to macOS 15.0 in CMake (was riding the build host's SDK — `otool` verified `minos 15.0`); `LSApplicationCategoryType` games category added to Info.plist; README fresh-clone build recipe fixed (`mkdir -p build`) and the asset-claim wording corrected (bundle carries Feral's freely-distributed intro/outro cinematics); RELEASING.md records the no-`--prerelease` policy (the update notifier doesn't filter on the flag); session-start log banner now carries the release version (`v1.3.0r4 [branch@sha]`) via a configure-time `ONI_VERSION` define read from Info.plist.
- **fix(hardening) — level-load NULL guards** (addresses #71): `FXrEffects_LevelBegin` dereferenced the laser-contrail texture lookup unchecked at every level begin (modded/partial data with the texture missing crashed on entry — the draw path itself tolerates NULL); `PSrPartSpec_Draw` now early-returns on a NULL PartSpec or private data (callers like the cinematic border cache `TMrInstance_GetDataPtr` results unchecked) — missing UI/FX assets degrade to invisible instead of crashing.
- **fix(64bit) — WM private-data slots sized for pointers; modal-dialog channel widened** (addresses #69): eight window-class registrations (`Picture`, `PopupMenu`, `Button`, `CheckBox`, `RadioButton`, `ProgressBar`, `Desktop`, and Oni_Windows' literal `4`) declared 4-byte private-data slots that `WMrWindow_Get/SetLong` accesses 8 wide — now `sizeof(uintptr_t)`. `WMrDialog_ModalBegin/ModalEnd`'s result channel (`out_message`) widened `UUtUns32`→`uintptr_t` (it round-trips a `char*` for `WMrDialog_GetString` and an `OBJtObject*` for `OWrSelectObject`); the compiled callers' out-locals retyped (5 in Oni_OutGameUI, the two pointer round-trips in Oni_Object.c / Oni_Win_Tools.c), and the two `WM_ListBox` leaf-name casts fixed (`(UUtUns32)` truncated the pointer before `AddString`'s already-`uintptr_t` param). Verified clean with a temporary `-Werror=incompatible-pointer-types` + `-ferror-limit=0` fence build: **zero** width-changing pointer mismatches remain in compiled code. The *permanent* fence is deferred — ~139 pre-existing benign `T**`↔`void**` mismatches (same diagnostic, no width change) block it; tracked as issue #76. Discovery en route: `Oni_Win_Sound.c` isn't in the build at all (its 18 ModalBegin calls are dead text; only `Oni_Win_Sound2.c` compiles).
- **fix(64bit) — `UUrError_ReportP_Internal` params widened to `uintptr_t`** (addresses #68): the three value params were `UUtUns32`, so every `char*` routed through a `%s` in a `UUmError_ReturnOnErrorMsgP` message was truncated to 32 bits at the prototyped call and then dereferenced by `sprintf` — the error-reporting path itself crashed on 64-bit. Integer callers are unaffected (AArch64 passes the first slots in 8-byte registers; `%d` reads the low 32 bits exactly as before), so `%d` keeps working and `%s` starts working.
- **fix(logging) — 10 MB log rotation + verbose diagnostics gated behind `ONI_DIAG_VERBOSE`** (addresses #70): append-mode logs grew unbounded (91 MB dev `startup.txt` observed) — `iOpenLogFile` now rotates any log past ~10 MB to `<name>.old` on both the cwd and `~/Library/Logs` paths. The four unconditional diagnostic families (`[HEALTH-OVER-DBG]` ×6, `[SNDD]` ×3 per sound instance, `[BD]` per binary-data instance, `[PrepFM-*]` per-descriptor + the per-frame tripwire snprintf in the run loop) are gated behind a new `UUrDiagVerbose()` check (`ONI_DIAG_VERBOSE=1` env var) — tags stay in the code per `feedback_keep_diagnostics`; the `[Tripwire]` **CORRUPT** line and `[RG1]` one-shots stay unconditional. The stale `>=9335`/`>=9000` PrepFM hunt filters are gone; the raw block now logs only the original hunt's signal (`templatePtr == NULL`).
- **fix(render) — Metal uploads fmt-7 as RGBA bytes, not a BGRA word** (addresses #67): the Metal backend still carried the pre-#63 assumption that ARGB8888 texel data is a native little-endian word (= B,G,R,A bytes) and uploaded it as `MTLPixelFormatBGRA8Unorm` — the same R/B swap the GL path fixed in `c8f1c02` (blue face / olive glass), reproduced under Metal with HD-pack 32-bit textures. fmt-7 is R,G,B,A byte order on the wire, so the `BGRA8_Native` upload kind is retired and fmt-7 routes through the straight-copy `RGBA8_Native` path. Also mirrors GL's one-shot `[gl-tex]` warning as `[metal-tex] NO pixel converter` so a missing converter pair can never present as mystery-white again. Static fix — needs a user Metal run with CuratedHD to confirm on screen.
- **fix(data) — missing `.raw` companion fails level load cleanly** (addresses #28): the `TMtInstanceFile` struct comes from non-zeroing `Block_New`, and a raw-map failure NULLed only `rawPtr` — `rawMapping` stayed garbage for dispose to `UnMap`, and every SNDD's raw offset later resolved against a NULL base into a bogus pointer dereferenced at first sound play. Now: companion fields are NULLed at allocation, a missing/unmappable `.raw` refuses the load with a named `[tm]` log + "reinstall the game data" error (Feral 1.2 parity), and the SNDD LoadPostProcess resolve leaves `data` NULL when there's no raw base so playback guards fire instead.
- **fix(data) — .dat header validation runs in release builds** (addresses #66): the four consistency checks on a just-read instance-file header (name/instance descriptor counts, block-offset ordering) were `UUmAssert`s — compiled out in release, so a truncated or corrupt `.dat` SIGBUSed on the descriptor walk with no message. Promoted to `TMcError_DataCorrupt` returns, with 64-bit intermediates so hostile counts can't wrap the arithmetic that validates them.

- **fix(file-io) — the IO layer reports real errors again** (addresses #66): `BFrFile_Read`/`BFrFile_Write` computed a short-read/short-write error and then returned `UUcError_None` unconditionally (fork-introduced; BungieSource returned the real code) — truncated or corrupt data files parsed as uninitialised heap downstream instead of failing at the read. Also: `BFrFile_Map`'s mmap path now checks `MAP_FAILED` (mmap never returns NULL), and `BFrFile_ReadPos` pre-checks `pos+len` against the actual file length (fseek past EOF "succeeds") with a `[file-io]` log line on violation.

### 2026-07-07 — Session 58: fmt-7 R/B channel swap fixed — blue face / olive glass (addresses #63); anisotropic filtering (addresses #65)

- **#64 neural-upscale pilot: built, played, parked.** Full offline pipeline (retail level-8 TXMPs → Real-ESRGAN ×4 at 60% blend, alpha/shininess masks upscaled separately, formats rewritten fmt-0→7 / →8, env-links preserved through OniSplit XML round-trip) produced a working 513-texture `level8_NX1` overlay — log-verified rendering at 512², first at-scale exercise of the #63 fmt-7 converter (clean). User A/B verdict: perceptually marginal at gameplay distance (characters are screen-density-limited; flat env art has nothing to recover) — CuratedHD's hand-authored art is the better win; #64 closed, pipeline preserved as `scripts/experimental-neural-pack.sh`. The pilot's real payoff: its A/B isolated *filtering* as the visible bottleneck → #65.
- **revert(render) — aniso removed same session** (addresses #65): user verdict on the vanilla+aniso A/B run was "didn't really see the difference" — crop analysis showed receding surfaces holding texture, but it didn't register in play, and a change nobody perceives isn't worth carrying. Clean revert of the commit below; #65 stays closed with the verdict recorded.
- **feat(render) — 16× anisotropic filtering in the GL path** (addresses #65). Found via the #64 neural-pack pilot A/B: 512² textures were provably sharp facing the camera (door-sign crops) yet indistinguishable from vanilla at any distance or grazing angle — plain trilinear collapses oblique surfaces to low mips regardless of source resolution, so filtering (not content) was the visible bottleneck. Init queries `GL_EXT_texture_filter_anisotropic` once (`gl->max_anisotropy`, startup-logged); every mipmapped texture upload sets `GL_TEXTURE_MAX_ANISOTROPY_EXT` to the device cap. Benefits vanilla and all packs alike.

- **fix(render) — ARGB8888 converter decoded the wrong byte order.** User verdict on `fa6950f`'s residual captured with screenshots: Konoko's face rendered pale BLUE and the door glass olive-GREEN — the exact signature of an R/B swap with G and A intact (which is why shading and shininess looked right). Root cause confirmed at the byte level: fmt-7 TXMP texel data is **R,G,B,A byte order** (OniSplit `TextureFormat.RGBA = 7`, `Color.WriteRgba`; pack source `TXMPIteration001%2Fk4_head.oni` row-160 skin pixels read `e9 b5 90` = (233,181,144) with R first), but `IMiConvert_ARGB8888_to_RGBA_Bytes` decoded each texel as a native little-endian ARGB *word* via `IMmARGB8888_to_argb` — extracting R from where B lives and vice versa, rendering skin (233,181,144) as (144,181,233) blue. The source bytes are already exactly `RGBA_Bytes` layout, so the fix collapses the converter to a straight `UUrMemory_MoveFast` copy, with the wire-order fact documented at the function.

### 2026-07-07 — Session 57: adversarial verification of the #63 record; two blind spots instrumented (addresses #63)

Re-verified every session-55/56 claim against the binary, logs, pack contents, and engine source before continuing the white face/door hunt. Most claims held (env-diag genuinely ran in level 8 with the pack loaded; `envksface` is only a name-only *import* descriptor in the pack, not an override; `KS_face` absent / `KS_neck`+`foofhair` present as recorded). Two load-bearing claims failed verification, both baked into the planned "one decisive run":

- **The "white NONE fallback" premise is wrong: retail `NONE` is a 4×4 pure-black DXT1** (extracted from `level0_Final.dat`, every texel `#000000`). A NONE-bound surface should render black under the modulate pipeline — so if the face/door do bind NONE, the *white* must come from a render-side failure (e.g. GL texture-incomplete → texturing disabled → lit vertex colour), not from placeholder content.
- **`[tex-diag]` can never see the door:** runtime door drawing (`OBJiDoor_Draw`) is entirely `#if TOOL_VERSION` (0 in game builds) — shipped doors are gunk drawn via `M3rEnv_DrawGQList`, a path with no diagnostic. The planned run would have named the face's binding and silently said nothing about the door.
- New corroborating datum: the 09:20 env-diag run (level 8, pack active, white face on screen during the bike-confirm cinematic) logged **no** `KS_face` line — so the white face was drawn with `envMap == NULL`, i.e. it was *not* carrying vanilla KS_face's baked env link. Consistent with a resolution/binding failure, not an env-map effect.
- **`diag(textures)` — two new capped diagnostics** to make the next run decisive on both symptoms: `[bridge-null]` in `iBridgePrepare_ResolveTemplatePtr` (`BFW_TM_Game.c`) logs every named cross-file ref that resolves NULL at load — previously written silently (`(void)` on the lookup); `[env-tex-diag]` in `MS_GC_Method_Env.c` logs each unique env-GQ texture bind (name/dims/format/flags, NULL slots called out) — the path doors actually render through. Build + `.app` verified to carry all three diag strings.
- **White face ROOT-CAUSED from the diagnostic run (user single pass, level 8, OGL).** `[bridge-null]` named 91 unresolved refs — 64 of them the *pack's own* envMap refs (`strikerarmor`×35, `envmap`×27, `envksface`×2, all owned by `level0_HD1.dat`). Level 8's Konoko is the **K4 body set**, and the pack overrides `Iteration001/k4_head` (256², ARGB8888, `ReceivesEnvMap`, alpha-mean 21/255 — byte-identical shininess semantics to retail's ARGB4444 k4_head, alpha-mean 22). The overlay file is loaded and prepared **first**, so its envMap refs resolve NULL permanently (`preparedForMemory` guards re-prepare): with the env link nulled, the draw falls from the 2-pass env path (alpha = shininess, opaque base) to single-pass (alpha = **transparency**) — Konoko's head renders ~92% see-through and the bright scene reads as "white face/hair". The mod content is innocent; sessions 55–56 chased `KS_face` (the *other* body set, unused in the airport).
- **`fix(64bit)` — deferred cross-file ref fixups** (`BFW_TM_Game.c`): named TemplatePtr refs that resolve NULL at prepare are now recorded (owner file, slot, tag, name) and retried after every full prepare pass, so refs into later-loaded files heal (pack→level0 at startup, pack→levelN at each level load); on file unload, fixups whose slots die are dropped and fixups whose resolved target dies are re-NULLed + re-pended. Restores env links (reflection sheen) for all 60+ alpha-carrying pack character textures, not just the face. Pending in-game verification; the white door's mechanism is still open (may or may not be the same class — `[env-tex-diag]` + fixup logs will discriminate).
- **User verdict: still white** — the fixups verifiably healed (`[bridge-fixup] resolved … envksface`, and tex-diag now shows the pack k4_head with `env=envksface`) but the head rendered solid white in a **dark** garage, killing both the transparency and bright-reflection mechanisms. Level identity corrected by the user: **level 8 is TCTF Headquarters, not the airport** — the older "correct glass windows (AIR_GLASS)" attribution was impossible (AIR_* isn't in level 8; the correct windows were retail `HQ_GLASS*`).
- **REAL root cause — missing pixel converter (`fix(render)`):** the white set is exactly the pack's **ARGB8888 (fmt 7)** textures (`k4_head` face/hair, `GLASS04B`/`TCDOORGLASS` door glass); the fmt-8 BGR bulk is fine. `IMrImage_ConvertPixelType` has no `ARGB8888 → RGBA_Bytes` entry — no retail texture was ever 32-bit, so Bungie never wrote it, and the HD pack ships the first ones. The GL upload's per-level convert fails silently (release assert), `glTexImage2D` is skipped for **every** mip level, the texture object stays data-less → incomplete → fixed-function GL disables sampling → solid white in any lighting, no log lines. Fix: `IMiConvert_ARGB8888_to_RGBA_Bytes` (mirrors the ARGB4444 sibling, uses the in-tree `IMmARGB8888_to_argb` macro) + dispatch-table row + a one-shot-per-type `[gl-tex] NO pixel converter` warning so a missing pair can never present as mystery-white again. Content verified innocent: pack k4_head's alpha/RGB grids match retail cell-for-cell; level-8 `envksface` is dark (14/255).

### 2026-07-06 — Session 56: HD-Screens red menu fixed (vanilla-revert); red bike + white face reframed (addresses #63)

The red main-menu chrome was **not** an engine channel-swap and **not** the env-map/alpha/sky theories — it's HD-Screens shipping menu widgets with R and B swapped in the mod data. Proven statically: OniSplit's BGR path round-trips faithfully (blue TGA → BGR .oni → blue) and the engine's `IMiConvert_RGB888_to_RGB_Bytes` reads R from byte[2] just like OniSplit, so the engine renders the stored bytes correctly — the stored bytes are simply wrong. Also eliminated the `SwapBytes`/`M3cTextureFlags_LittleEndian` bit (both `0x1000`) as the cause: a mis-flagged 32-bit texture would come out G/R-scrambled, not cleanly red↔blue.

- **`fix(textures)` — menu chrome reverted to vanilla** — HD-Screens' menu widgets are a **same-resolution restyle** (buttons 128×64, navi 128×128, options/kanji 256² — all identical to vanilla), so reverting costs no quality. New `TEXTURE_EXCLUDE="buttons navi"` in `build-hd-overlays.sh` drops the swapped widgets from the overlay so the engine loads the vanilla-blue base. Rebuilt 1,242 instances, reinstalled. **User-confirmed** the menu is correct. HD-Screens' splash/level-intro/win screens (its actual content) are kept.
- **`fix(textures)` — red bike R/B-corrected (keeps HD):** the intro bike `TXMPMOTORCYCLE02` (24104-HQ-Airport, BGR) shipped a red body (`#BC2A2A`) that R/B-swaps to a coherent blue "RONIN" bike — genuinely HD (512²), so kept and corrected rather than reverted. New `hd-pack-corrections/` layer + `scripts/build-hd-corrections.sh` generator; the build stages corrections last so they override the mod. This **invalidated the prior session's premise** that the bike had no overridden texture — the whole "shared sky reflection" white-face theory rested on that false premise, which is why the sky exclusion never fixed the face.
- **White face reframed + instrumented, still open:** ruled out with evidence — the face texture (vanilla; mod `KS_face` guard-excluded), its env source `envksface` (vanilla, brightness 16/255, **not** overridden), base_alpha (retail hair/neck are DXT1, already opaque), head content (skin tones, not white), head flags (no additive/self-illum), and channel-swap. The 2-pass env-map math is `final = base + env·base_alpha`, so a white blowout needs a *bright* env — but the face's env is dark and there is **no runtime `envMap` override** in the engine (only tool-side assignment), so env-map cannot be the cause. Added a live diagnostic (`Motoko_Geom.c` — `[env-diag]` log, deduped per surface) to name what each reflective surface actually reflects in the level-6 intro; the cause is a render-time effect invisible in the files.

### 2026-07-05 — Session 55: HD-pack crash root-caused — template webs hijacked ONLV enumeration (addresses #62)

First user drive of the CuratedHD pack: menu rendered HD (overlay mechanism working) but showed an AE-branded splash, then **SIGSEGV loading Syndicate Warehouse**. Systematic debugging, root-caused in one pass — the crash report's new `1.3.0r4+main@9003656` stamp (#46) confirmed the binary on first read.

- **Root cause (#62):** the pack was not TXMP-only — Realistic Skies ships ONSK webs embedding carrier ONLV/AKEV instances, character mods ship TRBS webs embedding ONCC, HD Screens ships TXMB splashes + a WMDD pause screen (the AE-branded art). An always-loaded, searched-first level0 overlay carrying ONLV instances hijacks `TMrInstance_GetDataPtr_ByNumber(ONcTemplate_Level, 0)` in `ONrGameState_LevelBegin` — the sky mod's carrier ONLV (environment ref empty → the 64-bit resolver silently writes NULL) got picked as "the level", and `AKrCollision_LevelBegin(NULL)` faulted at +24. Verified by disassembly of the shipped binary + byte-level name-table inspection; the debugger.txt "failed to locate instance" lines were ruled out as pre-existing furniture misses (978 in the previous pack-less session too).
- **`fix(textures)` `d12b353`** — `build-hd-overlays.sh` stages **`TXMP*.oni` only** (textures are leaf templates, no outbound web). Rebuilt + reinstalled: 1,277 TXMPs, zero foreign tags (verified), single `level0_HD1` triplet, 785 MB. Accepted v1 deltas: splashes/pause screen stay vanilla (removes the AE branding the maintainer flagged), Realistic Skies doesn't apply, character retextures apply only via same-named TXMP replacement (TRBS rebind strategy = follow-up).
- **`fix(level)` `07ce031`** — defence-in-depth engine guard: level selection now picks the first ONLV **with a non-NULL environment** (logging skipped carriers) and fails with a clean error instead of a NULL-deref if none qualifies — any future user pack with stray ONLVs degrades gracefully.

### 2026-07-05 — Session 55 (part 3): level-6 whiteout was the SKY, not the skins (addresses #63)

The alpha-mask guard (part 2) did not fix the level-6 intro whiteout — user re-verified face/hair still white with the guarded pack, normal with the pack fully off, and crucially the **motorbike recoloured red→blue** between the two. That vehicle clue reframed the whole bug: level 6 has no overridden bike texture, so a shared **environment reflection** had to be the cause. Traced to the sky — level 6 is `ONSKnight`→`night_*`, and the pack overrode all `night_*` (Realistic Skies' sky *image* TXMPs win by name even though its ONSK template was already dropped under #62). The shared sky feeds every env-mapped surface's reflection: the bike's paint sheen and Konoko's shiny anime face/hair — so both shifted together, and removing KS_face alone never helped.

- **`fix(textures)` (sky exclusion)** — `MOD_EXCLUDE` drops Realistic Skies from the pack build (skies were already out of v1 scope). Rebuilt 1,244 instances, zero sky names remain, reinstalled. Three earlier theories were killed *with evidence* before this landed (not guessed): DXT1 punch-through (OniSplit DDS shows retail hair 100% opaque), env-map base-alpha (`gl_utility.c:98-99` — DXT1 and BGR both upload alpha-less, sample 1.0 identically), and mod hair content (dark, not white). Lesson logged: character/reflection textures interact globally; the world-texture bulk is the safe HD win.

### 2026-07-05 — Session 55 (part 2): white-blowout — alpha-mask downgrades in the HD pack (addresses #63)

(Superseded by part 3 — the alpha-mask guard was correct for its class but was not this bug.) User verdicts from the first full HD run: **levels 1–5 pass with HD applied** (the #45/#44 visual gate finally closes green), level 6 intro showed a pure-white Konoko face/hair + a white glass door. Root-caused statically in one pass: startup.txt was clean (no skip/failure lines) and the mod art itself is dark with full mips — but **retail \`KS_face\` is BGRA4444 whose alpha is the env-map shininess mask**, while the mod replacement is alpha-less BGR → the engine expands A=1.0 → the additive reflective pass fires at full strength → white. Faces/hair/glass are exactly the env-mapped surface set.

- **\`fix(textures)\` \`b56a176\`** — alpha-mask guard in the pack build: new \`scripts/txmp-format-index.c\` parses TXMP name→texel-format straight from the retail .dats (~50 ms, cached TSV), and the build now drops any mod texture that would downgrade an alpha-carrying retail format to alpha-less (\`ALPHA-SKIP\` log). Exactly 5 skips (KS_face — hair is baked into it — chestpack, shoulder, two road decals); glass mods keep real alpha so HD glass survives; 1,269-instance pack rebuilt + reinstalled. Anime Face is excluded until a v2 composites retail alpha onto mod RGB. If the white door persists it's alpha-content or a vanilla level-6 bug — \`ONI_TEXTUREPACKS=0\` discriminates.

### 2026-07-04 — Session 54: HD integration + AI QoL — overlay merged to main, Metal HD-ready, character packs proven, curated pack built (addresses #16, #21, #22, #60, #61)

Wave 2/3 of the 1.0 release plan, same day as session 53. The `hd-texture-packs` branch merged to main, the Metal blocker fixed, the #61 spike delivered the best-case verdict, both AI QoL fixes landed with kill switches, and the curated HD pack was built + installed for the visual gate.

- **Merge `aecd795`** — `hd-texture-packs` (7 commits: overlay discovery/registration + the #44/#45 capacity fixes) onto main. Conflicts: `Oni.c` keeps both startup blocks (update check → pack discovery, both pre-`TMrInitialize`), CMakeLists both source pairs, HISTORY interleaved chronologically (branch entries annotated as their own stream). #44/#45 closed pending-verification; the level-1 visual gate confirms.
- **`fix(render)` `4686b7a`** — Metal conversion buffer grows on demand (mirrors the GL #45 fix: 128 MB cap, per-mip sizing, UUtUns64-safe math), the M1-era >256² skip guard deleted, and a `MetalTextureName_Failed` sentinel makes deterministic upload failures one-shot instead of per-frame retry+log. Unblocks HD under Metal — ≥438 of the curated pack's textures are >256² convert-format and were invisible. Addresses #60.
- **#61 spike — verdict CAN, no engine change.** The swizzle branches on whether the *target* is named, not on reference locality: named targets resolve via `TMrInstance_GetDataPtr` even for same-file references (`BFW_TM_Game.c:2291-2296`), name lookup walks files in registration order, overlays register first. All character TXMPs are named (verified in retail name blocks + OniSplit's `HasName` export rule) → character packs work through the existing overlay. Packaging rule: character/global textures as one always-loaded `level0_<suffix>` pack.
- **`fix(ai)` `e94a97b`** — projectile-dodge root cause: vanilla measured closest-approach distance *from the world origin* and passed a world point where a direction is expected, so dodge weights were hugely negative and NPCs never dodged (the bug Feral 1.2 fixed and Daodan's `projaware` pokes around). `cur_dist` is now the character→danger vector (length = true distance, orientation = what `danger_dir` consumers need). `ONI_AI_DODGE_FIX=0` restores vanilla. Addresses #21.
- **`feat(ai)` `49d4b73`** — Definite-contact decay floored at 300 frames (5 s): measured retail ONCC memory constants (OniSplit export, 13 classes, uniform) show a *seen* target demotes after just 180 frames (3 s) of broken LOS — the "forgets you around a corner" symptom. Strong/Weak tiers untouched. `ONI_AI_LOS_TUNE=0` restores vanilla. Addresses #22.
- **`feat(instrumentation)` `bc7a77f`** — configure-time git stamp (`ONI_BUILD_STAMP`, `ONI_BUILD_EPOCH` pattern, auto-refreshes via `CMAKE_CONFIGURE_DEPENDS` on `.git/HEAD`+`index`): session banner in both logs now reads `===== session start <ts> [main@<sha>[-dirty]] =====`, and `make oni_app` (dev only) suffixes CFBundleShortVersionString with the stamp pre-signing (update-notifier-safe — the dev-epoch guard keys on the epoch, not the version string). Ends the which-binary-wrote-this-log forensics problem. Addresses #46.
- **`feat(dev)` `8fd0c47`** — `ONI_DEV_ACCESS=1` enables developer access at launch (mirrors the `thedayismine` enable branch after `ONiInitializeAll`): console + F-keys available immediately in dev sessions without the cheat dance; unset → byte-identical. Resolves #47 under its corrected framing (SHIPPING_VERSION=1 means the shipped build's console stays opt-in via the cheat, which already works in one entry).
- **`feat(textures)` `9b4ba90`** — `scripts/build-hd-overlays.sh`: stages all 15 curated mods (Tier1-first + descending-package-ID collision policy, matching AE's install-order convention — 107 shadowed, incl. Anime Face deliberately over Pt1), compiles via OniSplit `-import:sep` into one `level0_HD1` uber-pack (1,301 instances) **plus 14 tiny per-level splash packs** — the level-end `win/fail/intro_splash_screen` TXMBs are same-named with different art per level (md5-verified), so a flat uber-pack would show one level's splash everywhere; the script detects the pattern data-driven. Artifacts (787 MB total — too big to bundle in-app; distribution decision pending) verified by OniSplit `-list` re-read (1,342/1,342 instances) and installed to `TexturePacks/CuratedHD/` for the pending level-1 visual gate.

### 2026-07-02/04 — Session 53: wave-1 hardening — the bug-hunt bounds/NULL/truncation backlog + Feral limit parity (addresses #11, #42, #51, #53–#58)

Pre-march hardening off the 2026-06-24 bug-hunt backlog, executed subagent-driven from a grounded plan (`../docs/superpowers/plans/2026-07-02-wave1-hardening.md`), one commit per issue, every commit build-verified. Release scope also changed this session: HD textures + QoL features move INTO 1.0 (see #14/#16 comments); a 4-agent investigation grounded that decision (new issues #60 Metal convert-buffer, #61 anonymous-instance spike).

- **`fix(script)` `680dc96`** — four `<=` off-by-one loops in `env_show`/`env_texswap`/`env_broken`/`env_shade` read one past the `TMtIndexArray` var-array (inherited; low-probability wild GQ write when the past-end word matched). Addresses #57.
- **`fix(render)` `72fff50`** — `MAX_BINS` 0x1000→0x10000 in the visible-sort: covers the full `UUtUns16` textureMapIndex domain, making the per-frame bin write provably in-bounds (was guarded only by a release-stripped assert). Addresses #54.
- **`fix(physics)` `5380136` + `b4060f8`** — all four collider-fill write sites (env fill ×2 incl. the 2-sided duplicate, sphere-tree volume append, phy-vs-phy) now clamp against `OBcMaxColliders` with a one-shot startup-log warn instead of overrunning the 256-entry stack buffer. The fourth site was found in implementer review, beyond the issue's three. Addresses #53.
- **`fix(spawn)` `fc75ef2`** — spawn path (`ONrGameState_NewCharacter`) now refuses classes with NULL `body`/`animations` and skips costume logic on NULL `variant`, mirroring the console path's guards; the variant-scan loop also guards per-class NULL variants. Addresses #56.
- **`fix(bounds)` `2408464` + `677ca82`** — clamped the six data-driven-count writes into fixed arrays: patrol-path waypoints (parse-all/store-64, buffer cursor preserved), neutral-character lines (same, 10), diary pages (mirrors the items-loop guard), script param exprs (both parse fns, 8), animation attack collect (2), and the review-found `variants[]` costume-candidate append. Addresses #58.
- **`fix(64bit)` `0149ad5`** — the five live `AUrQSort_32` pointer-array sites now use `qsort` with pointer-width comparators (deallocated-sound flush — whose broken sort fed a binary search that NULLs freed pointers on every level unload, i.e. a use-after-free factory; console tab-completion; perf-timer display; editor listbox + an adjacent `SetItemData` pointer truncation; particle dump). The sixth site is `TOOL_VERSION`-dead. Addresses #11.
- **`fix(64bit)` `e859ce1`** — `AUtSharedString.data` UUtUns32→`uintptr_t` (+ `AddString` param): un-truncates the `SLtSymbol*` tab-completion payload, and surfaced + fixed the same truncation of `BFtFileRef*` in the live Linux/SDL `BFrDirectory_GetFileList`. Full-tree rebuild verified no wire-format dependency (grep-audited every user first). Addresses #55.
- **`fix(64bit)` `55202a3`** — `TRrAnimation_SetSoundName` now dedupes by content and stores an owned copy: kills the monotonic-growth 8192 ceiling, the level-unload dangle, AND a previously unrecognised dangle (the table stored pointers into growable `UUrMemory_Array` elements that can move). Addresses #51.
- **`feat(parity)` `71d3f25`, `121239d`, `6c4b4ef`** — Feral 1.1/1.2 limit parity: pathfinding grid cache 256 KB→2 MB, collision limit 255→1024 (+`OBcMaxColliders`→2048 in lockstep with the new #53 guards; the vestigial `<= UUcMaxUns8` assert removed after a tree-wide 8-bit-consumer sweep), object-sort buffer 4K→10K (silent transparent-geometry drops in heavy scenes). Audit also verified the active-character limit already matches Windows (our tree is the Windows-fork baseline) — #41 closes with no change. Addresses #42, #41.

### 2026-06-28 — Session 52: first bug-hunt fixes — boss-melee pointer truncation + OpenAL cache invalidation (addresses #50, #59)

First fixes off the 2026-06-24 adversarial bug-hunt (architecture map `../docs/architecture-map-2026-06-24.md`, issues #50–#59). Both surgical, subagent-implemented + controller-reviewed, build-verified (clean compile + link of the changed TUs); behavioural confirmation still pending, so the issues stay open as `addresses`.

- **`fix(64bit): widen the combat NewAnimation animation-pointer cast`** — `AI2rMelee_NotifyAnimation` ([Oni_AI2_Melee.c:1300](OniProj/OniGameSource/Oni_AI/Oni_AI2_Melee.c)) passed `(UUtUns32) inAnimation` (a `TRtAnimation*`) as the `uintptr_t inParam3` of `AI2rCombat_Behavior`, truncating the 64-bit pointer. The only consumer that reads param-3 back as a pointer is `AI2iBehavior_MutantMuro` ([Oni_AI2_Combat.c:3617](OniProj/OniGameSource/Oni_AI/Oni_AI2_Combat.c)), which dereferences it at `:3620` — so the MutantMuro boss fight would SIGSEGV on the first combat animation in a release build (the `UUmAssertReadPtr` between is compiled out). One-token fix `(UUtUns32)`→`(uintptr_t)`; the receiver chain is `uintptr_t` end-to-end and this is the sole sender, so it's correct-by-construction. Other behaviours read the anim *type* from param-1, so levels 1–5 were unaffected. Port-exposed, not a Bungie bug. Addresses [#50](https://github.com/andiyar/OniARM64/issues/50).
- **`fix(audio): invalidate the OpenAL buffer cache on level unload`** — the port-added OpenAL buffer cache ([BFW_SS2_Platform_OpenAL.c](BungieFrameWork/BFW_Source/BFW_SoundSystem2/Platform_OpenAL/BFW_SS2_Platform_OpenAL.c)) keys on `SStSoundData*` and was only freed at `SS2rPlatform_Terminate`, never on level unload. Across a multi-level session that left freed sound-data pointers as live cache keys (wrong-sound / use-after-free on address reuse) and — since the count never reset and `Insert` stops at 50 % load — silently disabled the cache after ~2048 distinct sounds, re-introducing the #9 combat-audio lag. New `SS2rPlatform_BufferCache_FlushAll` (wraps the existing `FreeAll`) is now called from `SS2rSoundData_FlushDeallocatedPointers` ([BFW_SoundSystem2.c](BungieFrameWork/BFW_Source/BFW_SoundSystem2/BFW_SoundSystem2.c)) — the existing per-unload disposed-sound flush (`Oni_Level.c:319`), keeping layering clean — so the cache and its count clear each level. Prototype `#if UUmOpenAL`-gated (non-OpenAL builds compile it out); `ONI_AUDIO_CACHE=0` escape hatch preserved. Addresses [#59](https://github.com/andiyar/OniARM64/issues/59).

### 2026-06-20 — Session 51: Metal renderer M4 — feature-complete parity (addresses #43)

M4 closes the four remaining `DrawEngine` parity items (`screenCapture`, `pointVisible`, pixel-format coverage, gamma/HiDPI), declaring the Metal backend feature-complete with OpenGL. Plan grounded directly against each GL seam (`../docs/superpowers/plans/2026-06-20-metal-renderer-m4.md`), inline execution. The grounding overturned two assumptions the umbrella spec had carried unproven: **`screenCapture` is the in-game screenshot key, not "savegame thumbnails"** (Oni has none — the sole consumer is `iScreenShot` → `screen_shot_*.bmp`, default-bound to the unreachable fkey13, superseded by macOS system screenshots); and **`pointVisible` is *not* pure parity** — the particle lens-flare path is dead-coded (`if (0 && …)`) to engine-agnostic CPU raycasts, but the sun lens-flare path (`Oni_Sky.c:646`) reaches the always-TRUE stub, so the sun glow doesn't soft-occlude under Metal (an accepted cosmetic, outdoor-only delta — a Metal depth readback isn't worth a flare fade). Net: one safety fix + three documented parity confirmations, OpenGL byte-identical.

- **`fix(render): M4 — safe screenCapture stub + document pointVisible parity`** (`bf2f2f1`) — the `screenCapture` stub returned success over an **uninitialised heap buffer**, so a screenshot-key press under Metal wrote heap garbage to `screen_shot_*.bmp`; descoped (no per-frame readback for a dormant, OS-superseded feature) but made safe — the caller asserts `UUcError_None` and reads every byte, so the stub now returns None and zeroes the RGB888 buffer (a black BMP, not garbage). `pointVisible`/`supportPointVisible` gained a source-cited comment recording the two-path parity finding + the accepted sun-flare delta.
- **Pixel-format soak — passed on accumulated evidence, no converter needed.** `I1` and `ABGR1555` are the only two `IMtPixelType`s Metal doesn't upload — and both are non-texture under GL too (`I1` = `_gl_pixel_download_invalid` at `gl_utility.c:87`; `ABGR1555` absent from GL's table, a Mac big-endian relic), so the skip is parity, not regression. The existing `[Metal] unsupported texel type` tripwire logged **zero** hits across 19 accumulated Metal sessions spanning ~15 levels (menu through the final level 19, including `env_lensflare*` sky content) — real shipping data never carries the two unsupported texels.
- **Gamma + HiDPI — parity confirmed.** Gamma is renderer-independent shared code (`M3rSetGamma`, the SDL window-ramp in `gl_sdl.c:18`, called identically by both backends at `metal_engine.mm:229`); user-confirmed the gamma slider responds under Metal. HiDPI native-pixel drawable was already log-evidenced in M1 (2560×1440). No code needed for either.

### 2026-06-20 — Session 50: Metal renderer M3 — environment-map reflective combine (addresses #43)

Shiny character armour reflects under the Metal backend again. Plan written + grounded directly against the GL env-map seam (`../docs/superpowers/plans/2026-06-14-metal-renderer-m3.md`); subagent-driven, two-stage review per task. The grounding resolved the design's one real risk: on this Mac, GL's multitexture resolves to `_gl_multitexture_none` (`gl_engine.h:77-83` build switches — ARB + `texture_env_combine` commented out, NV combiners absent on Apple GL), so GL renders env-map in **two passes** (`Motoko_Geom.c:213-242`), net `diffuse·(base.rgb + env.rgb·base.alpha)`, opaque — the base texture's alpha is the shininess mask. Metal forces single-pass, so it computes that identical formula in **one** fragment shader. **Status: env-map confirmed firing + stable** — a ~2-min busy-combat run logged 1,336,800 env-map triangle draws (env-mapping is character armour: all single-triangle draws) with zero env-ring/main-ring overflow, zero nil-drawables, clean exit; user saw no visual anomalies in real play. Full GL-vs-Metal A/B screenshot not performed (user satisfied with the live run). Per-triangle env draw-call overhead (~185/frame in that fight) flagged as a perf follow-up ([#48](https://github.com/andiyar/OniARM64/issues/48)); batching is the existing M5 lever.

- **`feat(render): M3 task 1 — env render core`** (`364eaff`) — additive, inert infrastructure that leaves the 28-byte single-UV hot path untouched: a 40-byte `MetalEnvVertex` carrying two perspective UV sets (base + reflection), runtime-compiled `oni_env_vertex`/`oni_env_fragment` MSL computing `diffuse·(base.rgb + env.rgb·base.alpha)` with the M2 fog tail, a dedicated opaque (ONE,ZERO) env PSO, and a 2 MB triple-buffered env vertex ring. Allocated but unwired — behaviour unchanged.
- **`feat(render): M3 task 2 — env-map combine live`** (`c281378`) — `metal_private_state_update` now resolves the reflection map (`EnvTextureMap`) and selects `MetalGeom_EnvMap` (gated on `ONrMotoko_GraphicsQuality_SupportReflectionMapping()`, mirroring GL's quality gate — SuperLow still draws the M1 base-only fallback); `metal_submit_polygon` gains a dedicated branch emitting two-UV-set vertices through the env PSO, reflection→fragment texture(0)/base→texture(1) matching GL's `texture0=env`/`texture1=base`. A buffer(0) rebind guard in `metal_select_textures` keeps the env ring and main ring from cross-binding when env and normal draws interleave.
- **`refactor(render): fold MetalGeom_EnvMap into the shared switch group`** (`c41b937`) — code-review cosmetic: the env-map case sat after `default:` with a dead assignment (the dedicated branch intercepts it first); grouped with the other base-textured modes. No behaviour change.
- **`diag(render): log Metal env-map draw counts`** (`e933eb4`) — session counter on the env-map draw branch (first draw + every 600th) so reflective geometry can be located empirically from `startup.txt` rather than guessed — Oni's env-mapping is subtle (a faint armour sheen). This is what confirmed the 1.34M-draw firing above.
- **`release: 1.3.0r4`** ([Info.plist](macos/Info.plist)) — first public build carrying the GitHub update notifier (Session 49) **and** the experimental Metal renderer (M0–M3, opt-in via hold-Option chooser / `-metal`; OpenGL remains the default). Cut from `main` after merging the Metal M2/M3 work off `metal-renderer`; signed + notarized + stapled at both `.app` and DMG layers via `make oni_app_release`, published to [Releases](https://github.com/andiyar/OniARM64/releases) as a prerelease.

### 2026-06-18 — Session 49: GitHub release-update notifier v1 (addresses #40)

Notify-only update check: a released `.app` now tells you when a newer GitHub release exists instead of stranding you on a stale build (the recurring `gh release create` flow is unchanged — zero per-release burden; Sparkle-style in-app install stays a deferred v2). Built off the approved design (`../docs/superpowers/specs/2026-06-05-github-update-notifier-design.md`) on a branch off `main` so it can ship alongside the Metal renderer. Subagent-driven execution, two-stage review (spec + quality) per task. **User-verified end-to-end** (against a synthetic older `CFBundleShortVersionString` with the dev-build guard temporarily disabled): the dialog fires with the correct version comparison, **Download… opens the release page and quits** (the check runs pre-window at launch, so quitting to let the user install the new DMG beats dropping them into the old build), Skip/Later continue into the game, the ~4 h throttle suppresses repeat launches, and it does **not** collide with the hold-Option renderer chooser (the two are sequential pre-window modals — chooser resolves `useMetal` in `ONiMain` before `ONiInitializeAll` ever runs the check).

- **`feat(update): pure release-update decision core + unit tests`** (`29c53aa`) — new dependency-free [ONi_UpdateCheck.c](OniProj/OniGameSource/ONi_UpdateCheck.c) (sibling to `ONi_GameData.c`): `ONrUpdateCheck_PickLatest` (newest non-draft by `published_at`, prereleases included) + `ONrUpdateCheck_Decide` (skip-tag → version-equality with single-leading-`v` normalisation → dev-build epoch guard → Available). Comparison is **timestamp/tag-based, not version-string parsing** — sidesteps fragile `a1`/`r1`/`r3` ordering. 11 standalone TDD tests (`tests/test_oni_updatecheck.c`).
- **`feat(update): Cocoa shell — fetch/parse/decide/NSAlert`** (`41c8e72`) — new [Oni_UpdateCheck_macOS.mm](OniProj/OniGameSource/Platform_SDL/Oni_UpdateCheck_macOS.mm) (mirrors the `Oni_DataSetup_macOS.mm` split): time-capped (~2.5 s, semaphore-bounded, main-thread-safe) `NSURLSession` GET of the public releases API, `NSJSONSerialization` + `NSISO8601DateFormatter` parse into `ONtReleaseInfo[]`, then the pure core decides. `NSAlert` (Download… / Skip This Version / Later) with a "Don't check for updates" suppression checkbox; state (skipped tag / last-check / opt-out) in `NSUserDefaults`; ~4 h throttle; offline/error → silent, `lastCheck` not advanced so the next launch retries. **Download… opens the release page then `exit(0)`s** (mirrors the data picker's pre-window quit) — Skip persists the exact tag and continues, Later just continues.
- **`feat(update): wire release notifier into build + startup path`** (`af71371`) — pure `.c` added to the cross-platform source list, `.mm` APPLE-gated with ARC + PCH-skip like its siblings, configure-time `ONI_BUILD_EPOCH` baked via `string(TIMESTAMP)` (the dev-build guard; staleness only ever errs toward *showing* the notice). Hooked into [Oni.c](OniProj/OniGameSource/Oni.c) at the same pre-window, main-thread point the first-run picker uses — after game-data resolution, before `TMrInitialize` — fire-and-forget, never gating startup on the check.

### 2026-06-13 — Session 48: Metal renderer M2 — fog + engine-agnostic particle fog query (addresses #43)

Fog under the Metal backend, plus retiring the M1 `8e5a533` `gl == NULL` stopgap. Plan written + grounded against the landed M1 Metal code and the GL fog path (`../docs/superpowers/plans/2026-06-13-metal-renderer-m2.md`); subagent-driven execution, two-stage review (spec + quality) per task. The grounding caught a trap a paraphrase would have missed: the **rendering** fog and the **particle** fog-factor use *different* formulas — `gl_calculate_fog_factor` (the particle path) is internally inconsistent (clamp branches say near→0/far→1, but its interpolation term runs the opposite direction — an upstream Bungie quirk), so the shader uses the correct monotonic `GL_LINEAR` curve while the Metal particle mirror reproduces the quirk verbatim for parity. **Status: build-verified + reviewed; Metal active, shader compiled, fog system initialized, warehouse renders clean (no regression) per startup log + user screenshots. Fog-appearance + particle-fade VISUAL parity DEFERRED to a foggy level (the warehouse's `start=.99` fog has no sightline long enough to show) — not yet user-confirmed; GL-regression pass after the Task-3 shared-code changes also pending.**

- **`feat(render): Metal M2 fog state`** (`32ea879`) — new [metal_fog.mm](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/Metal/metal_fog.mm) mirrors the GL backend's fog globals (colour/start/end + smooth-ramp state) and registers the **same-named** `gl_fog_start/end/red/green/blue` script variables + `gl_fog_*_changeto` ramp commands under Metal — only registered in the GL init path before, so the 14+ level `.bsl` scripts that set them silently did nothing under Metal. Real `metal_reset_fog` (replaces the M1 stub, restores defaults + cancels ramps on level teardown), a `metal_fog_update` per-frame ramp called from `frame_start` (mirrors GL's `gl_fog_update`, including its `fog_start_delta`-in-the-end-branch upstream quirk), and `metal_calculate_fog_factor` (verbatim mirror of `gl_calculate_fog_factor`, inconsistent formula and all, for particle parity).
- **`feat(render): Metal M2 fog rendering — in-shader GL_LINEAR fog`** (`7df2415`) — the runtime-compiled fragment shader applies `f = clamp((end − z)/(end − start), 0, 1)` then `mix(fogColour, fragment, f)` (alpha untouched, fog after texture×colour) keyed on the screen-space z the vertex already carries (`v.pos.z` — the engine draws software-pre-projected screen-space verts, so this is the same quantity GL fogs against, not eye distance); per-batch fog-enable decoded from `M3cDrawStateIntType_Fog`; fog colour/range fed via a 24-byte `MetalFogUniform` (`_Static_assert`-guarded layout) set per primitive. Menu (UI at z=0.5) and GL launch unaffected.
- **`feat(render): engine-agnostic particle fog query`** (`8fe2fba`) — new `fogFactor` DrawEngine vtable method (mirrors the `resetFog` slot) + `M3rDraw_GetFogFactor` manager wrapper; GL fills it with the existing `gl_calculate_fog_factor`, Metal with `metal_calculate_fog_factor`. `P3rModifyAlphaByFogFromDepth` ([BFW_Particle3.c](BungieFrameWork/BFW_Source/BFW_Particle/BFW_Particle3.c)) now queries through the manager instead of a direct `extern` into the GL global, so particle fog-fade works under Metal; the now-unreachable `gl == NULL` guard in `gl_calculate_fog_factor` is removed (reached only via GL's vtable entry, i.e. GL-active ⇒ non-NULL). Reviewer-confirmed no DrawEngine backend leaves the slot NULL (only GL + Metal compile/register). GL runtime behaviour unchanged — additive vtable slot + provably-dead-guard removal.

### 2026-06-11 — Session 47: Metal renderer M1 — menu + in-game world render natively (addresses #43)

The Metal backend went from M0's teal clear to rendering the game. Plan written + pre-execution-audited (`../docs/superpowers/plans/2026-06-11-metal-renderer-m1.md`); the audit caught a would-be crash before any code existed (`IMrImage_ConvertPixelType` is a sparse `[dst][src]` proc table — RGB565/RGBA5551/RGBA4444 have **no** conversion entries; GL uploaded them natively as packed pixels, so routing them through the converter would assert). Subagent-driven execution, two-stage review per task. **User-verified on an M4 Max**: menu + mouse correct under the Option-chooser Metal pick, then level 2 gameplay — combat, explosions, smoke particles, HUD, in-game console text — 4,200 frames at 2560×1440, clean exit; GL default launch regression-clean.

- **`feat(render): Metal M1 render core`** (`78ff268`) — runtime-compiled passthrough MSL pair (the CPU Software GeomEngine already projects to screen space; the fragment shader divides `(u·invW, v·invW, invW)` to reproduce GL's hyperbolic q-texturing), four blend PSOs (opaque/alpha/additive/multipass-base — `glBlendFunc` parity incl. always-on blending), four depth-stencil states (`gl_depth_mode_set` semantics), 3-deep 8 MB in-flight vertex ring with semaphore-capped CPU run-ahead, real `privateStateUpdate` mirroring `gl_private_state_update` branch-for-branch (clear/depth/texture0/constant-colour/geom-mode), and `metal_apply_display_settings` — window size, `FULLSCREEN_DESKTOP` policy, gamma, native-pixel `drawableSize` (HiDPI free: the ortho uniform uses logical size), and the **`GLgGameWidth/Height` feed** so mouse coordinates scale correctly (the M0-flagged prerequisite).
- **`feat(render): Metal M1 texture system`** (`1bae7ba`, hardening `53e8f45`) — `MTLTexture` table addressed through the existing `opengl_texture_name`/`opengl_dirty` fields (shared struct untouched); native DXT1→BC1 / ARGB8888→BGRA8 / RGBA_Bytes→RGBA8 uploads; `IMrImage` conversion only for pairs its table actually has; hand expanders for A8 (**white+alpha** — Metal's `A8Unorm` samples RGB=0 where GL forced RGB bias 1.0; without this, menu text renders black), I8, A4I4, RGB565/RGBA5551/RGBA4444; mip-chain walk with the GL quality gates; and a TXMP template proc handler mirroring GL's — without it the text system's runtime glyph-cache updates (`TMrInstance_Update`) never mark the texture dirty and menu text goes stale. Review fixup: stale-slot bounds check, 256² convert-buffer guard, defined out-sampler.
- **`feat(render): Metal M1 primitives — menu renders under Metal`** (`5e49448`) — all eight draw entries through the vertex ring: triangle/quad/pent fan-triangulated with per-vertex shades + perspective-correct UVs, sprites with GL's exact quirks mirrored character-for-character (corner/UV order `[0][1][3][2]`, flat z from `points[0]`, `spriteArray` ignoring its colour parameter), lines/points opaque. `supportSinglePassMultitexture` now returns true — **load-bearing**: `M3rGeometry_Draw` (Motoko_Geom.c:213-242) otherwise calls `gl_prepare_multipass_*` directly, GL internals that dereference NULL gl state under Metal. Env-mapped geometry draws base-map-only (Bungie's own low-quality fallback) until M3.
- **`feat(render): Metal changeMode`** (`c9b3800`) — Options→Resolution routes through the shared display-settings helper. Implemented + reviewed; in-session switch not yet user-exercised.
- **`fix(render): guard GL-state reads reachable while the Metal engine is active`** (`8e5a533`) — first in-game run crashed in ~0.5 s: `P3rModifyAlphaByFogFromDepth` (engine-agnostic particle code, BFW_Particle3.c:9609) calls `gl_calculate_fog_factor` directly, which reads `gl->fog_start/fog_end` — GL state never allocated under Metal → SIGSEGV at NULL+0xacc. Systematic sweep of every `gl_*` extern / `gl->` deref outside the GL engine found two more same-class sites: the perf-overlay texture stats and the **level-19 win path's** 3Dfx check (would have crashed on completing the game). All three guarded with the same `gl != NULL` pattern the voodoo/S3 checks in gl_utility.c already use; `gl_calculate_fog_factor` answers 0.f (its own no-fog convention — also correct Metal-M1 semantics). Exposed by the second draw engine, not a GL-era bug. M2 carry-forward: replace the particle fog-alpha path with an engine-agnostic fog query.

### 2026-06-10 — Session 46: Metal renderer backend — M0 scaffolding (addresses #43)

Started the additive Metal DrawEngine backend (design spec + audited plan in `../docs/superpowers/specs|plans/2026-06-10-metal-renderer-*`): a second Motoko draw engine selectable at launch, OpenGL untouched as the default, zero Metal code compiled on non-Apple platforms.

- **`feat(render): renderer-selection plumbing`** — `useMetal` on `ONtCommandLine` ([Oni.h](OniProj/OniGameSource/Oni.h)), `-metal` / `-renderer metal|opengl` arguments and `ONI_RENDERER` env resolution in `OniParseCommandLine` ([Oni.c](OniProj/OniGameSource/Oni.c)), startup-log line for the resolved choice. Inert until the Metal engine registers (next commits); OpenGL remains the default.
- **`feat(render): Metal DrawEngine skeleton + CMake wiring`** — new backend unit [metal_engine.mm](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/Metal/metal_engine.mm): registers a "Metal" draw engine (device/queue/`CAMetalLayer` via `SDL_Metal_CreateView`), clears each frame to a distinctive teal and presents via `presentDrawable`+`commit`; every geometry/texture vtable entry is a typed no-op stub (rendering lands in M1). Reuses the GL backend's pure-SDL display-mode enumerator for an identical curated resolution list. Compiled only inside the CMake `if(APPLE)` block (with `Metal.framework`) — Linux builds compile zero Metal code. Not yet referenced by any caller; behaviour unchanged.
- **`fix(64bit-port): C/C++ layout divergence in SDL-branch empty-struct typedefs`** — first launch of the Metal path drew black, not teal: `typedef struct {}` (`UUtAppInstance` [BFW.h](BungieFrameWork/BFW_Headers/BFW.h), `M3tPlatformDevice` [BFW_Motoko.h](BungieFrameWork/BFW_Headers/BFW_Motoko.h)) is **0 bytes in C but 1 in C++**, so the ObjC++ `metal_engine.mm` read `ONgPlatformData.gameWindow` at offset 8 while every C TU laid it at offset 0 → NULL window → `SDL_Metal_CreateView("Invalid window")` → nil layer, silently swallowed by nil-messaging. Same defect skewed `M3tDisplayDevice` (mode list read off-by-padding by the C side). Both typedefs are now `void*` (mirroring the Mac/Win32 branches), and `-Werror=extern-c-compat` on the ObjC++ units turns any future divergence into a build error — clang had flagged exactly this in the Task-2 build as a "pre-existing" warning. Port-exposed: the community SDL branch's empty structs were fine in the all-C world; our first ObjC++ consumer of these headers broke the assumption. Diagnosed via boundary instrumentation (frame-bracket counters → `layer=0x0` → `window=0x0 flags METAL=0` → "Invalid window"), root-caused statically, user-verified: Metal now presents (2560×1440 layer, frames pacing correctly) and the default GL launch still loads the game post-layout-shift.
- **`feat(render): select Metal at launch`** — `M3cDrawAPI_Metal` enum; pre-window `metal_is_available()` probe in `ONiMain` ([Oni.c](OniProj/OniGameSource/Oni.c)) — the only point where Metal→OpenGL fallback is possible, since the window's `SDL_WINDOW_METAL`/`OPENGL` flag is fixed at creation and engines register after; index-0 Metal-or-GL registration switch + `M3rTerminate` symmetry ([Motoko_Manager.c](BungieFrameWork/BFW_Source/BFW_Motoko/Manager/Motoko_Manager.c)); window flag in [Oni_Platform_SDL.c](OniProj/OniGameSource/Platform_SDL/Oni_Platform_SDL.c). `ONI_RENDERER=metal` now opens a Metal window — **user-verified teal** with the boundary diagnostics kept in place (geometry stubs until M1); OpenGL remains the untouched default, **user-verified loading the game**.
- **`feat(render): hold-Option "Choose Renderer" dialog`** — new pre-SDL Cocoa unit [Oni_RendererSelect_macOS.mm](OniProj/OniGameSource/Platform_SDL/Oni_RendererSelect_macOS.mm) (sibling to the data-setup picker): holding Option/Alt at launch shows a native `NSAlert` offering OpenGL (default) vs Metal; the pick feeds the same `useMetal` resolution chain (parse → chooser → availability probe → window flag). No modifier → no dialog, unchanged default launch. Chooser-first is the validation-phase UX; once Metal proves robust it inverts into a "force safe OpenGL" escape hatch (spec §4.4).

### 2026-06-10 — Session 47 (hd-texture-packs stream): Review pass + spike-crash root cause — two Bungie-era capacity limits hardened for HD overlays (addresses #44, #45, #16)

Second-pair-of-eyes review of the branch (different model driving) found that #16's overlay mechanism would overflow a Bungie-era static the moment a real pack was installed; fixed before the Phase-2 spike rather than after the symptom. The spike drive then crashed on a *second* Bungie-era capacity limit, root-caused the same evening.

- **`fix(render): grow the GL pixel-conversion scratch for HD-sized textures`** — the re-driven spike crashed again at load completion, one layer downstream: SIGSEGV inside Apple's GL driver (`MTLResourceListAddResource` ← `gl_sprite` ← `splash_screen_internal`), driver resource list stomped with **3-byte-packed grey texel runs**. The #45-fixed temp-storage layer verified working in the same log (`[textures] temporary texture storage grown to 1398100 bytes`, level-1 `main()` script now completes; a level-4 control load with the overlay registered-but-not-loaded ran clean). Third Bungie-era fixed buffer in the chain: `gl->converted_data_buffer` ([gl_engine.c](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/OpenGL/gl_engine.c), `256*256*sizeof(long)`) — `gl_texture_map_download_generic` converts each LOD to `IMcPixelType_RGB_Bytes`/`RGBA_Bytes` into it ([gl_utility.c](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/OpenGL/gl_utility.c)) with no size check; the 512×512 base level needs 786 KB+ vs 512 KB capacity. Fix: size-tracked `gl_converted_buffer_ensure` grows the scratch on demand before the LOD loop (same shape as the #45 temp-storage fix: 128 MB sanity cap, failure → skip the upload, texture stays `opengl_dirty`). Textbook cascade — each capacity fix exposes the next retail-sized buffer downstream. Addresses [#45](https://github.com/andiyar/OniARM64/issues/45); spike re-drive pending.
- **`fix(textures): grow temporary texture storage for HD-sized .sep textures`** — the Phase-2 spike crashed on level-1 load: SIGSEGV in `AIiScript_EnvShow` with `ONgGameState->level` = `0xff686864ff4a4b49` — **two grey ARGB texels**. Root cause via disasm + spike-file hexdump + log forensics: `M3rTextureMap_TemporarilyLoad_Internal` ([Motoko_Texture.c](BungieFrameWork/BFW_Source/BFW_Motoko/Manager/Motoko_Texture.c)) streams `.sep` textures through a **fixed 384 KB buffer** (`M3cTexture_TemporaryStorageSize`, "CB: maximum texture size is 256 x 256") guarded only by a release-stripped `UUmAssert`; the spike's `WH_CONCRETE_256` (512×512 RGB888 + mips, 1.4 MB — OniSplit decompresses DXT for Mac targets) overran the heap by ~1 MB of concrete-grey texels, stomping `ONgGameState->level` and the loaded-file name tables (`failed to locate U_crate_single_2…` in debugger.txt) before `env_show` dereferenced the wreckage. **Feature-exposed**: retail data never exceeds 256×256; HD overlays do. Fix: grow the buffer on demand at load (provably empty at that point), 128 MB sanity cap for corrupt headers, allocation-failure → skip-the-texture (downstream #28 NULL guards handle it), distinct `[textures]` log lines. The crash incidentally **proved the overlay override mechanism works** — the texels in the crash registers are the overlay's own. Addresses [#45](https://github.com/andiyar/OniARM64/issues/45); spike re-drive pending.

- **`fix(textures): bound the instance-file registry + reserve base capacity`** — `TMiGame_InstanceFileRef_Add` ([BFW_TM_Game.c](BungieFrameWork/BFW_Source/BFW_TemplateManager/BFW_TM_Game.c)) wrote `TMgGame_InstanceFileRefs_List[Num++]` with **no capacity check** against `TMcInstanceFileRefs_Max` (16) — harmless for 25 years because retail data registers exactly 15 `level*_Final.dat` files, but the #16 overlay scan registers pack files into the same array: the **second** overlay file would write a ~260-byte `TMtInstanceFileRef` past the end of the static array (a 15-level pack ≈ 3.7 KB stomped at startup), with the corruption likely misread as a #30-style signature. **Feature-exposed, not a Bungie bug** — their cap was right for their data. Fix: cap 16→64; hard bounds check in `Add` (clean error instead of OOB write); matching defensive check in `TMiGame_LoadedInstanceFiles_Add` (same pattern, checked before the instance-file allocation so the error path leaks nothing); overlay scan now stops while `TMcInstanceFileRefs_BaseReserve` (16) slots remain so the GameDataFolder scan can always register the base game in full; main scan tolerates an index collision against an installed overlay (logs loudly, skips the base file — removing the pack restores it) instead of bricking boot on a suffix-hash collision, while base-vs-base conflicts and the no-overlay path hard-fail exactly as vanilla. Registry capacity events all land in `startup.txt` with distinct messages. Build clean; standalone tests 6/6 + 26/26; behavioural verification rides the Phase-2 spike (#16).

### 2026-06-06/07 — Session 46 (hd-texture-packs stream): HD texture packs Phase 1 — overlay discovery + registration (addresses #16)

Plan Phase 1 (`docs/superpowers/plans/2026-06-05-hd-texture-pack.md`, Tasks 1–2) on branch `hd-texture-packs`. Strategy pivoted from the abandoned bake-into-GameDataFolder pipeline to **runtime overlays**: packs are `level<N>_HD.dat`(+`.raw`) files under `~/Library/Application Support/OniARM64/TexturePacks/<pack>/`, registered before the GameDataFolder so their instances win first-match name resolution (`TMrInstance_GetFromName` returns first match in registration order) — no full-level re-import, so the #30 corruption path is never exercised.

- **`feat(textures): pack-directory discovery module + tests`** — new pure-libc [ONi_TexturePacks.c](OniProj/OniGameSource/ONi_TexturePacks.c) enumerates pack subdirectories under `TexturePacks/`, sorted ascending for deterministic load order, `ONI_TEXTUREPACKS=0` kill switch; standalone TDD tests (`tests/test_oni_texturepacks.c`, mirrors the `test_oni_gamedata.c` pattern).
- **`feat(textures): BFW overlay-search-dir registry + game-layer pack wiring`** — BFW stays game-agnostic: new `TMrGame_SetOverlaySearchDirs` copies directory refs into a static registry, and `TMrGame_Initialize` scans them ahead of the GameDataFolder via `TMiGame_OverlayDir_Scan` (mirrors the main scan; dup-index defensive path). The game layer ([Oni.c](OniProj/OniGameSource/Oni.c)) derives the App Support dir from the resolved GameDataFolder path, enumerates packs, and registers them before `TMrInitialize` — cleaner layering than the plan's sketch (no OniGameSource include in BFW).
- Next per plan: Phase-2 **GATE** — OniSplit-built one-texture `level1_HD.dat` spike, user-verified visually, before any native-packer investment (Phases 3–4).

### 2026-06-03 — Session 45: First-run guided data-setup picker + content-validating resolver (addresses #38)

Picked up a first-run-data-setup design from another machine; checking it against the tree showed its premise was **stale** — it claimed data is found only via a cwd-relative `BFrFileRef_Search` (so Finder launches with cwd=`/` fail), but that describes pre-`1da832f` code. The current resolver is already launch-method-independent (HOME-based App Support + `_NSGetExecutablePath`-anchored bundle Resources). Re-scoped with the maintainer to the two genuinely-missing pieces — silent-quit when no data is configured, and no content validation — built non-breaking (accept both folder names) instead of the design's "clean break" rename.

- **`feat(bundle): content-validate the game-data resolver, accept both folder names`** — `ONiBundlePath_ResolveGameDataFolder` ([ONi_BundlePath.c](OniProj/OniGameSource/ONi_BundlePath.c)) now requires each candidate to *validate as real Oni data* (holds `level0_Final.dat`, or any `levelN_Final.dat`) rather than merely exist, so an empty / wrong / double-nested folder no longer resolves and then dies downstream. Each location (App Support, bundle Resources, legacy cwd search) tries the natural retail name **`GameDataFolder`** first, then legacy **`gamedata`** — a freshly-dropped retail folder works without renaming, while existing `gamedata` installs and the dev symlink keep working. Validation lives in a new pure libc-only core [ONi_GameData.c](OniProj/OniGameSource/ONi_GameData.c) (`ValidateFolder`/`FindFolderIn`/`CopyTree`) with 26 standalone TDD tests (`tests/test_oni_gamedata.c`).
- **`feat(macos): native first-run guided data-setup picker`** — when nothing validates, instead of the silent quit a Finder double-click used to produce ([Oni.c:182](OniProj/OniGameSource/Oni.c)), the engine runs a new Cocoa picker [Oni_DataSetup_macOS.mm](OniProj/OniGameSource/Platform_SDL/Oni_DataSetup_macOS.mm): inform → `NSOpenPanel` → validate (descending into a child `GameDataFolder`/`gamedata` if the parent was picked) → copy into `~/Library/Application Support/OniARM64/GameDataFolder` behind an indeterminate progress window (copy on a background queue; main thread pumps the run loop) → Retry/Choose-again/Quit on error. The hook ([Oni.c](OniProj/OniGameSource/Oni.c), `#if defined(__APPLE__) && UUmSDL`) re-resolves on success or `exit(0)`s on quit. Follows the `Oni_Cinematic_macOS.mm` C-shim pattern.
- **Hardening from a 3-lens adversarial review before commit**: `CopyTree` only removes a destination *it* created on failure (never a pre-existing install); copy recursion is depth-capped (no stack overflow on the ~512 KB GCD worker); `close()` errors are caught (no silently-truncated copy near a full disk); `[NSApp finishLaunching]` is called so the out-of-process `NSOpenPanel` works this early (the picker runs *before* `SDL_Init`). Verified working end-to-end on a clean first-run launch — no-data detected → picker appeared (pre-SDL) → 606 MB copied to App Support with `level0_Final.dat` directly inside (no double-nesting) → re-resolve succeeded → engine continued normally. Fixes [#38](https://github.com/andiyar/OniARM64/issues/38).
- **`release: 1.3.0r2`** ([Info.plist](macos/Info.plist)) — refreshed public DMG carrying the first-run picker. Topline change from 1.3.0r1: the game now prompts you to locate your `GameDataFolder` on first launch and copies it into place, instead of requiring a manual drop into `~/Library/Application Support/OniARM64/`. Built via `make oni_app_release` (Developer-ID signed + notarized + stapled at both `.app` and DMG layers), published to [Releases](https://github.com/andiyar/OniARM64/releases) as a prerelease.
- **`feat(render): real display-mode enumeration for the resolution menu`** — the Options → Resolution list was a hardcoded six-entry table in `gl_enumerate_valid_display_modes` ([gl_sdl.c](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/OpenGL/gl_sdl.c)); SDL's real enumeration sat commented-out under a `//TODO`, so the menu never reflected the actual monitor (iritscen flagged the missing resolutions). It now enumerates the display's real modes (`SDL_GetNumDisplayModes`/`GetDisplayMode` + a desktop-mode seed so native is always offered) through a new pure, unit-tested curator [gl_resolution_curate.c](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/OpenGL/gl_resolution_curate.c) (dedupe by W×H, drop <640×480, sort ascending, keep-largest, 32-bit only), falling back to the fixed list only if SDL reports nothing; `M3cMaxDisplayModes` 16→32; labels simplified to `W × H`. Built subagent-driven from a brainstorm→spec→plan (`../docs/superpowers/specs|plans/2026-06-03-resolution-enumeration*`), 13 standalone curator tests + an adversarial review pass. The apply path / `GLgGameWidth` / mouse mapping / FOV are deliberately untouched — it only changes *what populates the list*. **Port-completion, not a Bungie bug**: original Oni enumerated modes from the OS driver (Win32 `IDirectDraw_EnumDisplayModes`); the community OpenGL/SDL engine stubbed it. User-verified on a 5K panel: the dropdown now lists 960×540 → 5120×2880 instead of capping at 1920×1080. Fixes [#39](https://github.com/andiyar/OniARM64/issues/39).
- **`release: 1.3.0r3`** ([Info.plist](macos/Info.plist)) — refreshed public DMG carrying the real-display-mode resolution menu (above). Topline change from 1.3.0r2: the Options → Resolution list now reflects your actual monitor (real SDL modes, up to 5K) instead of a fixed table that capped at 1920×1080. Built via `make oni_app_release` (Developer-ID signed + notarized + stapled at both `.app` and DMG layers), published to [Releases](https://github.com/andiyar/OniARM64/releases) as a prerelease.

### 2026-06-02 — Session 44: Widescreen cutscene framing — 4:3-authored shots hold their frame (fixes #36)

- **`fix(cutscene): clamp in-engine cutscene cameras to 4:3 horizontal framing on widescreen`** — cutscene cameras inherit the window aspect (`ONcMotoko_AspectRatio` = `M3rDraw_GetWidth()/GetHeight()`), so on a 16:9 display the horizontal FOV widened 57.8°→72.7°, opening the left/right view-frustum planes ([`Motoko_Geom_Camera.c:166`](BungieFrameWork/BFW_Source/BFW_Motoko/Manager/Motoko_Geom_Camera.c) `fovx02 = atan(aspect·tan(fovy/2))`) that feed **both** the visibility cull and the GL projection. That pulled a near wall the 4:3-authored level-2 intro shot framed *out* into view — ~50u from camera, dominating the frame. Fix: in the per-frame render setup ([Oni_GameState.c](OniProj/OniGameSource/Oni_GameState.c), just before `AKrEnvironment_StartFrame`), when `local.in_cutscene` and the window is wider than 4:3, lower the camera's vertical FOV so the *horizontal* FOV equals 4:3's — `fovy' = 2·atan((4:3 ÷ aspect)·tan(fovy/2))`, new constant `ONcMotoko_CutsceneAspect` ([Oni_Motoko.h](OniProj/OniGameSource/Oni_Motoko.h)) — filling the wide screen by trimming vertical instead of widening horizontal (no pillarbox). Sourced from the authored global FOV so it never telescopes; outside cutscenes it re-asserts the authored FOV, auto-restoring on cutscene exit. General to every in-engine cutscene, not an L2 patch. **Port-exposed**, not a Bungie bug: the aspect math is correct for 4:3; the modern-Mac widescreen default exposes it. The long elimination chain that preceded the fix — camera position/orientation proven == authored `Camout01`, `env_show` flag mechanism + remap byte-identical to OniSplit ground truth, culling code byte-identical to Bungie 2000-12-21 — is in [#36](https://github.com/andiyar/OniARM64/issues/36); the 4:3 A/B test isolated aspect as the sole causal variable. User-verified at 1920×1080: wall gone, establishing shot framed cleanly, nothing clipped. Fixes [#36](https://github.com/andiyar/OniARM64/issues/36).
- **`release: 1.3.0r1`** ([Info.plist](macos/Info.plist)) — consolidated the private prerelease line onto a single clean **1.3.0** release and dropped the stray **1.3.1** tags (the repo is private and none had been announced, so the older DMGs were deleted rather than renumbered). 1.3.0r1 supersedes every earlier private build: it carries the Apple Silicon port (levels 1–4), Mac + PC `GameDataFolder` drop-and-play, native AVFoundation intro/outro cinematics, and the widescreen cutscene-framing fix above. Kept **1.3.0a1** as the historical first-alpha marker; future releases increment the R number (1.3.0r2, …). Built via `make oni_app_release` (the #36 investigation diagnostics stashed during the build so the public binary ships clean), signed + notarized + stapled, published to [Releases](https://github.com/andiyar/OniARM64/releases) as a prerelease.

### 2026-06-01 — Session 43: Mac retail SNDD loads + plays natively (addresses #37)

First half of Mac retail `GameDataFolder` drop-and-play — the original 2001 Mac disc data now loads and its sound plays on the ARM64 build, no PC data required. Two paired fixes (the crash fix exposed the next layer — the classic cascade):

- **`feat(mac-data): Mac SNDD layout — engine-native IMA4 load`** — Mac `.dat` files carry a distinct template-checksum family (`0x0003bcdf23c13061` vs PC `…33dc271f`) and lay SNDD out as a 16-byte record (Flags/Duration/DataSize/RawOffset), where the single PC layout the 32→64 bridge compiles is 72 bytes (`num_bytes`@56 / `data`@64). Walking Mac SNDD through the PC layout read garbage `num_bytes` → `SSiIMA_DecompressSoundData_Stereo` ran ~56 MB out of bounds → SIGSEGV on the first stereo sound (`mus_amasian1_1`, level 1). New per-file detector ([`TMtInstanceFile.isMac`](BungieFrameWork/BFW_Source/BFW_TemplateManager/BFW_TM_Game.c), set from the header checksum at load) + new [`BFW_TM_MacLayout.c`](BungieFrameWork/BFW_Source/BFW_TemplateManager/BFW_TM_MacLayout.c) that remaps the 16 Mac bytes into the shared `SStSoundData` (derives `nChannels` from Flags, forces `wFormatTag`≠2 to route to the Apple-IMA4 decoder not ffmpeg MS-ADPCM, copies the 8-byte container preamble so `TMrInstance_GetRawOffset` resolves the owning file). One branch at the bridge translate site selects it when `isMac`; PC files have `isMac==UUcFalse` and short-circuit byte-for-byte unchanged. Resolves §5 of the spec as a hand-written translate fn (not the swap-code-array approach — Mac→shared-struct is a remap with derived fields, which swap codes can't express).
- **`fix(audio): Mac IMA4 state words are big-endian — gated swap`** — with the crash gone the IMA4 decoder ran but produced uniform static across every Mac sound. Root cause: commit `5ca88b2` removed `UUmSwapBig_2Byte` from the IMA state-word reads (correct for PC little-endian `.raw`; its own message predicted *"will matter for any IMA-format sounds"*). Mac IMA4 stores each packet's predictor/step-index big-endian. Re-added the swap at the three decode sites in [`BFW_SS2_IMA.c`](BungieFrameWork/BFW_Source/BFW_SoundSystem2/BFW_SS2_IMA.c), gated on a new per-sound `SScSoundDataFlag_MacIMA4` set by the Mac translate — so PC decode is byte-identical (zero regression by construction).

User-verified: Mac level 1 loads from a save, menu music + in-level gunfire clean, no SIGSEGV; PC/CX data re-checked, unchanged. Stage 1 of the spec done (SNDD). A follow-up playtest (cloak/Daodan/explosions/combat/textures) + clean log scan then showed OSBD/BINA/TXMP already work through the shared layout — Stages 2–3 **de-scoped** (only SNDD needed a Mac-specific runtime path; they resolve bulk data via the already-wired `.sep` separate-file path). Cut release **1.3.1r1** (Mac + PC drop-and-play); [#37](https://github.com/andiyar/OniARM64/issues/37) closed.

### 2026-05-31 — Session 42: Native AVFoundation intro/outro cinematics (fixes #31)

- **`feat(cinematics): native AVFoundation playback for intro/outro .mov`** — the Bink FMV path (`BFW_Bink.c`) has been a dead no-op on ARM64 (`ONI_BINK_VIDEO=OFF` compiles every function body out, so `BKrMovie_Play` is a bare `return`), meaning the intro/outro never played. New [`Oni_Cinematic_macOS.mm`](OniProj/OniGameSource/Platform_SDL/Oni_Cinematic_macOS.mm) + C shim `ONrCinematic_PlayNative` plays the Feral-converted `intro.mov`/`outro.mov` (H.264 + LPCM, 512×384, already bundled in `Contents/Resources/`) via `AVPlayer`/`AVPlayerLayer` in a self-owned borderless window — hardware decode + audio for free. `ONrMovie_Play` ([Oni_Bink.c](OniProj/OniGameSource/Oni_Bink.c)) routes through it on Apple+SDL (`"intro.bik"` → `intro.mov`, extension stripped); the dead Bink body is preserved behind `#else` for Windows, and a missing asset is a graceful no-op so startup never blocks. The movie window is anchored to the Oni game window's monitor via `SDL_GetWindowWMInfo` (true fullscreen on the correct display for multi-monitor setups), letterboxed (`ResizeAspect`, no stretch), and skippable (Esc / any key / click). CMake gains an `if(APPLE)` block under the SDL branch: `enable_language(OBJCXX)`, links AVFoundation/CoreMedia/CoreVideo/QuartzCore/Cocoa, per-source `-fobjc-arc` + `SKIP_PRECOMPILE_HEADERS`. Bypasses RAD's Bink SDK entirely. User-verified (intro plays with audio on the correct monitor, Esc skips into the menu). Fixes [#31](https://github.com/andiyar/OniARM64/issues/31).

### 2026-05-31 — Session 41: Pre-announce polish — README screenshots + macOS-15 claim alignment

- **`docs(readme): add gameplay screenshots, drop placeholders`** — wired four user-captured screenshots (third-person corridor gameplay, Syndicate combat, main menu, level-select / save-load) into the README Screenshots section as a 2×2 grid; downscaled 5120×2880 → 1600px wide (2.5 MB total for all four) and removed the three placeholder stubs. README-only by request — not added to the GitHub release asset or the (forthcoming) r/Oni announcement post.
- **`fix(release): align macOS minimum claim to 15 (Sequoia)`** — the shipped 1.3.0r1 binary + bundled SDL2 are stamped `LC_BUILD_VERSION minos 15.0` (CMake defaulted the deployment target to the build host's Sequoia 15.7.7), while the README badge, `macos/Info.plist` `LSMinimumSystemVersion`, and the published release notes all claimed macOS 13+ — an untested claim the binary contradicts. Corrected the README badge (13+ → 15+ Sequoia), `Info.plist` (13.0 → 15.0, forward-looking — the already-shipped DMG still embeds 13.0), and the published release notes' "You need" line. Lowering the target to actually support 13/14 is tracked for later. Addresses [#35](https://github.com/andiyar/OniARM64/issues/35).

### 2026-05-31 — Session 40: Release 1.3.0r1 — refreshed public DMG (levels 1–4)

- **`release: bump version to 1.3.0r1`** ([Info.plist](macos/Info.plist)). Cut the second public artifact, the first that plays past the opening levels. The published `1.3.0a1` DMG (2026-05-24) predated the level 2–4 gameplay-path crash fixes — it hard-crashed on the level-2 Deadly Brain screamer. `1.3.0r1` bundles all 10 commits since: the material-bounds fix (#32), TRAM `soundName` indirection (#33), `M3rDraw_BigBitmap` NULL guard (#28), New Game dialog truncation (#34), and the widescreen-portrait / splash-centering render fixes. Built clean via `make oni_app_release` (session-40 cinematic diagnostics stashed during the build so the public binary ships without instrumentation). Signed + notarized + stapled, published to [Releases](https://github.com/andiyar/OniARM64/releases) as a prerelease.

### 2026-05-30 — Session 40: Levels 3 & 4 playable end-to-end (playtest milestone)

- **Gameplay progress**: user drove level 3 start-to-finish (session 39's TRAM `soundName` indirection-table fix for the acid/explosion death animation holds) and completed level 4 end-to-end. **Levels 1–4 now playable**; the Phase 6 "All 14 levels playable" march continues to level 5 next.
- **In flight, not landed**: #3 level-2 opening-cinematic investigation. Live `[CINE-CAM]`/`[CM-INTERP]`/`[ENV-SHOW]`/`[DISP-CAM]` diagnostics are in the tree (uncommitted). Current evidence reframes the bug away from "camera-position / TRAM" toward the `env_show` wall-hide path: a clean `autostart=2` trace shows the camera obeys the script (`cm_interpolate Camout01` → a valid in-level coordinate) and `env_show 561 0` flags 58 wall quads, matching the intended sequence in the original `manplant_cutscene.bsl` (community-svn). Suspect is now `env_show` → `AKcGQ_Flag_BrokenGlass` → GQ-visibility in `BFW_Akira_Render.c`, i.e. the near wall not actually being hidden. See [#3](https://github.com/andiyar/OniARM64/issues/3).
- **No code changes** — docs only (diagnostics remain uncommitted).

### 2026-05-28 — Session 39: Two gameplay crashes fixed — levels 2 and 3 now playable (fixes #32, addresses #33)

- **`fix(64bit): uintptr_t cast in New Game dialog level-list message`** — `ONiLevelList_GetLevelNumber` in `Oni_Dialogs.c:170` passed `(UUtUns32)(&level_name)` to `VMrView_SendMessage` (which accepts `uintptr_t`), truncating the stack address on 64-bit ARM64. Found via codebase-wide audit; not yet triggered in gameplay because level selection was done via cheats. Addresses [#34](https://github.com/andiyar/OniARM64/issues/34).
- **Codebase audit**: thorough sweep of all `(UUtUns32)` pointer-truncation patterns across the tree. Confirmed remaining sites are all in dead code: TOOL_VERSION editor (`Oni_Windows.c`, `Oni_Win_Tools.c`, `Oni_Win_Particle.c`), disabled features (`ONmUnitViewer`), PowerPC-only (`MS_Geom_Transform_AltiVec.c`), unused subsystems (networking, Bink). The `sizeof(UUtUns32)` stride pattern (session 34's `M3rDraw_State_Push` bug) has no remaining instances. Active gameplay code paths are clean after sessions 23–39's fixes.
- **`fix(materials): || vs && bounds-check bug in material/character validation`** — Bungie's original source used `||` instead of `&&` in three bounds-validation sites for unsigned values (particle impact tex_material, weapon impact tex_material, damage-owner character index). On 32-bit the always-true check was benign; on 64-bit the out-of-bounds `MAgMaterialArray` read returns NULL → SIGSEGV at `MArMaterialType_GetParent+32`. Triggered by the screamer particle weapon on level 2 (Deadly Brain fight). Also added runtime bounds checks in `MArMaterialType_GetParent`, `MArMaterialType_GetDepth`, and `MArImpactType_GetParent` as defense-in-depth. Fixes [#32](https://github.com/andiyar/OniARM64/issues/32).
- **`fix(sound): indirection table for TRtAnimation.soundName pointer truncation`** — `TRtAnimation.soundName` is `UUtUns32` in the TRAM template struct but holds a `char*` at runtime. On 64-bit the cast truncates the pointer → SIGSEGV in `AUrString_GetHashValue` when looking up impulse sounds during character death animation. Can't change the struct type without breaking the template bridge layout, so the `UUtUns32` slot now stores a 1-based index into a static `const char*` table. Triggered on level 3 (explosion + acid death). Addresses [#33](https://github.com/andiyar/OniARM64/issues/33) pending user verification.
- **Gameplay progress**: user played through the rest of level 2 (including Deadly Brain phase 3 with heavy particle effects) and most of level 3, confirming particle screamers, lasers, and environmental effects all work. Levels 1–3 now playable end-to-end.

### 2026-05-27 — Session 38: Widescreen portrait aspect-ratio fix (addresses #25)

- **`fix(render): uniform scaling for cinematic portraits at widescreen aspect`** — `OCiCinematic_Draw` ([Oni_Cinematics.c:260-314](OniProj/OniGameSource/Oni_Cinematics.c#L260)) scaled portrait sprites with independent X/Y factors from a 1024×768 virtual screen to actual resolution. At 16:9 (1920×1080), `width_scale = 1.875` vs `height_scale = 1.406` — portraits stretched ~33% wider than intended. Fix: sprite **dimensions** now scale uniformly using `height_scale` (preserving aspect ratio), while **position** still uses `width_scale` for X so fly-in animations reach correct screen edges. This matches the fix Feral shipped in their Intel 1.2 release notes. Addresses [#25](https://github.com/andiyar/OniARM64/issues/25) — pending user visual verification.

### 2026-05-26 — Session 37: HD bake pipeline — export-based index catches unnamed textures (#16)

- **`fix(pipeline): build instance index from OniSplit -export, not -list`** — the previous `build-hd-gamedata.sh` built its per-level instance index via `OniSplit -list`, which only returns named/public instances. Character body textures (`k4_head`, `KS_face` in some levels, `KO_` cop textures) are unnamed in several level .dat files — they exist in the data but have no name-table entry. The HD character mod 23951 (Konoko+Cops) targets these textures; the old index silently missed them, so mod overlays were never staged and Konoko rendered with vanilla textures (white-hair regression was likely a vanilla/mod texture mismatch artifact during partial overlay). Now: step 1 runs `OniSplit -export` per level upfront (cached — skips if export dir is newer than .dat), then builds the index by scanning exported filenames (`ls | sed` to decode `%2F` and strip `.oni`). This catches all instances OniSplit knows about, named and unnamed. The per-level bake loop reuses the cached export instead of re-exporting. Verified: `k4_head` now appears in the index for levels 0, 8, 11, 12, 13, 14, 18, 19 (was missing from 0, 8, 18 under `-list`). Addresses [#16](https://github.com/andiyar/OniARM64/issues/16) — pending user re-test of the full 15-mod bake with the new pipeline.

### 2026-05-26 — Session 37: Splash screen centering fix for oversized TXMBs

- **`fix(render): signed arithmetic for splash screen centering`** — `splash_screen_internal` ([Oni_GameState.c:602-603](OniProj/OniGameSource/Oni_GameState.c#L602)) computed centering offsets via `UUtUns16 x_location = (M3rDraw_GetWidth() - splash_screen->width) / 2`. When an HD texture mod produces a TXMB wider than the screen, the unsigned subtraction wraps to a large positive value (~64K), pushing the splash entirely offscreen. Changed to signed `int` arithmetic before converting to `float` for `point_screen.x/y`. Negative results (image wider than screen) now correctly crop the image rather than wrapping. The desktop-paint path (`WMiDesktop_Paint`) already uses signed types and was not affected.

### 2026-05-26 — Session 37: NULL-texture guard in BigBitmap renderer (addresses #28)

- **`fix(motoko): NULL guard in M3rDraw_BigBitmap and M3rDraw_Bitmap`** — `M3rDraw_BigBitmap` ([Motoko_Utility.c:654-656](BungieFrameWork/BFW_Source/BFW_Motoko/Manager/Motoko_Utility.c#L654)) walked `textures[index]` with no bounds check on `index < num_textures` and no NULL check on the texture pointer. When HD texture mods produce TXMB instances with sub-texture references that don't resolve (OniSplit repack can create TXMBs pointing at TXMPs not present in the .dat), the template system silently writes NULL into the pointer slot (`iBridgePrepare_ResolveTemplatePtr` returns success with resolved=NULL). `M3rDraw_Bitmap` then dereferences NULL at offset 0x84 (`width` field). Added: (1) bounds check `index >= num_textures → continue` in `M3rDraw_BigBitmap` (prevents out-of-bounds read when `num_x * num_y > num_textures`), (2) NULL check `texture == NULL → continue` (skips missing tiles), (3) belt-and-suspenders `inBitmap == NULL → return` at the top of `M3rDraw_Bitmap` since it's called from multiple sites. Bungie's original code is byte-identical here — the gap predates the port but only fires with modded data. Addresses [#28](https://github.com/andiyar/OniARM64/issues/28) pending user verification.

### 2026-05-24 — Session 36: Minimal ffmpeg build strips GPL deps from bundle (fixes #19)

- **13 dylibs → 3 dylibs.** Bundle's `Contents/Frameworks/` previously shipped SDL2 + 12 ffmpeg-related dylibs (libavcodec, libavutil, libswresample, libvpx, libdav1d, libmp3lame, libopus, libSvtAv1Enc, libssl, libcrypto, **libx264 (GPL-2+)**, **libx265 (GPL-2+)**) — all transitive deps of Homebrew's `--enable-gpl` ffmpeg bottle. The redistributed binary was effectively GPL-subject because of x264/x265 even though Oni never calls them at runtime. After this commit: SDL2 + a custom `libavcodec.62.dylib` (356 KB) + `libavutil.60.dylib` (720 KB), both LGPL-2.1, both linked only to Apple system frameworks. GPL exposure gone.
- **Approach:** Option C from #19 — rebuild ffmpeg ourselves with `--disable-gpl --disable-nonfree --disable-everything --enable-decoder=adpcm_ms` and a long list of explicit `--disable-*` flags (avdevice, avfilter, avformat, swresample, swscale, videotoolbox, audiotoolbox, coreimage, xlib, libxcb, sdl2, securetransport, network, protocols, devices, filters, muxers, demuxers, parsers, bsfs, encoders, iconv, zlib, lzma, bzlib). The only enabled component is the ADPCM_MS audio decoder — the one thing Oni actually uses (`BFW_SS2_Platform_OpenAL.c:307-411`). Pinned to ffmpeg 8.1.1, matching the Homebrew bottle ABI Oni was tested with — same libavcodec.62 SONAME, same decoder code, zero risk to the audio path that's been historically fragile (sessions 23 ap_wiz, 27-29 gunfire-lag, 29 OpenAL buffer cache, 30 Daodan ap_wiz backstop).
- **Why not the more aggressive Option A/B** (replace ADPCM decode with AudioToolbox / hand-rolled): audio path's track record bites. Touching the working decoder for marginal cleanliness gain wasn't worth the regression risk. Option C is the conservative "drop the GPL libs without touching audio code" path.
- **Files:**
  - `scripts/build-ffmpeg.sh` — new. Downloads pinned ffmpeg 8.1.1 source, configures minimal, builds, installs to `extern/ffmpeg/`. Idempotent. Cached tarball at `extern/build/ffmpeg-8.1.1.tar.xz` on subsequent runs.
  - `OniProj/OniCMakeProjs/OniProj/CMakeLists.txt` — prepends `extern/ffmpeg/lib/pkgconfig` to `PKG_CONFIG_PATH` if it exists; falls back to Homebrew otherwise (preserves the dev workflow where `extern/ffmpeg/` doesn't exist yet).
  - `macos/build-bundle.sh` — BFS-walk awk pattern now matches both `/opt/homebrew/` AND `$SOURCE_DIR/extern/`. The walk picks up our custom dylibs cleanly.
  - `.gitignore` — `/extern/` (the build output, not committed).
  - `README.md` — Bundled third-party software section trimmed from 10-row GPL-flagged table to 2 rows (SDL2, FFmpeg minimal). GPL warning gone.
  - `THIRD_PARTY_LICENSES.md` — dropped sections for libmp3lame, libopus, libvpx, libdav1d, libSvtAv1Enc, OpenSSL, libx264, libx265. Replaced GPL note with a "No GPL components" note pointing at the build script.
- **Verification:** clean cmake-from-scratch + make + make oni_app → bundle ships with exactly `libSDL2-2.0.0.dylib` + `libavcodec.62.dylib` + `libavutil.60.dylib`. Total dylib payload 2.5 MB (was ~50 MB+). Smoke-test launch: PID alive 4s, no SIGSEGV. User ear-test on tutorial level: music + gunfire + combat impacts + voice + footsteps all audible and correct via the new minimal libavcodec. Audio cache stats from prior runs corroborate: ADPCM decode produces non-zero hit rates, which requires successful decodes. `fixes #19`.
- **Bundle weight win is real but not headline.** The 95 MB bundle total is now dominated by the intro/outro `.mov` cinematics (89 MB combined). The dylib win is ~50 MB → 2.5 MB. Removing the `.mov` AVFoundation gap (separate issue, not filed yet — would require wiring intro/outro through AVFoundation since they bypass BFW_Bink.c entirely) would cut another 89 MB and leave a ~6 MB total bundle.

### 2026-05-24 — Session 35: Developer ID signing + notarization pipeline (#13)

- **`make oni_app_release` lands end-to-end.** Produces `build/OniARM64.dmg` (98 MB) — signed with the Developer ID Application cert (`Benjamin Thomas / 5W72UJL332`, minted this session in Xcode), notarized + stapled at both the `.app` and `.dmg` layers, drag-to-Applications DMG layout via `create-dmg`. `spctl --assess` returns `source=Notarized Developer ID` for both artifacts — the canonical "ships to strangers" signal. Closes Phase 7's `.app bundle + code signing` milestone.
- **Pipeline shape:** new `oni_app_release` CMake target chains `build-bundle.sh` (Developer ID mode) → `notarize-bundle.sh` (`.app` zip + notarize + staple) → `package-dmg.sh` (DMG create + sign + notarize + staple). Two Apple notary round-trips per release. End-to-end wall clock ~6 min for this run (Apple SLA is 30 min, real-world is usually 1-5). `make oni_app` dev target unchanged — ad-hoc signing, sub-second, identical behaviour to before.
- **`build-bundle.sh` refactor (`33469cf`, `22411ec`):** third positional arg `SIGN_IDENTITY` defaults to `-` (ad-hoc, dev-loop behaviour preserved exactly). When set to a Developer ID identity, adds `--options runtime --timestamp` and applies `macos/entitlements.plist` (single entry: `com.apple.security.cs.disable-library-validation` — defensive cover for Homebrew dylib quirks under hardened runtime). Signing order corrected to inside-out (dylibs → main binary → bundle); previous order worked for ad-hoc but would invalidate hardened-runtime sigs. New `codesign --verify --strict` at end of assembly catches sign-order or missing-dylib bugs before they reach Apple's notary service.
- **Two new pipeline scripts (`23a2d8e`, `7fd98dd`):** `notarize-bundle.sh` ditto-zips → submits via `notarytool --wait` → staples ticket → runs `spctl --assess` on the `.app`. `package-dmg.sh` wraps the stapled `.app` in a DMG via `create-dmg` (Homebrew formula by Andrey Tarantsov), signs the DMG with Developer ID + timestamp, then runs its own notarize/staple/spctl round-trip. Preflight in `package-dmg.sh` requires the `.app` to already be stapled and `create-dmg` to be on `$PATH` — fails fast with actionable error messages if either is missing.
- **CMake wiring (`5eb76eb`):** `ONI_SIGN_IDENTITY` cache variable (set via `cmake .. -DONI_SIGN_IDENTITY="..."`); empty value makes the `oni_app_release` target print a clear setup error and exit non-zero instead of silently falling back to ad-hoc (which would notarize-fail confusingly).
- **Licensing compliance (`35bd851`, `a82952b`):** new `THIRD_PARTY_LICENSES.md` at repo root carries full license texts for the 10 bundled libraries (SDL2, FFmpeg trio, LAME, Opus, libvpx, dav1d, SVT-AV1, OpenSSL, x264, x265). Versions captured from current Homebrew installs. LGPL components note the dynamic-linking provision via `@executable_path/../Frameworks/` so users can swap libraries without relinking. GPL components (x264, x265) carry the corresponding-source pointer required by GPL section 3 and reference #19 as the cleanup path. `build-bundle.sh` copies the file into `Contents/Resources/THIRD_PARTY_LICENSES.txt` so the redistributed bundle is self-contained for compliance.
- **README distribution section (`1952786`):** new `## Building a release` top-level section between Build details and Contributing, wrapped in `<details>` to keep the landing page lean. Covers one-time setup (`brew install create-dmg`, `notarytool store-credentials`, CMake identity cache var), per-release invocation, notarytool recovery, `create-dmg` flake note.
- **No `.c` / `.h` changes.** All edits in `macos/` (3 new scripts + 1 plist + bundle.cmake) and repo-root markdown. Existing dev workflow (`make oni_app` + binary-swap inner loop) completely unchanged. Bare-binary fallback also preserved.
- **Commits this session** (oldest → newest): `c9b721e` (entitlements.plist) → `33469cf` (SIGN_IDENTITY arg + inside-out order) → `22411ec` (final verify) → `23a2d8e` (notarize-bundle.sh) → `7fd98dd` (package-dmg.sh) → `5eb76eb` (CMake target) → `35bd851` (THIRD_PARTY_LICENSES.md) → `a82952b` (license copy wiring) → `1952786` (README distribution section). 9 commits.
- **Issue lifecycle:** #13 verified end-to-end (`spctl --assess` returned `source=Notarized Developer ID` on both `.app` and `.dmg`; DMG mounted cleanly with drag-to-Applications layout; stapled `.app` inside the DMG validates). #19 (ffmpeg/x264/x265 GPL cleanup) remains open and referenced from `THIRD_PARTY_LICENSES.md`.
- **Install-flow validation (`1d6c51c` → `7ec5505`):** symlinked `GameDataFolder` to `~/Library/Application Support/OniARM64/gamedata/` (the README's documented path); `cp -R` the notarized `.app` from `build/bin/` to `/Applications/` (sig intact, `spctl --assess` still passes after the copy); `open /Applications/OniARM64.app` launched via Finder with zero Gatekeeper friction. `startup.txt` confirmed `[BundlePath] game data folder resolved to /Users/andiyar/Library/Application Support/OniARM64/gamedata` — Candidate 1 hit on first try. Full launch sequence: window init → splash → template load from `level0_Final.dat` → main menu UI → `[SS2] SSrAmbient_Start name='main_menu_win'` (menu music played) → user-driven clean quit → `OpenGL disposed properly`. State files (`persist.dat`, `key_config.txt`) auto-resolved to App Support since cwd-relative paths fail when launched from `/Applications`. Phase 7 milestone `.app bundle + code signing` ticked. **`fixes #13`** landed in `7ec5505`. One caveat for future testers: the truly cold-start case (no `~/Library/Application Support/OniARM64/` dir at all) wasn't experimentally exercised — the dir pre-existed from session-28 work, though the resolver's `mkdir(dir, 0755)` at `ONi_BundlePath.c:218` should handle it.
- **README download instructions updated (`832dd19`):** swapped `OniARM64.app.zip` → `OniARM64.dmg` to match the new release artifact; dropped the "right-click → Open to bypass Gatekeeper (ad-hoc signed; Developer-ID builds coming)" workaround since builds now ship Developer-ID signed + notarized; reordered so gamedata setup happens before launch (less of a footgun than launch → fail → read about data setup). The link still points at an empty Releases page until the first DMG is uploaded — that's a separate cutting-a-release task.
- **Spec + plan in parent repo:** `docs/superpowers/specs/2026-05-24-developer-id-signing-design.md` (`f119d6d` → `bcbccfa`), `docs/superpowers/plans/2026-05-24-developer-id-signing.md` (`3d76067`).

### 2026-05-24 — Session 34: Level-3 (bio lab) cinematic crash — draw-state push truncated 8-byte pointers to 4 (#29)

- **Symptom:** consistent SIGSEGV in `gl_quad` (NULL deref at `gl_geometry_draw_method.c:222`) during the bio-lab opening cinematic, the moment Barabas fires his energy projectile (`benrgy_fly`). User crashed ~10 times today across multiple `Oni-*.ips` reports. Stack: `gl_quad ← M3iSort_Draw_Quad ← M3rSort_Frame_End ← M3rDraw_Commit_Alpha ← ONiGameState_Display_Overlay_Elements`. Last log line before crash: `[SS2] SSrAmbient_Start name='benrgy_fly' usable=1 pos=0x16b696cb4`.
- **Root cause via disasm of the crashing instruction** (`gl_quad + 0x7ac`): `ldp s0, s1, [x8]` where `x8 = state_ptr[3] + indices[0]*8 = NULL + 0`. `state_ptr[3]` is `M3cDrawStatePtrType_EnvTextureCoordArray`. The crash path is `gl_quad`'s env-map branch, which we reached because `state_ptr[6] = EnvTextureMap` read non-NULL, which it shouldn't have.
- **The bug ([Motoko_State_Draw.c:270](BungieFrameWork/BFW_Source/BFW_Motoko/Manager/Motoko_State_Draw.c#L270)):** `M3rDraw_State_Push` copied the pointer stack with stride `sizeof(UUtUns32) * NumTypes` = 32 bytes. On 64-bit, the pointer stack is `void**` — 8 pointers × 8 bytes = 64 bytes. Only the first 4 pointers (entries 0-3: `ScreenPointArray`, `ScreenShadeArray_DC`, `TextureCoordArray`, `EnvTextureCoordArray`) actually got copied; entries 4-7 (`BaseTextureMap`, `LightTextureMap`, **`EnvTextureMap`**, `VertexBitVector`) retained whatever stale values were left in the new stack slot from a previous unrelated push. So during the alpha-quad pass, `state_ptr[6]` held a stale-but-still-valid env-map pointer from an earlier env-mapped scope, `gl_private_state_update` ([gl_engine.c:441](BungieFrameWork/BFW_Source/BFW_Motoko/Engines/DrawEngine/OpenGL/gl_engine.c#L441)) saw it as non-NULL and switched `geom_draw_mode` to env-map, and `gl_quad` then read `state_ptr[3]` — which the alpha quad never set — and dereferenced NULL.
- **Why level 3 specifically:** bio lab has shiny env-mapped lab surfaces (heavier env-map usage than levels 1/2), so by the time the alpha pass runs there's a strong chance entry 6 is a stale-but-valid pointer from prior env render. Barabas's beam particle was just the first alpha submission of that frame to read it. Issue #7 (level-2 cinematic crash — single observation, no repro) plausibly shares the root cause.
- **Fix (`fix(motoko): copy 8-byte pointers in draw-state push, not 4-byte`):** one-line change — replace `sizeof(UUtUns32)` with `sizeof(void*)` so the push copies all 64 bytes of the pointer array. After the fix, entry 6 is correctly copied as NULL from the outer scope, the env-map branch isn't taken, and the alpha quad renders via `_geom_draw_mode_default` using only entries 0/1/2 (which `M3iSort_Draw_Quad` does set). Addresses [#29](https://github.com/andiyar/OniARM64/issues/29) pending user verification of the bio-lab repro.
- **Framing**: port-exposed 32→64. Bungie source is byte-identical here (`diff -w` empty); the 2000-12-21 code was correct for its 32-bit target where `sizeof(void*) == sizeof(UUtUns32) == 4`. Same family as issue #11 (`AUrQSort_32` treating pointer arrays as `UUtUns32`).

### 2026-05-24 — Session 33: Daodan sound leak FIXED — SSiSoundChannel_SetLooping never reached OpenAL (#2)

- **The 4-session-old `ap_wiz` framing was wrong.** Sessions 27-30 chased "ap_wiz ambient persists after Daodan ends" and shipped a backstop in `Oni_Character.c` calling `OSrAmbient_StopByName("ap_wiz")` at the super-particle Stop event. That backstop has been a no-op for the actual bug. A clean test (no combat, no gunfire, hypo + Daodan only, killed mid-bug) showed zero `ap_wiz` events in the log; the actual leaking ambient was `health_over` (Daodan health-overflow), started via `ONrGameState_ConditionSound_Start(HealthOver)` from the per-frame condition-sound update at [Oni_GameState.c:4121](OniProj/OniGameSource/Oni_GameState.c#L4121). Past sessions' "30 ap_wiz starts / 0 stops" telemetry came from in-combat sessions where NPCs firing TAP pistols spawned `w1_tap_p01` particles with `ap_wiz` ambients — completely unrelated to the Daodan leak.
- **Root cause (`fix(audio): propagate SSiSoundChannel_SetLooping to OpenAL`):** [BFW_SoundSystem2.c:2054](BungieFrameWork/BFW_Source/BFW_SoundSystem2/BFW_SoundSystem2.c#L2054) `SSiSoundChannel_SetLooping` just flips the `SScSCStatus_Looping` bit on Bungie's internal `SStSoundChannel` shadow state. **It never propagated to OpenAL.** The only `alSourcei(source, AL_LOOPING, ...)` call in the entire tree is inside `SS2rPlatform_SoundChannel_Play()` at [BFW_SS2_Platform_OpenAL.c:249](BungieFrameWork/BFW_Source/BFW_SoundSystem2/Platform_OpenAL/BFW_SS2_Platform_OpenAL.c#L249), fired at the moment the source is first started. After that, OpenAL kept looping forever regardless of how many times the shadow bit flipped. `SSrAmbient_Stop`'s graceful-stop path (which fires for any ambient without `SScAmbientFlag_InterruptOnStop`) was the only way for the `health_over` ambient to ever stop — and that path relied entirely on `SetLooping(false)` taking effect. Almost certainly worked on Bungie's original DirectSound (Win32) target where looping was state-tracked differently; on OpenAL the loop ran forever (or until level change emptied the ambient table).
- **Fix scope:** added `SS2rPlatform_SoundChannel_SetLooping()` to the platform API in [BFW_SS2_Platform.h](BungieFrameWork/BFW_Source/BFW_SoundSystem2/BFW_SS2_Platform.h), implemented in the OpenAL layer as a single `alSourcei(AL_LOOPING)` call, and wired into the shared `SSiSoundChannel_SetLooping` so every shadow-bit flip propagates immediately. User-verified: `health_over` cuts <5s after Daodan ends (the residual is the graceful tail — OpenAL plays the current buffer iteration to natural end after `AL_LOOPING=false`, which is the correct intent of "graceful stop"). Closes [#2](https://github.com/andiyar/OniARM64/issues/2).
- **Investigation discipline note**: ~4 sessions of telemetry-driven analysis converged on the wrong sound name because the only ambient we ever traced with a name filter was `ap_wiz` (from the very first observation). The session-33 clean test (controlled-conditions repro, no confounding signals) revealed the actual ambient name in one log read. Lesson: when telemetry from a "rich" session disagrees with a "clean controlled" session, the clean session wins. Also a partial-credit lesson mid-session — I constructed a "super-particle Stop chain spawns w1_tap_p01 as parting flourish" theory from coincidental log proximity, no evidence; user (correctly) called me on it and we got back to data.
- **Diagnostics retained** per `feedback_keep_diagnostics`: `[HEALTH-OVER-DBG]` state-change tracer at line 4115 of Oni_GameState.c, `[HEALTH-OVER-DBG] Start/Stop ENTRY/CALL` tracers inside `ONrGameState_ConditionSound_Start/Stop`, `[SS2-STOP-DBG]` tracer at the head of `SSrAmbient_Stop` logging entry conditions + which branch (interrupt vs graceful) was taken. All cheap (rare-fire on state transitions, not per-frame). Useful for any future ambient-leak investigation.
- **Cleanup landed**: the session-30 backstop in `ONiCharacter_SuperParticle_SendEvent` calling `OSrAmbient_StopByName("ap_wiz")` was demonstrably useless for the bug it claimed to address (wrong sound name) and a real potential regression with the SetLooping fix in place (would cut NPC-played TAP-pistol bullet-whoosh in flight). Reverted: removed the backstop call site in `Oni_Character.c`, the `OSrAmbient_StopByName` implementation in `Oni_Sound2.c`, and its declaration in `Oni_Sound2.h`. Function had no other callers.

### 2026-05-24 — Session 33: Logs persist across launches (#17 — append mode + session banner)

- **`fix(logging): startup.txt + debugger.txt persist across launches`** ([BFW_Error.c](BungieFrameWork/BFW_Source/BFW_Utility/BFW_Error.c)). `fopen("startup.txt", "w")` → `"a"` and `fopen("debugger.txt", "wb")` → `"ab"`, plus a new `iWriteSessionBanner()` helper that writes `===== session start YYYY-MM-DD HH:MM:SS =====` on each open. `grep -n "===== session start" startup.txt | tail` jumps straight to the latest run; old sessions are archived in the same file. Closes the workflow tax where users (and Claude) had to remember to SIGKILL the process before reading the log, because the *next* launch's `fopen("w")` would truncate it. Issue [#17](https://github.com/andiyar/OniARM64/issues/17), `fixes #17` pending user verification of the banner format.
- **Mechanism correction**: prior memory and CLAUDE.md framing claimed Oni "unlinks startup.txt at startup; survives SIGKILL, vanishes on graceful exit." That was wrong — no `unlink` ever existed in the tree. The actual mechanism was `fopen("w")` truncating at process startup. The SIGKILL folklore worked by accident: killing left the file intact and the user typically read it before relaunching, which is also true post-graceful-exit. Memory body had been corrected session 27 but the index entry stayed stale, so the wrong framing kept getting re-cited. Index updated this session.

### 2026-05-24 — Session 32: Laser-beam investigation (#6) — bridge hypothesis refuted, diagnostic coverage expanded

- **Phase 1 evidence: the bridge hypothesis from #6 is unlikely.** TRGE (`OBJtTriggerEmitterClass` at `Oni_Object.h:487`) is `M3tPoint3D + M3tVector3D + geometry* + u32` — no embed-struct-with-inner-pointer pattern. TRIG (`OBJtTriggerClass` at `Oni_Object.h:497`) is flat u32/u16/float/pointer/char[32]. Walker handles each field natively. No `[bridge] SIZE MISMATCH` lines for these or any template in the latest startup.txt. Also: the issue's third suspect `ONWCLaserSight` doesn't exist as a template — session-27 audit conflated it with `ONiDrawLaserSight` (function at `Oni_Character.c:6297`), which draws *character weapon* laser sights (sniper rifle), not the tutorial security tripwires.
- **Three new `[LASER-DBG]` tracers added**, all gated by the existing `ONI_LASER_TRACE` env var (no perf cost when unset). Sized to discriminate four alternative hypotheses in a single playthrough:
  - **`Reset`** in `OBJrTrigger_Reset` (one-shot per trigger per level load) — logs `emit_count, color, time_on/off, flags, laser_on`. Captures load-time data values: trigger loaded? with what color/timing/initial state?
  - **`FX-init`** in `FXrEffects_LevelBegin` (one-shot per level begin) — logs `FXgLaser_Texture, alpha, width`. If `tex=0x0`, the `laser_contrail` template lookup failed at startup and every later `FXrDrawLaser` call asserts in TOOL_VERSION or passes NULL to the renderer in release.
  - **`Draw-entry`** in `OBJiTrigger_Draw` (throttled 1/60, fires unconditionally before the Hidden gate) — logs `emit_count, flags, color, laser_on`. Closes the gap left by the existing per-emitter `gate` tracer, which only fires in the non-Hidden else-branch and so can't distinguish Hidden flag vs emitter_count==0 vs Draw not reached.
- **Coverage now**: 6 `[LASER-DBG]` tracers total (3 existing from session 27 + 3 new). One playthrough through the tutorial security-laser room with `ONI_LASER_TRACE=1` discriminates: (A) trigger Hidden, (B) emit_count==0, (C) color alpha=0, (D) texture==NULL, (E) all gates pass but contrail renderer fails. Diagnostics retained per `feedback_keep_diagnostics`.
- **Fix landed, user-verified end-to-end** (`fix(render): gate Trigger laser color byte-swap on UUmEndian_Big, not UUmPlatform_Mac`): the instrumented run produced clean evidence — `Reset` showed `trigger_long color=0xFFFF0000` (opaque red), but `FXrDrawLaser-call`/`-entry` showed `color=0x0000FFFF`, every active trigger passing a byte-reversed color to the renderer. Inside `FXrDrawLaser` the byte-reversed `(inColor >> 24) = 0x00` made `alpha = 0` → fully transparent beam. The byte-reversal at `OT_Trigger.c:540` was gated `#if defined(UUmPlatform) && (UUmPlatform == UUmPlatform_Mac)` — correct for Bungie's 2001 target (Classic Mac == PowerPC == big-endian), incorrect for modern macOS (Intel + ARM64, both little-endian). Re-gated to `#if UUmEndian == UUmEndian_Big` so the swap only fires on actually-big-endian targets. Post-fix log confirms `color=0xffff0000` reaching `FXrDrawLaser` unchanged; user screenshot shows three opaque red beams in the tutorial security-laser room, exact match to Bungie's original. Closes [#6](https://github.com/andiyar/OniARM64/issues/6) and ticks the Phase 4 `Security-laser tripwire beams render in the tutorial level` milestone.
- **Bug 2 surfaced but deferred:** the instrumented run also revealed that 660 of 886 gate samples had `active=0` — many `trigger_long` instances carry `flags=0x0020` (PingPong only), no `InitialActive` (0x0008), no `Active` (0x0004). They reach the draw call only when a level script flips Active on them post-Reset. For the tutorial-laser scene the script-activated triggers suffice (three beams visible post-fix). A broader question — "are there laser groups in other levels that should be active at level-start but aren't?" — is separate from #6; filing a new issue for tracking.
- **Framing carry-forward**: this is a port-exposed bug, not pre-existing latent. Bungie's swap was correct for their target. ARM64-on-modern-macOS just happens to share its little-endianness with Win32, so the Mac-specific swap path now misfires.

### 2026-05-24 — Session 31: Embed-struct bridge audit (issue #4) — 3 new HIGH-confidence candidates identified

- **Audit dispatched overnight at user's request.** Six parallel Opus subagents partitioned by subsystem (Akira/Path, Motoko/Render, Text/UI/Sound BFW, Totoro public, Oni game-logic, Oni AI). Two follow-up agents closed gaps: one auditing `BFW_Source/BFW_Totoro/BFW_Totoro_Private.h` (TRAM's home, out of the original Totoro agent's scope) and one reading `BFW_TM_Bridge.c` to disambiguate whether vararray/fixedarray element stride uses C `sizeof` (safe) or sum-of-swap-codes (broken). ~95 `tm_struct` + ~70 `tm_template` definitions examined; no code changed. Full inventory + per-candidate analysis posted on [issue #4](https://github.com/andiyar/OniARM64/issues/4).
- **3 candidates initially flagged HIGH-confidence; cross-check against bridge's existing `SIZE MISMATCH` log produced a correction (committed as a follow-up comment on #4):**
  - **`IGPG`** — RECLASSIFIED AS SAFE (false positive). Field after `font_info` is `tm_templateref pict` (align-8). Walker `iAlignUp(28, 8) = 32` matches C's `pict` placement. dst_size=56 matches sizeof+preamble=56. Agent 5 saw the IGSt-style embed pattern but missed that IGSt's killer is the trailing `char[]` (align-1, no walker compensation) while IGPG's next field is a pointer (align-8, walker auto-compensates).
  - **`ONCC` (`ONtCharacterClass`)** and **`TRAM` (`TRtAnimation`)** — INDETERMINATE. Total-size mismatch check at `BFW_TM_Bridge.c:539` fires 0 times in real-world logs, meaning walker dst_size matches C sizeof for both. But these two have the shape where the *internal* drift could exist — walker placing pain/extent fields at -4 offsets, then a later align-8 boundary catches both views up at the same absolute offset, so total sizes match while individual fields are mispositioned. The existing diagnostic can't catch this class.
- **Real value of audit**: ~85+ templates exhaustively verified SAFE. IGPG cleared as false positive. ONCC + TRAM remain unverified — bridge diagnostic limitation surfaced (total-size check ≠ per-field check). Suggested follow-up tooling: extend `TMrBridge_ValidateDescriptor` with per-field `offsetof()` validation, which would deterministically resolve ONCC and TRAM in one diagnostic pass.
- **Bridge vararray-stride question definitively answered** by Agent 8: `iAppendScalar` (`BFW_TM_Bridge.c:76`) already aligns RawPtr/TemplatePtr/8Byte to 8 on a per-field basis; both FixedArray (line 163) and VarArray (line 240) pad element stride to `sub_state.alignment`. So walker stride == C `sizeof(elem)` for any element whose alignment quirk is *only* from pointer fields. The AKVA-class bug only manifests when an embedded *inner tm_struct* has a leading-field alignment lower than its own natural alignment (which the walker can't infer from the swap-code stream). Disambiguates 3 MEDIUM candidates from Agent 5 (ONCP, ONOA, TURR) — all confirmed SAFE.
- **Confirmed SAFE**: ~85+ remaining `tm_struct`/`tm_template` across all 6 audited subsystems. M3t* geometric primitives all pure float/int (align ≤ 4). Pointer-bearing templates with pointers at top level (walker handles). `SStFormat` packed (align 1) protects SNDD. Oni AI subtree has no `tm_raw` pointers in any tm_struct at all.
- **No code changed.** Three candidate fixes queued for prioritization — each fits the established pattern (one `inTemplate->tag` branch in `iFixupEmbeddedStructAlignment` shifting fields at-or-after the embed's outer offset). Suggested order in the issue comment: IGPG (lowest risk, fastest verify) → TRAM (widest blast radius, may explain #3 cinematic) → ONCC (character config / pain sounds).

### 2026-05-23 — Session 30: Daodan ap_wiz leak — slot-0 hypothesis disproved, backstop landed

- **Slot-0 fingerprint hypothesis from session 28 disproved.** Extended `[DAODAN-DBG]` tracer in `Oni_Character.c:3683` to dump per-slot template shape on Stop event (classname, `eventlist[Stop]` action count, num_emitters + emittedclass names). Live repro: slot 0 = **`superglow_e01`** (a separate class, not a broken duplicate of slots 1+2), 2 stop_actions, 1 emitter → `superglow_p01`; slots 1+2 = `super_e01`, 5 stop_actions, 2 emitters → `super_p01`, `super_p03`. The 0x1 flag-bit toggle difference (slots 1+2 set Hidden on Stop, slot 0 doesn't) is a visual design choice in `super_e01`'s template, not a missing handler. All three slots show `current_sound=SScInvalidID` — the looping `ap_wiz` ambient isn't held by the top-level super-particles directly. Leak is in a descendant of the emit chain.
- **Backstop fix (`fix(audio): force-stop ap_wiz at Daodan-end transition`):** new `OSrAmbient_StopByName(const char *)` in `Oni_Sound2.c` walks `OSgPlayingAmbientHandles` (stable index — `OSrAmbient_Stop` nulls the slot, doesn't shift other entries) and force-stops every ambient whose name matches. Called from `ONiCharacter_SuperParticle_SendEvent`'s Stop branch with `"ap_wiz"`. Cuts the user-visible symptom (persistent looping until level change) without needing to identify which descendant in the emit chain actually carries the sound. Side-effect: NPCs play `ap_wiz` too (session 28: 83 paired starts/stops in no-hypo session); backstop cuts NPC-played instances in flight as well, but NPC cycles re-fire on next event and Daodan-end is a rare per-cycle transition. If audibly perceptible, narrow to position-proximity-to-Konoko in followup.
- **Identification-layer diagnostics added to `BFW_Particle3.c` for next repro:** `[DAODAN-DBG] P3_StartAmbient ap_wiz` in `P3iAmbientSound_Update` (filter by ambient name), `P3_EndAmbient` in `P3iAction_EndAmbientSound` (super* classname filter), `P3_Dispose` in `P3rDisposeParticle` (same filter). On next Daodan run we'll see which descendant class actually plays `ap_wiz` and whether the cleanup path ever reaches it — that's the data we need to convert the backstop into a root-cause fix in `super_e01`'s emit chain.
- **Pending user verification.** Fix shipped untested as the user was AFK; commit carries `addresses #2`. Close when behaviour-confirmed.

### 2026-05-23 — Session 29: Gunfire-lag root cause + OpenAL buffer cache

- **Gunfire lag root-caused via external `sample` profiling.** After five fflush-gating passes in sessions 27-28, user reported the lag still dramatic on gunfire and worse under strafe + Daodan. Three macOS `sample` captures (`/tmp/oni-sample-20260523-{113844,114424,125918}.txt`) revealed the smoking gun: `SS2rPlatform_SoundChannel_SetSoundData → alBufferData → OALBuffer::AddAudioData → usleep → __semwait_signal`. Apple's OpenAL implementation internally calls `usleep` per PCM upload, blocking the main thread ~1ms per `alBufferData` call. Under stress (cheat + Daodan + strafe-fire), **4951 main-thread samples sat in this single syscall path = 8.3% of wall clock**. Issue #9's stated mechanism (ADPCM decode + alGetError sync) was refuted by the same profile: alGetError = 0 samples, libavcodec = ~13 samples total — neither is hot. The cost is `alBufferData` itself, not the decode preceding it.
- **Fix: lazy OpenAL buffer-ID cache in `BFW_SS2_Platform_OpenAL.c`.** 4096-bucket open-addressing hash table keyed by `SStSoundData*` (stable for process lifetime — these are template-backed and live in rawBase). First play of a sound: `alGenBuffers` + `alBufferData` (eats the usleep once). Subsequent plays: just `alSourcei(source, AL_BUFFER, cached_id)` — no PCM upload, no usleep. Channel's per-channel `pd.buffer` is preserved as fallback for `alGenBuffers` failure and as the legacy path when cache is disabled. Cache freed in `SS2rPlatform_Terminate` after dumping hit/miss stats. Env-var escape hatch `ONI_AUDIO_CACHE=0` reverts to per-call upload without a rebuild.
- **Post-fix verification (clip 3, same stress scenario, 90s `-wait` sample):** **0 `alBufferData` samples** anywhere in the profile (down from 4951). `SS2rPlatform_SoundChannel_SetSoundData` dropped from thousands of leaf samples to 17 — not because it's called less, but because each call now returns in microseconds and the 1ms sampler rarely catches it in flight. `OSrImpulse_Play` correspondingly dropped from 4491 to 243. Terminate-time cache stats: `80 entries, 828 hits, 80 misses (hit ratio 91.2%)` — the 1-miss-then-N-hits-per-unique-sound amortisation pattern. User verdict on subjective listening test (solo gunfire, Daodan, strafe, weapon reloads): *"ran around, fire, daodan, shoot a bad guy reloading clips and it looks pretty good"*.
- **Mass-combat verification (clip 4, crowd-combat session, 300s `-wait` sample — `/tmp/oni-sample-20260523-183843.txt`, 180,855 main-thread samples):** numbers held under roughly 2× the sound-play volume of the solo run. `alBufferData` = **2 samples** (0.001% of wall clock, was 4951 / 8.6% pre-fix), `SetSoundData` leaf = 41 (was 17 solo, thousands pre-fix), `OSrImpulse_Play` = 29 (was 243 solo, 4491 pre-fix). Terminate-time cache stats: `294 entries, 1873 hits, 294 misses (hit ratio 86.4%)` — slightly lower than solo's 91.2% because crowd combat introduces more unique sound variants (multiple weapons, varied hit-reaction sounds), but still strong amortisation across 2167 total plays. User subjective verdict: *"seems fine."* Closes #1 (per-frame fprintf+fflush stalls, gated over sessions 27-28) and #9 (sound-playback architecture, cache landed in 7e98cfe).
- **Tooling added: `scripts/profile-gunfire.sh`** — external `sample`-based profiler with `-wait` semantics so it attaches automatically when Oni launches. Zero observer effect (no `UUrStartupMessage` instrumentation; the original fflush stalls of #1 were a self-inflicted noise floor). Reusable for any future audio/perf investigation. The bash script and CLAUDE.md `## Test Commands` cleanup (lead with `.app` workflow now that Phase α landed) live in the parent repo.
- **Process note (investigation discipline):** `superpowers:systematic-debugging` Phase 4.5 ("5 same-hypothesis attempts = wrong architecture, profile before more fixes") was the right framing. Issue #9's writeup correctly predicted the *direction* of the fix (cache so you don't pay per play) but got the *mechanism* wrong (decode vs upload). The lesson: a directional architectural hypothesis can still be useful even when the underlying mechanism is wrong — but the verification step (profile, not assume) is non-optional.

### 2026-05-22 — Session 28: Phase α — dev .app infrastructure

- **Phase α complete.** `make oni_app` produces `build/bin/OniARM64.app`, a clickable dev bundle that launches via Finder/`open` and reaches the main menu identically to bare-binary `OniNative/Oni`. 11 commits land the infrastructure end-to-end; the bare-binary workflow is unchanged — every fix is additive or wrapped in cwd-first-then-fallback semantics. User-verified end-to-end: launches, plays menu music, resolution + subtitle + mouse settings persist across launch cycles. Tracks against [#15](https://github.com/andiyar/OniARM64/issues/15); every commit carries `addresses #15`.
- **Bundle assembly (`ee9c0ec` → `67fbe95` → `25dc8b3` → `86752c8` → `2c0b6c8`):** new `macos/` tree with `Info.plist` (`com.andiyar.oniarm64`, ARM64-only, HiDPI, macOS 13+), `PkgInfo`, and `build-bundle.sh`. The script does a BFS dylib walk to fixed point — direct + transitive Homebrew deps (libSDL2 + libavcodec's transitive subtree: libswresample, libvpx, libdav1d, libmp3lame, libopus, libSvtAv1Enc, libx264, libx265, libssl, libcrypto, libavutil = 13 dylibs total) get copied into `Contents/Frameworks/` with `install_name_tool` rewriting LC_ID_DYLIB and LC_LOAD_DYLIB to `@executable_path/...`. CMake exposes this as `make oni_app`. After all path rewrites the binary and every dylib are re-signed ad-hoc — Apple Silicon dyld rejects with `SIGKILL (Code Signature Invalid)` otherwise, since `install_name_tool` invalidates the original signatures. Verified with a real crash report from a pre-codesign launch attempt.
- **Game-data resolver (`1da832f` → `368fe5b`):** new `ONi_BundlePath` module replaces the 14-line `BFrFileRef_Search` block in `Oni.c` with a layered lookup: (1) `~/Library/Application Support/OniARM64/gamedata/`, (2) `<bundle>/Contents/Resources/gamedata/` (via `_NSGetExecutablePath` + `realpath`), (3) the legacy `BFrFileRef_Search` chain (bare-binary fallback — `OniNative/GameDataFolder` symlink still found). `[BundlePath]` tracers fire 3-4 times per launch announcing which candidate hit. Followup fix: `realpath()` scratch buffers swapped from `BFcMaxPathLength` (255, Mac OS 9-era) to `1024` (macOS PATH_MAX); `realpath(3)` is UB per POSIX with a buffer smaller than PATH_MAX even though the typical exec path was short enough to dodge it in practice.
- **Log + state-file fallbacks (`abd04df` → `e7f0505`):** the .app's cwd under Finder/`open` is `/`, so cwd-relative `fopen` silently fails and settings disappear between sessions. `iOpenLogFile` in `BFW_Error.c` and `ONiBundlePath_ResolveStateFile` in `ONi_BundlePath.c` apply the same cwd-first-fallback pattern: under .app, `startup.txt` + `debugger.txt` redirect to `~/Library/Logs/OniARM64/`, and `persist.dat` + `key_config.txt` redirect to `~/Library/Application Support/OniARM64/`. Apple convention: `~/Library/Preferences/` is reserved for plist files managed by cfprefsd via NSUserDefaults/CFPreferences — Oni's `persist.dat` is a custom binary blob with its own version/swap-code header, so Application Support/ is the conventionally-correct home. Bare-binary OniNative workflow is preserved exactly — existing `OniNative/persist.dat` (206KB) and `OniNative/key_config.txt` (759B) detected and reused.
- **Workflow tooling (`75caffb` → `6d31856`):** README Build section split into Option A (bare binary, unchanged) and Option B (.app bundle, new) with a table of macOS-conventional file locations. `setup-dev-bundle.sh` one-shots rebuild + gamedata symlink + quarantine `xattr` strip — auto-runs `make oni_app` if the bundle is missing, so accidentally deleting `build/bin/OniARM64.app/` (e.g., during a git cleanup, as happened mid-session) no longer breaks the workflow.
- **What's NOT done in Phase α:** Developer-ID code signing + notarization (deferred to Phase δ for redistribution). The current bundle is ad-hoc signed — works locally on this Mac, isn't redistributable. Phase 7 milestone ".app bundle + code signing" remains unticked because of the second half.

### 2026-05-21 — Session 27: Cheats usable from a fresh save

- 2026-05-22: Gunshot lag — fifth pass. User-tested pass 4 (commit `3db9c28`): "better but not perfect, especially obvious running + firing, worse with Daodan on." Slice analysis of the gameplay window confirmed pass-4 gate worked (0 `[SS2/OAL] SetSoundData ADPCM` lines, was 887) but `[SS2]` tracers in `Oni_Sound2.c` are now dominant — 735 lines in the 1004-line gameplay window (73%). Tail was 60+ consecutive `OSrAmbient_Start/SSrAmbient_Start/OSrAmbient_Stop name='ap_wiz'` lines firing per-frame while Daodan active. Mechanism: position-based ambients re-trigger as Konoko moves (explains running), and the super-particle's ap_wiz cycles per-frame (explains Daodan). Wrapped all 12 `UUrStartupMessage` call sites in `Oni_Sound2.c` (OSrAmbient_BuildHashTable / OSiAmbient_Load / OSrAmbient_Start / 4× OSrAmbient_Stop variants / 2× OSrMusic_Start / hash-table tracers) behind new `ONI_SOUND_TRACE` env var via new `oniSoundTraceEnabled()` helper. Same env var as pass 4 — single switch re-enables full sound tracing if needed for Daodan investigation. `[DAODAN-DBG]` in `Oni_Character.c` left ungated (rare-fire on super-particle transitions; still load-bearing for Daodan leak dig).
- 2026-05-22: Gunshot lag — fourth pass under `superpowers:systematic-debugging`. User-tested pass 3 (commit `ab8e034`): still lagged after machine-gun reload + 2 shots, with NO Daodan involvement. Slice analysis of the gameplay window (lines 8702-10249 of startup.txt) showed all known AI / FNtW / TEXT / GRID tracers fully suppressed, but `[SS2/OAL] SetSoundData ADPCM` fires per sound-start with 23 calls in 22 consecutive lines during a single gunfire burst. Source: `BFW_SS2_Platform_OpenAL.c:320`. Pass-1 had already established that `UUrStartupMessage` does `fprintf+fflush` per call (the same mechanism that previously caused HALC overloads); 23 fflushes back-to-back in a ~100ms burst is the perceptible stall. Wrapped behind new `ONI_SOUND_TRACE` env var via new `oniSoundTraceEnabled()` helper in `BFW_SS2_Platform_OpenAL.c`. Architectural cost beyond the log (ADPCM decode + `alGetError` syncs per call, no PCM cache) deferred — separate refactor if log gating doesn't fully resolve.
- 2026-05-22: Gunshot lag — third pass. User-tested pass 2 (commit `7b11012`) under Daodan + gunfire stress: still lagged. Log slice analysis showed pass-2 gates fully suppressing TEXT/MOVE/LASER/PURSUIT/WEAPON/FNtW tracers, but 90 unsuppressed `[GRID-DBG]` lines in ~5s of gunfire from `Oni_AI2_MovementState.c:774,783,789` (path setup) + `Oni_AStar.c:1787` (DetEnd-FAIL). Wrapped all 4 sites behind the existing `ONI_AI_TRACE` env-var helper; added the helper to `Oni_AStar.c`. Residual non-instrumentation cost during gunfire is now ADPCM decode (489 `SetSoundData` calls in the 5s window) — architectural, not log overhead.
- 2026-05-22: Gunshot lag — second pass. User-tested first gate (commit `8764c71`): "better but still some lag." Log analysis showed 4M lines→111K (massive improvement, 0 HALC overload events) but residual hot tracers remained: `[TEXT-DBG]` 70K lines, `[MOVE-DBG]` 25K, `[LASER-DBG]` 4.7K, `[PURSUIT-DBG]` 1.6K. Gated all of them with the same env-var pattern: `[TEXT-DBG]` (BFW_TextSystem.c) behind `ONI_TEXT_TRACE`; `[MOVE-DBG]` (Oni_AI2_MovementState.c) + `[PURSUIT-DBG]` (Oni_AI2_Path.c, Oni_AI2_Pursuit.c) behind `ONI_AI_TRACE`; `[LASER-DBG]` (Oni_FX.c, Oni_Object/object_types/OT_Trigger.c) behind `ONI_LASER_TRACE`. 6 new files modified; same pattern as 8764c71. Pending behavioral verification.
- 2026-05-21: Gunshot lag fixed — root cause was OUR debug instrumentation, not audio. Gated per-frame `[WEAPON-DBG]` / `[KNOWLEDGE-DBG]` / `[ALERT-DBG]` tracers in `OniProj/OniGameSource/Oni_AI/Oni_AI2_{Targeting,Combat,Knowledge,Alert}.c` and the per-triangle `[FNtW]` tracer in `BFW_Source/BFW_Motoko/Engines/GeomEngine/Software/MS_Geom_Transform.c` behind env vars (`ONI_AI_TRACE=1`, `ONI_FNTW_TRACE=1`). With 5 armed AIs in combat the per-frame Targeting tracer alone was hitting `fflush` ~300×/sec, stalling the main thread and triggering downstream `HALC_ProxyIOContext` overload events. Kept in tree per `feedback_keep_diagnostics`; off by default. User verified: "better but still some lag" — partial fix; followup landed 2026-05-22.
- 2026-05-21: Daodan-mode sound-leak investigation diagnostics added (not a fix). One attempted fix mirroring `P3rDisposeParticle`'s cleanup on `P3cEvent_Stop` did not stop the leaking ambient (user verified). Reverted that and instrumented `ONiCharacter_SuperParticle_SendEvent` ([DAODAN-DBG]) and `OSrAmbient_Stop` ([SS2]) to capture exactly which ambient leaks and whether Stop ever fires. Log evidence so far: 30 OSrAmbient_Start ap_wiz events, 0 Stop events. Investigation parked for next at-desk run with diagnostics in place.
- 2026-05-21: Cheats usable from fresh save. Removed `THE_DAY_IS_MINE` compile-time gate around `thedayismine` and removed the `ONrPersist_GetWonGame()` runtime gate. All entries in `ONgCheatTable` (winlevel, liveforever, etc.) now work on first launch with developer access enabled. Test-loop accelerator for the rest of Phase 6.
- 2026-05-21: Hi-res console fix. `COrTextArea_Resize` now shifts the command line's top up 10px and the console-lines area's bottom up 10px so the input line isn't clipped at the bottom edge at modern resolutions. Lifted from Daodan's `DD_COrTextArea_Resize` patch (community-svn `Oni2/Daodan/src/Patches/Patches.c:125-133`).

### 2026-05-20 — Session 26 (continued): Text clipping FIXED — second instance of the embed-struct bridge bug

- **The session 26 text-clip bug is fixed and user-verified.** Three screenshots after the fix show the diary screen rendering full text everywhere: `BALLISTIC AMMO...........RELOAD WEAPON` (was `ISTIC AMMO`), `Coordinating the arms of mercenary...` (was `dinating...`), `Their solution was modular...` (was `r solution...`), `Hint:` (was `:`), `Reloading a weapon takes time:` (was `ading a weapon...`), `Plan accordingly.` (was ` accordingly.`), plus the encrypted-message diary entry and the weapon info panel all render their leading characters intact.
- **Root cause (`fix(64bit): bridge embedded ONtIGUI_FontInfo alignment in IGSt template`, commit `9821d11`):** exact same bug class as session 25's AKVA fix, applied to a different template. `ONtIGUI_String` (template `'IGSt'`) embeds `ONtIGUI_FontInfo` at its head; `ONtIGUI_FontInfo` contains a `TStFontFamily*` pointer so its alignment on 64-bit is 8 and its compiler-rounded sizeof is 24 (vs the 20-byte sum of field sizes). The bridge walker, oblivious to embedded-struct boundaries, walks the field stream flatly — `font_info`'s last field (`flags`, 2 bytes) ends at walker descriptor offset 28 (preamble 8 + 20), and the following `char string[384]` array is 1-byte-aligned so no alignment bump absorbs the drift. Walker writes `string[0]` at offset 28 but C reads `string[0]` from offset 32. The first 4 bytes of every on-disk string fall into the `font_info` trailing-pad slot in C's view and disappear.
- **Why the symptom looked like a clip-rect bug:** every dialog title and body line lost its leading 3-5 characters. The actual mechanism was upstream of the renderer — the strings were already truncated at template-load time. The first investigation agent hypothesised a `WMrClipRect` trivial-reject; the `[TEXT-DBG]` tracer I added at `TSiContext_DrawTextLine` showed every glyph at correct position with `glyph.left == clip.left` for i=0 — proving the renderer was getting already-clipped strings, not clipping them itself. Two screenshots from the user (showing `TH METER TRAINING` and `ISTIC AMMO`) made the 4-byte-character-offset pattern obvious enough to inspect `ONtIGUI_FontInfo` directly.
- **Fix:** extended `iFixupEmbeddedStructAlignment` in `BFW_TM_Bridge.c` with an `'IGSt'` branch that shifts every field at-or-after walker offset 28 by +4 bytes. The 384-byte string is encoded as two consecutive FixedArrays (255 + 129); both get the shift. The walker's existing 8-alignment padding on the descriptor's `dst_size` (= 416) supplies the 4 extra bytes — no overflow.
- **Embed-struct bug class scoreboard:** AKVA (PHtRoomData embed → silent AI movement failure, session 25) and IGSt (ONtIGUI_FontInfo embed → silent text clipping, session 26). Two instances of the same class, two completely different visible symptoms. The remaining ~112 templates still need an audit. The leading candidate is the laser-beam template flagged earlier this session: env-effect / particle-class templates are the next likely embed sites.
- **Diagnostic retained per `feedback_keep_diagnostics`:** `[TEXT-DBG]` tracer at `BFW_TextSystem.c:1829` logs glyph clip vs bounds for first 5 chars of every text-line draw. Confirmed innocent in this investigation; kept in tree for the next text-rendering regression.

### 2026-05-20 — Session 26: Tutorial level completed end-to-end — Phase 5 done, Phase 6 first ticks

- **User completed the tutorial level start-to-finish and crossed the next-level transition** with no crash. First time the port has ever crossed a level boundary mid-gameplay. Three milestones tick in one playthrough: Phase 5's last item (NPC-vs-NPC combat completes), Phase 6's "Konoko vs NPC end-to-end combat", and Phase 6's "Tutorial completable to next-level transition".
- **What worked, observed visually:** info-panel tutorial dialogs render and step through Next/Previous, weapons fire and reload correctly, melee combat works on NPCs, NPC-vs-NPC combat resolves to a kill, level-exit trigger fires, next-level load completes without SIGBUS/SIGSEGV.
- **Two visible bugs survived the playthrough.** Both are pre-existing; neither blocked completion:
  - **Text clipping** (known, Phase 2 open item): `"TH METER TRAINING"` instead of `"HEALTH METER TRAINING"`, `"ISTIC AMMO"` instead of `"BALLISTIC AMMO"`, `"ading a weapon takes time"` instead of `"Reloading a weapon takes time"`. Consistent 3–5 char left-edge clip in dialog body and title text. Affects every tutorial popup.
  - **Security-laser beams not rendering** (new): in the tutorial security-laser room, the wall-mounted projector hardware (the three-rail emitter mounts) renders correctly, but no beam visual appears between paired emitters. Strong hypothesis: same embed-struct bridge-alignment bug class as session 25 (113 templates not audited), applied to whichever env-effect / particle-class template defines the laser beam visual. Symptom signature matches the `can't find emitted particle ''` warning floods we already see in debugger.txt — env-effect template loading a name field that came back empty because the bridge dropped a slot.
- **Logging gotcha discovered:** `startup.txt` is unlinked-while-open by Oni (lsof confirms FD held, dir entry absent). It survives a SIGKILL (kernel flushes on FD release) but vanishes on graceful exit — so a clean playthrough leaves no log on disk. To capture logs from a clean run we either need to SIGKILL the process before it shuts down OR fix the Oni code to not unlink. Filed as a workflow concern, not a port bug.
- **No code changes this session** — pure verification + docs.

### 2026-05-20 — Session 25 (continued): Scripted patrol movement also verified

- After the AKVA bridge fix landed, a separate repro confirmed scripted NPC patrol paths now execute end-to-end. User observed an NPC follow its patrol route, turn around (waypoint completion), detect the player on sight (Knowledge contact + Pursuit goal transition), and pursue up a flight of stairs (cross-BNV pathfinding + stair-flag handling). One fix, two milestones ticked.
- 55 GRID-DBG events on this run, 55 success=1, zero DetEnd-FAIL, MOVE-DBG with non-NULL next_pt on 946/1814 commits. Stable behaviour across multiple NPCs / multiple BNVs / scripted vs reactive movement paths.

### 2026-05-20 — Session 25: Pursuit movement FIXED — embedded PHtRoomData bridge alignment

- **The fix landed and is user-verified.** Konoko stood in front of an NPC in level 1; the NPC walked over and engaged. MOVE-DBG metrics flipped from 0/1041 → 368/869 commits with non-NULL `next_pt`, 519/869 with `actual_dir` non-Stopped, mostly Forward. PHrPrepRoomForPath tracer flipped from `gridX=80 gridY=0` (broken) to `gridX=37 gridY=80` (correct — room is 37 columns × 80 rows, was previously reading disk's gridY into the gridX slot and zero into the gridY slot).
- **Root cause (`fix(64bit): bridge embedded PHtRoomData alignment in AKVA template`, commit `6c030e0`):** the 32→64 template-instance bridge walker in `BFW_TM_Bridge.c` (which translates on-disk 32-bit instance data into 64-bit in-memory C structs) walks the swap-code stream as a flat sequence of scalars/pointers/arrays. There is no swap-code marker for "begin embedded `tm_struct`" — those boundaries were a no-op on 32-bit because no `tm_struct` had an alignment requirement larger than 4. On 64-bit, `PHtRoomData` (embedded inside `AKtBNVNode`) contains pointers (`compressed_grid`, `debug_info`) and therefore has alignment 8. The C compiler 8-aligns the embedded struct by inserting 4 bytes of pad before `roomData` in `AKtBNVNode`. The walker, oblivious, placed `gridX` and `gridY` at dst offsets 28/32 — the C compiler has them at 32/36. Because the walker correctly aligns `RawPtr` to 8 when it later hits `compressed_grid`, the drift cancels at offset 40 and every field after `compressed_grid` ends up correct. So the visible damage is exactly: `room->gridX` reads disk's `gridY`, `room->gridY` reads memset-zero.
- **Why the symptom looked like AI movement and not the template manager:** `room->gridY = 0` made every `(inY >= room->gridY)` bounds check in `ASiDetermineEndPoint` (Oni_AStar.c:1795) succeed for all `inY ≥ 0`, so no destination square was ever passable. `ASrPath_Generate` returned `UUcFalse` for every call. Inside `AI2iMovementState_SetupPath`, that triggered `AI2_ERROR` + `AI2rMovementState_ClearPath` — but `AI2_ERROR_REPORT = 0` in release, so the error was completely silent. The visible symptom was just "NPC sees you, enters Pursuit, never moves." Session 24's diagnostics narrowed to "grid-path generation is empty" but couldn't see why. Session 25 added three GRID-DBG tracers, the very first SetParams event printed `gridXY=(80x0)`, and the bridge bug became immediately obvious.
- **Fix scope:** a small post-pass (`iFixupEmbeddedStructAlignment`) in `TMrBridge_BuildDescriptor` recognises the `'AKVA'` template tag and shifts the two leading scalars in the element sub-descriptor by +4 bytes each. 64 lines added, all in `BFW_TM_Bridge.c`. No changes to swap codes, no checksum changes, no on-disk format changes.
- **Audit deferred:** `PHtRoomData` is the only known embedded multi-pointer `tm_struct` in this codebase (grep confirms it's embedded only in `AKtBNVNode` per `BFW_Akira.h:516`). A systematic audit of the other 113 templates for similar patterns is a follow-up — any template embedding a `tm_struct` whose alignment is `> 4` on 64-bit will have an equivalent bug, manifesting as some other "silent struct field is zero" symptom downstream.
- **Diagnostics retained per `feedback_keep_diagnostics`:** `[GRID-DBG] SetParams`, `[GRID-DBG] Generate`, `[GRID-DBG] DetEnd-FAIL`, `[GRID-DBG] SetupPath-FAIL`. The first two are also useful for verifying any future grid-path regression.

### 2026-05-20 — Session 25 (earlier): [GRID-DBG] tracers land; AI2_ERROR silent-in-release explains session 24's misread

- **Three `[GRID-DBG]` tracers added** around `AI2iMovementState_SetupPath`'s two failure-bearing calls (`ASrPath_SetParams`, `ASrPath_Generate`) and at the silent UUcFalse return in `ASiDetermineEndPoint` (`Oni_AStar.c:1787`). Retained per `feedback_keep_diagnostics`. Plus one tracer at the SetupPath ClearPath site for confirmation.
- **Diagnostic discovery #1: `AI2_ERROR` is silent in release.** `Oni_AI2_Error.h:22-25` defines `AI2_ERROR_REPORT=0` for non-TOOL_VERSION builds, so the `AI2_ERROR(...)` macro expands to `AI2rHandleError(...)` (no console / log output) instead of `AI2rReportError(...)`. Session 24's conclusion "no AI2_ERROR in startup.txt ⇒ SetupPath returned true" was wrong — the absence carried zero information. SetupPath was almost certainly returning false silently and calling `AI2rMovementState_ClearPath` (which is what produced `grid_num=0`, `next_pt=NULL` in MOVE-DBG).
- **Diagnostic discovery #2: the "successful" 7890/7891 PURSUIT-DBG event was during level-load.** The two lines immediately around it in startup.txt are `[lvl-load] before SLrScript_ExecuteOnce(main)` and `[lvl-load] after SLrScript_ExecuteOnce(main) err=0`. It was a script-driven path (different BNVs, took the `path_connections[0]->from` branch), not a real-combat pursuit. The 19 in-combat events (all same-BNV, all `ok=0 num_nodes=1`) are the actual symptom — every one of them silently failed in SetupPath.
- **No code semantics changed.** Diagnostic-only commit. Next repro categorises the failure (SetParams vs Generate vs DetEnd vs CalcWaypoints), then a targeted fix from one of four pre-analysed branches.
- **Build observation noted for follow-up:** `Oni_AStar.c:809-810` warns on `UUtInt16 *` → `UUtUns16 *` pointer-sign mismatch in `PHrWorldToGridSpace` calls. Pre-existing in Bungie's source; not 32→64, but worth keeping in mind if the GRID-DBG tracers show out-of-bounds grid coordinates being silently pinned to UUtUns8.

### 2026-05-20 — Session 24: Alert-escalation tracers land, reveal Pursuit-movement is the bug

- **Five `[ALERT-DBG]` tracers** added across `Oni_AI2_Alert.c` (four: at NotifyKnowledge entry / NotThreat-return / before UpgradeStatus call / before CombatGate) and `Oni_AI2.c` (one: in `AI2rEnterState`'s `AI2cGoal_Combat` dispatch). Bracket every decision point on the Knowledge → Combat escalation chain. Retained per `feedback_keep_diagnostics`.
- **Repro produced a clean diagnosis.** User played to a level-2 hallway with a hostile NPC. `startup.txt` captured: 42 `NotifyKnowledge` events, 42 `UpgradeStatus-call` events, 42 `CombatGate` events with `will-enter=0`, and exactly **one** `[ALERT-DBG] EnterState dispatching Combat` followed by the existing `[WEAPON-DBG] Combat_Enter` tracer. So combat *does* work — it fires once successfully when the conditions are right.
- **Type breakdown:** 3× type=1 (Sound_Interesting / Konoko's footsteps), 38× type=5 (Sight_Peripheral), 1× type=6 (Sight_Definite). The single type=6 event correctly escalated to Combat — `new=4 old=1 will-call=1` → `inStatus=4 ... will-enter=1` → EnterState dispatch → Combat_Enter. The chain is mathematically and structurally fine.
- **The real bug:** the NPC sees Konoko mostly via `Sight_Peripheral` (centre-cone math at `Oni_AI2_Knowledge.c:987-1010` requires `dist < central_dist`, and the central-cone radius is small at the angles the player sits in). Peripheral sight escalates only to `AI2cGoal_Pursuit` (verified via tracer: NPC's `goal` transitioned 3=Patrol → 6=Pursuit). **Pursuit then fails to translate into NPC locomotion.** The NPC stays put, so `aimrel_along` / `dist` stay roughly constant frame to frame, so peripheral classification stays sticky every frame, so combat is never re-entered. Only the player physically closing the distance produced the one type=6 event observed.
- **Next investigation target:** the Pursuit movement layer. Specifically `AI2rPursuit_Enter` / `AI2rPursuit_Update` / `Oni_AI2_Movement.c` / `Oni_AI2_MovementState.c`. The earlier session 21 scripted-NPC-doesn't-walk-into-room symptom and this Pursuit-doesn't-close-distance symptom are likely the same bug class: AI characters not translating position even when their state machine says they should. Investigation continues in a fresh thread; expect more diagnostic tracers, not a speculative widening sweep.
- **Eliminated as cause:** Knowledge `user_data` truncation (already widened, structurally correct), AI2_ERROR macro / inParam3-inParam4 truncation (already widened, structurally correct), the Alert→Combat escalation chain itself (proven working when fed Sight_Definite), goal-dispatch (proven working — `AI2rCombat_Enter` did fire after the dispatch tracer the one time the chain completed). All ruled out by evidence, not by assumption.

### 2026-05-20 — Session 23 continued: AI error subsystem widening — structurally correct, NOT symptom-verified

- **AI error subsystem widened across 10 files / 184 lines.** Sweep applied: `UUtUns32 inParam3 / inParam4` → `uintptr_t inParam3 / inParam4` everywhere in the AI error-handler signature family, plus the `AI2_ERROR` macro's cast from `(UUtUns32)` to `(uintptr_t)` on all four params. Touches `Oni_AI2_Error.{h,c}`, `Oni_AI2_Combat.{h,c}` (3-param `AI2tBehaviorFunction` family), `Oni_AI2_Maneuver.c`, `Oni_AI2_Melee.c`, `Oni_AI2_Patrol.{h,c}`, `Oni_AI2_Neutral.{h,c}`. Closes the cascade-fix #2 and #3 territory from session 21's audit in one pass — the macro previously truncated all four params, including pointers like `PHtNode *` (Maneuver pathfinding error) and `TRtAnimation *` (Melee combat behavior). The `AI2iManeuver_PathfindingErrorHandler` at `Oni_AI2_Maneuver.c:198` reads `inParam3` as `(PHtNode *)` — that path now receives an intact 8-byte pointer when triggered.
- **Symptom was wrong.** User-observed AI behaviour in level 2 (NPCs in combat stance refusing to advance until Konoko closes distance) was the hypothesised symptom — but post-fix verification showed the widening did not relieve it. The new diagnostic evidence reframes the bug: in this session's playthrough, `[KNOWLEDGE-DBG]` fired 10 times (owner=NPC, enemy=char_0/Konoko, types 1+6 = first sight + ongoing sight contacts) but **zero combat tracers fired**. `AI2rCombat_Enter` never ran. So combat is never being entered despite the NPC seeing Konoko clearly. The bug is upstream of combat — alert-level escalation in the Knowledge / Alert layer is the next investigation target.
- **Why land it anyway:** it's a real port-exposed 32→64 bug pattern, structurally correct, no regression risk (every `UUtUns32` value fits in `uintptr_t`; the previous truncating casts were only ever destructive). It will eventually be exercised by some code path that does need 8-byte param survival — better to close the latent trap now than re-discover it later. Marked clearly as "not symptom-verified" so future sessions know it's not the AI advance fix.
- **Diagnostics inventory in tree:** Knowledge tracers ([KNOWLEDGE-DBG] at `Oni_AI2_Knowledge.c` lines 647, 654, 1209, 1269 from session 21) and combat tracers ([WEAPON-DBG] at `Oni_AI2_Combat.c` lines 318, 507, 592, 3986 from session 21) plus animation diagnostics (`ANIM_PREP` BFW_Totoro.c:2711, `SND` Oni_Sound_Animation.c:1834 from session 22). All retained per the diagnostics-retention policy. Combat tracers proved their value this session by *not* firing — that absence is the evidence pointing to the alert-escalation layer.

### 2026-05-19 — Session 23: Footsteps audible — impact-effect on-disk struct bridge landed

- **Footsteps now play.** User-verified audibly in the level-1 opening — Konoko's run produces footstep impacts as designed.
- **Root cause:** `ONrIEBinaryData_Process` was rejecting the 47004-byte impact-effect blob against an `expected_size` of 49644, falling into its `goto cleanup` and triggering `ONrImpactEffects_CreateBlank` at `Oni_Level.c:454`, which silently produces an empty lookup table. With nothing to find, `ONrImpactEffect_Lookup` returns nothing for every footstep, and `OSrImpulse_Play` is never called. End-to-end silent footsteps.
- **Why on 64-bit:** `ONtIESound` and `ONtIEParticle` (via embedded `P3tEffectSpecification`) both carry pointer fields. On the Win32 disk file those slots are 4 bytes; on ARM64 our runtime structs put 8-byte pointers in them (plus alignment padding before the pointer in `ONtIESound`). The sanity check at `Oni_ImpactEffect.c:2075` was comparing `buffer_size` to a `sizeof()`-summed `expected_size`, so the 2640-byte delta (84 particles × +4 + 288 sounds × +8) always trips. The other five impact-effect struct types are pointer-free and disk-size == runtime-size, so no bridge needed for them.
- **Fix shape** (`Oni_ImpactEffect.c`): `ONcIESound_OnDiskSize` (48) and `ONcIEParticle_OnDiskSize` (84) constants added near the version enum; two per-record disk readers (`ONiIESound_ReadFromDisk`, `ONiIEParticle_ReadFromDisk`) copy each field at its known disk offset and leave pointer slots NULL; two per-type bridge functions (`ONiIESound_ConvertArrayFromDisk`, `ONiIEParticle_ConvertArrayFromDisk`) allocate `UUtMemory_Array`s and walk the disk buffer record-by-record. The three change sites in `ONrIEBinaryData_Process` are guarded `#if UUmPlatform_PointerSize == 4 / #else` so the 32-bit build path is unchanged. The existing `ONiImpactEffect_GetSound/GetParticle` accessors already abstract static-vs-dynamic-array; every call site just keeps working. Pointer resolution by name (`ONrImpactEffects_SetupSoundPointers` / `_SetupParticlePointers`) is unchanged and was already called by sound2 / particle3 init after binary data load — it now finds non-empty arrays to walk.
- **Verification:** clean build, clean launch under `lldb -b`, clean exit. `debugger.txt` no longer reports `expected size 49644 but buffer is 47004!` or the cleanup-path error at line 2216. Game reached level 1 and ran to a `game over` event. Pre-existing diagnostic fprintfs (`ANIM_PREP` in `BFW_Totoro.c`, `SND` in `Oni_Sound_Animation.c`) kept in tree per the diagnostics-retention policy — they helped triage the dead-end "animation sound system" path (`OSrSoundAnimation_Play` returns has=0 because shipping data has no `Binary/SoundAnims/`, expected behaviour) and confirmed the live path is the impact-effect chain.
- **Cascade carry-overs (not blockers for this fix):** the AI combat-stance-freeze-after-first-kill from earlier in the session still likely sits in cascade fix #2 (pathfinding error handler) territory. The `Particle class 'w10_sni_p01' is too large (268) for largest size class (256)!` warning is a separate latent issue unrelated to footsteps.

### 2026-05-19 — Session 23: AI Knowledge `user_data` widening landed

- **Knowledge `user_data` widened** (`Oni_AI2_Knowledge.h:87`, `Oni_AI2_Knowledge.c:74` and signatures at 124/130/1053/1126): `UUtUns32` → `uintptr_t` for `AI2tKnowledgeEntry.last_user_data`, `AI2tKnowledgePending.user_data`, and the `inAIUserData` parameter of `AI2iKnowledge_PostContact` / `AI2iKnowledge_AddContact`. Two truncating call-site casts in `AI2rKnowledge_Sound` (lines 648 and 654) switched from `(UUtUns32) inTargetN` to `(uintptr_t) inTargetN` so target-character pointers survive the trip into the knowledge layer. Cascade fix #1 from session 21's audit.
- **Behavioural run (verified non-regression, not symptom-verified):** game launched under `lldb -b`, played through the level-0 spectator combat room (3 NPCs: 2 hostile + 1 neutral) and quit normally. Clean exit, no crash. `[KNOWLEDGE-DBG] STORE` tracer fired 49 times across demo1/demo2/demo3/`char_0` with `user_data` values in `{0x0, 0x4, 0x5, 0x6, 0xa}` — all zero or damage-amount small ints. **`PostContact (sound...)` tracer and `READ-AS-PTR` tracer never fired** — the sound-event codepath the widening protects was not exercised by observed combat. Widening is structurally correct (closes a latent 32→64 truncation trap) but the test run did not bite into the protected bytes.
- **Symptom carried forward:** one pair fought to completion, then the surviving two locked into combat-ready stance facing each other and could not initiate engagement. That is target-acquisition-after-first-kill failure — AI knows there's an enemy (combat stance was entered), but cannot decide to act. Sits downstream of Knowledge, in pathfinding or behaviour-decision territory. Likely candidate: cascade fix #2 (`AI2iManeuver_PathfindingErrorHandler` declaring `inParam3` as `UUtUns32` and reading back `(PHtNode *) inParam3`).
- **Diagnostics retained** in tree per `feedback_keep_diagnostics` policy: `[KNOWLEDGE-DBG]` tracers at PostContact call-site (Knowledge.c:647/654), AddContact STORE (Knowledge.c:1209), AddContact READ-AS-PTR (Knowledge.c:1269).

### 2026-05-04 — Session 21: AI combat crash root-caused

- **AI combat crash fixed.** `AI2rCombat_NotifyKnowledge` (Combat.c:1120) passed `(UUtUns32) inEntry` as `inParam2` to `AI2rCombat_Behavior`, truncating an 8-byte `AI2tKnowledgeEntry *` to its lower 32 bits. The Hurt message handler at line 4055 cast `inParam2` back to `AI2tKnowledgeEntry *` and crashed on the first field access (`entry->enemy` at offset +8, address `0x4ea7e6b0` was the lower 32 bits of a real heap pointer). Changed cast to `(uintptr_t)`. Note: `inParam1` and `inParam2` are already `uintptr_t` in the typedef — only the call-site cast was wrong. Three NPCs entered combat without crashing in verification run. Initial handoff diagnosis (`weapon_parameters` at line 3963) was a stale crash report — the live crash was on a completely different path (`AI2rCombat_NotifyKnowledge` → `AI2rCombat_Behavior` → `AI2rBehavior_Default` Hurt handler, not the TooClose handler).
- **Cascade exposed:** AI combat now reachable but stalls intermittently. Audit identified 4+ remaining truncation sites in the AI message system: knowledge contact `last_user_data` field is `UUtUns32` and is fed by truncated pointers at Knowledge.c:648/652; `AI2tBehaviorFunction.inParam3` is `UUtUns32` (Melee.c:1300 truncates `TRtAnimation *`); `AI2_ERROR` macro pre-truncates all four params with `(UUtUns32)` casts; `AI2rManeuver_PathfindingErrorHandler` declares `inParam3` as `UUtUns32` and reads back `(PHtNode *) inParam3`. Tutorial combat playable but doesn't progress reliably — next investigation: coordinated sweep of these layers.
- **AUrQSort_32 audit (not landed):** 6 broken call sites identified that sort arrays of pointers through the 4-byte sort: `BFW_SoundSystem2.c:1015`, `Oni_Object.c:4168`, `BFW_Timer.c:398`, `BFW_Console.c:734`, `BFW_Particle3.c:2918,3027`. 11 other call sites are safe (they sort `UUtUns32` indices). Latent until those code paths execute.

### 2026-05-03 — Session 20: NPC activation fixes + HiDPI viewport fix

- **Pathfinding crash fixed:** `AKiPrepareGrids` had `offset = 0` on 64-bit, assuming the template bridge resolved AKVA's `tm_raw` pointers. But AKVA is a Leaf template — the bridge skips Leaf templates during `PreparePointers`. On 32-bit Bungie resolved raw pointers manually in subsystem `LoadPostProcess` callbacks; the prior session's `offset=0` broke this. Fix: restore resolution with `uintptr_t offset` (not `UUtUns32`, which truncates the rawBase). Also bypasses `UUmOffsetPtr` which casts offset to `UUtUns32`. Same fix applied to `gqDebug` name pointers in the same handler.
- **Character sort crash fixed:** `distance_from_camera_compare` took `UUtUns32` parameters (truncating 8-byte `ONtCharacter*` pointers), and `AUrQSort_32` sorted the array as 4-byte elements. Replaced with standard `qsort()` using pointer-width comparator. NPCs now render on screen for the first time.
- **Cascade status:** game now crashes in `AI2rBehavior_Default` (AI combat behavior) — bad `weapon_parameters` pointer (0x2e810310). Next investigation target.
- **HiDPI viewport scaling**: `glViewport` now uses `SDL_GL_GetDrawableSize()` to fill the actual screen instead of rendering 640×480 in the bottom-left corner. Mouse coordinates scaled from window space to game space in all three input paths (GetMouse, MouseMotion, MouseButton). Game's internal resolution stays 640×480 (ortho projection unchanged); the viewport stretches it to fullscreen. Files: `gl_sdl.c`, `gl_engine.c`, `gl_utility.c`, `OGL_DrawGeom_Common.c`, `BFW_LI_Platform_SDL.c`.
- **Resolution switcher fixed:** `ONiResolution_Switch` forced `osx = UUcTrue` for SDL builds, triggering the 2001-era "You must restart Oni" exit path on any resolution change — a workaround for 3Dfx/S3 video cards that couldn't hot-switch. Set `osx = UUcFalse` for SDL since viewport scaling handles runtime resolution changes natively.
- **Body horror fixed:** `TRrQuatArray_SetAnimationInternal` byte-swapped the per-bone offset into compressed animation data, guarded by `UUmPlatform == UUmPlatform_Mac`. This was correct for big-endian PowerPC but wrong for little-endian ARM64 — every bone read quaternion keyframes from a corrupted offset, producing twisted joints and levitation. Changed guard to `UUmEndian == UUmEndian_Big`. User-verified: Konoko and NPCs render with correct poses.
- **Resolution persistence fixed:** `BFrFile_FOpen` couldn't create new files because `BFrFileRef_Set` checks file existence and fails for non-existent paths. Added write-mode fallback that bypasses the file-ref layer and calls `fopen` directly. `persist.dat` now created on first resolution change; saved resolution restored on next launch.

### 2026-05-02 — Session 19: Phase 3 — Bug B fixed (character geometry crash)

- **Bug B root cause:** `MSrClip_ComputeVertex_TextureInterpolate` creates interpolated vertices at indices ≥ `numPoints` during frustum clipping. `objectVertexData.textureCoords` pointed directly into geometry's `texCoordArray` (exactly `numPoints` entries), while all other scratch arrays (`frustumPoints`, `screenPoints`, `clipCodes`) were pre-allocated with 2048 entries. Clip writes past `texCoordArray` stomped adjacent template memory — specifically `triNormalArray->numVectors` (329 → float 0.398506 bit pattern 1053559037), causing `FaceNormalToWorld` to loop into unmapped memory on the next frame.
- **Fix:** added `textureCoordsScratch` field to `MStTransformedVertexData`, allocated in `MSrTransformedVertexData_Alloc` with 2048 entries (matching other scratch arrays). At geometry draw time, `texCoordArray` contents are copied into the scratch buffer via `UUrMemory_MoveFast`, and both `objectVertexData.textureCoords` and the draw-state pointer are redirected to the scratch. Same pattern as the existing env-clipper fix for `gqVertexData` (session 9, `103496b`). Separate `textureCoordsScratch` pointer needed because sprite/contrail draw paths overwrite and NULL `textureCoords` between geometry draws.
- **Verified:** full training cutscene plays (Shinatama dialogue + camera pan + Konoko character model renders). All `[FNtW]` log entries show `numVec` in normal range (2–353). No corruption.
- **Body horror confirmed:** Konoko levitating mid-air, flexing joints. Separate from Bug B — bone-transform or animation-state issue (Step 3.2).
- **Subtitle/message fix:** `SSiSubtitleArray_FindByName` and `FindByNumber` used `(UUtUns32) TMrInstance_GetRawOffset()` which truncated the 64-bit rawPtr base. Changed to `uintptr_t`. Subtitles display correctly (T1 — user saw Shinatama's dialogue with portrait). Tutorial scripting runs end-to-end (25+ message lookups).
- **New cascade:** game crashes when an NPC becomes active and AI initializes pathfinding — `PHrGrid_Decompress` hits bad memory during `AI2rMovementState_Initialize`. The game now gets far enough into the tutorial to activate NPCs.

### 2026-04-27 — Session 17: Phase 2 — callback truncation sweep (Bug CB)

- **Full `UUtUns32 inUserData` → `uintptr_t` sweep across ~80 sites in 43 files.** Six typedef chains widened end-to-end: `OBJtEnumCallback_Object` impls, `OBJtEnumCallback_ObjectName` + `OBJtMethod_Enumerate`, `P3tClassCallback`, `AStHeap_CompareFunc`/`NotifyLocation`, `ONtEvent_EnumCallback_TypeName`, `WMtWindowEnumCallback` + `WMrDialog` userdata chain. Every callback impl that receives a pointer through `inUserData` now reads the full 64-bit value.
- **Level 1 load now completes.** All tripwires pass through `OBJrObject_LevelBegin`, `AI2rLevelBegin`, `ONrScript_LevelBegin`, `InitializeActionMarkerArray`, `ONrMechanics_LevelBegin`, character preload. The old `ONiFlagsExist` crash (truncated pointer `0x6aee1bab`) is gone.
- **Cascade found:** `P3rTraverseParticleClass` crashes during particle precache in level 1 begin — a `P3tParticleClass*` pointer (`0x2efb48`) is a 32-bit value that wasn't resolved to a 64-bit address. This is a template data pointer issue in the particle sub-emitter class linkage, not a callback truncation. Next investigation target.

### 2026-04-27 — Session 16: Phase 1 complete — menu music plays

- **Audio works end-to-end.** User audibly confirmed clean menu music (T1 verified). Phase 1 success criterion met.
- Root cause of session 15's "music under static": the shipping data is Microsoft ADPCM (wFormatTag=2, stereo, 1024-byte blocks) but the code routed it to Bungie's custom IMA ADPCM decoder based on a flags field that didn't indicate compression. IMA decoder blew up within the first packet (step index diverging, output railing at ±32768).
- Fix: route by `SStFormat.wFormatTag` (== 2 → ADPCM_MS via libavcodec) instead of `flags & SScSoundDataFlag_Compressed`. Read channel count, block alignment, sample rate, and bits-per-coded-sample from the SStFormat metadata rather than hardcoded constants. Stereo decode + OpenAL stereo upload now works correctly.
- Secondary fix: removed `UUmSwapBig_2Byte` from IMA state-word reads in `BFW_SS2_IMA.c`. The shipping Windows `.raw` files store IMA data in little-endian (platform-native) byte order; the big-endian swap was producing wrong predictor/index values. IMA path not exercised by menu music (which is ADPCM_MS) but will matter for any future IMA-format sounds.
- Stripped all session 15/16 audio instrumentation (Play/Resume logging, hex dumps, WAV dumps).
- New Game still crashes (Bug CB — `ONiFlagsExist` via `OBJiObjectGroup_EnumerateObjects+196`, SIGSEGV at truncated pointer `0x6aee1bab`). This is the known callback-truncation class, Phase 2 work.

### 2026-04-26 — Session 15: Phase 1 audio investigation

- Step 1.1 (audible-path instrumentation): confirmed **category γ** — `alSourcePlay` never called. Bug A diagnosis validated.
- Step 1.3γ (fix the gate): Re-applied Bug A `.sep→.raw` fallback. Fixed MSADPCM EAGAIN value (`-11` → `AVERROR(EAGAIN)` for macOS). Added `block_align`/`bits_per_coded_sample` to ADPCM codec context. Added safety guard for bad data pointers. Re-enabled `SStSoundData.data` raw-pointer resolution on 64-bit (was incorrectly `#if`'d out). Discovered data format is Oni's IMA ADPCM (not MSADPCM); implemented IMA decompression via `SSrIMA_DecompressSoundData`. Root-caused why all 796 SNDD instances had identical field values: the `SStFormat` struct (50 bytes) was commented out of the C struct but present in on-disk data, causing swap codes to read WAVEFORMAT fields as audio metadata. Uncommented `SStFormat f`, packed it with `__attribute__((packed))` to eliminate 2-byte trailing padding mismatch, and rewrote SNDD swap codes to include all SStFormat fields + fixed-length aCoef[7] array. Per-instance data now varies correctly. Audio reaches speakers — user reports "music underneath static." Remaining corruption likely in data pointer resolution or IMA state handling. **Not yet audibly clean — Step 1.3γ continues next session.**

### 2026-04-25 — Session 13: replan + land Phase 0 in-tree wins

Brainstormed and committed a stepwise spec replacing the cascade-grind: [docs/superpowers/specs/2026-04-25-path-to-playable-spec.md](../docs/superpowers/specs/2026-04-25-path-to-playable-spec.md). Six phases, each step pre-conditioned + verified + rollback-safe, skill mapping per step, re-entry protocol so a future session can pick up at "do step N." Phase 0 is landing the in-tree session-13 wins one commit at a time before chasing the audio cascade again.

- `4931969` Options dialog used Mac variant ID (157) but the shipping data only includes the PC variant (152) — the Mac dialog request silently no-op'd. Stripped the `#if (UUmPlatform == UUmPlatform_Mac)` branch in `Oni_OutGameUI.c` so the PC dialog (152) is requested unconditionally. Verified end-to-end this session: Options dialog opens, sliders/dropdowns visible, Cancel returns cleanly. Tiny user-visible win.
- `769798e` `P3iBridge32To64` for `P3tParticleDefinition` landed in `BFW_Particle3.c` behind `#if UUmPlatform_PointerSize == 8`. Bridges three layout gaps in the on-disk PAR3 blob: (a) `P3tAttractor::attractor_ptr` 4→8 bytes (struct grows 216→224), (b) `P3tParticleDefinition` trailing variable/action/emitter pointers 12→24 bytes, (c) `P3tEmitter::emittedclass` 4→8 bytes per emitter (struct grows 444→448). Pinned with `_Static_assert`s on every relied-on size and offset. **Inert without Bug A:** the BINA dispatch chain currently short-circuits at `OSiBinaryData_ProcHandler`, so `P3iProcessParticleClass` is never reached and the bridge function never runs. Verified: `num_ambients=0` in `startup.txt`, no PAR3 loader entries, binary reaches menu cleanly. Lands in tree now to preserve the work and prime the next session for the paired Bug A re-apply.
- Step 0.3 (`ONI_RESOLUTION` env var override) deferred. The env var path correctly drives the active mode index, GL viewport, and WM desktop size to 1920×1080 — but the main menu dialog (resource ID 150) carries hardcoded 640×480 dims in the shipping game-data fork, plus `SDL_SetWindowFullscreen(FULLSCREEN_DESKTOP)` likely expands the framebuffer past 1920×1080 on retina. Result: env var resizes the SDL window, but the menu renders ~640×480 in a corner of a giant black void. Fix requires aligning five separate coordinate-space layers (SDL window, SDL fullscreen, GL viewport, GL ortho, WM desktop, dialog resource, in-game HUD) — real Phase 4 HiDPI work, not a one-liner. Architecture map and three ranked fix candidates documented in [`docs/handoff-2026-04-25-session13-step0_3-deferred.md`](../docs/handoff-2026-04-25-session13-step0_3-deferred.md). Phase 0 closes with 0.1 + 0.2 banked.

### 2026-04-25 — Session 12: Bug A diagnosed, fix landed and reverted, cascade discovered

Net forward progress: Bug A's root cause is now known with certainty. Net regression: a 64-bit bug in the particle loader (Bug C) was unblocked by Bug A's fix and crashes init. Fix reverted at session close. Binary back to silent-but-runnable. Full handoff with re-entry instructions: [`docs/handoff-2026-04-25-session12-revert.md`](../docs/handoff-2026-04-25-session12-revert.md).

- `2f1e080` Audio investigation: re-traced the BINA dispatch chain end-to-end and added narrow breadcrumbs at every hop (`BDrRegisterClass`, `BDiBinaryData_ProcHandler` entry, `TMiGame_LoadedInstanceFiles_Add` per-tag `ContainsTemplate`, `TMiGame_LoadedInstanceFiles_PrivateData_Add`, `TMiGame_InstanceFile_Callback` with `numPrivateInfos`, `OSiAmbient_Load` entry). These stay in tree.
- Bug A root cause confirmed by playtest: every `BDiBinaryData_ProcHandler` call fires with `sep=0x0`. **Shipping Oni install has no `.sep` companion files** — verified across 2,035 files in CXOni: `.dat`/`.raw`/`.bik` only. Both `BDiBinaryData_ProcHandler` (BINA → texture materials / impact effects / particles) and `OSiBinaryData_ProcHandler` (OSBD → ambient/group/impulse audio, at `Oni_Sound2.c:4342`) gate their loads on `TMrInstance_GetSeparateFile() != NULL` and silently no-op when it's NULL. The importer concatenated the "separate" blob into `.raw`; verified by reading bytes at the on-disk `data_index` offsets — they land on valid `BDtHeader` rows (class_types `ONIE`, `OBJC`, `PAR3`, `OSAm`, …) followed by `data_size−8` bytes of payload.
- `c039fa5` (REVERTED in `7e51a55`) — fix(tm): when `BFrFile_Open(*.sep)` fails, fall back to opening `.raw` as the separate-file handle. Surgical 16-line change in `BFW_TM_Game.c` `TMiGame_InstanceFile_OpenAndLoadHeader`. Fix is correct in isolation: verified at runtime — `OSiAmbient_Load` 1,005×, `OSrAmbient_BuildHashTable num_ambients=1005`, `main_menu_win` found at idx 867. **But it unblocks Bug C**: `P3rLoad_PostProcess` SIGBUSes during 64-bit bridge of `P3tParticleDefinition` (KERN_PROTECTION_FAILURE at `0x19c8d9cb04`, classic above-4GB pointer truncation). Bug C was latent the whole port — particle binary data never reached the loader until Bug A was fixed. Fix reverted at session close pending Bug C diagnosis. **Do not re-apply solo. Land paired with Bug C fix.**
- `7e51a55` revert of `c039fa5`. Binary back to silent-but-runnable, same functional state as session 11 close.
- Discipline lessons (now in auto-memory): always use superpowers, always verify behaviourally (auto-launch + log inspect, not just diff review), never trust prior-session claims about current state, don't dispatch Opus implementer agents on uncertain hypotheses, trace one layer downstream of every unblock before declaring done. Session burned ~500K tokens for net-zero functional progress; the protocol corrections matter more than the diagnosis itself.

### 2026-04-24 — Session 11: FNtW crash characterised, audio root-caused

- Audio root cause identified and it is NOT an OpenAL / streaming / threading issue. Added breadcrumbs at every audio layer (platform → SS2 → OS layer → hash builder) and confirmed: the `SSrAmbient` template registry is completely empty (`SSrAmbient_GetNumAmbientSounds()` returns 0). `OSrAmbient_BuildHashTable` therefore adds zero entries. All subsequent lookups (`OSrAmbient_GetByName("main_menu_win")`) return NULL, so `OSrMusic_Start` and every other music/ambient/dialog call short-circuits silently. No sound can ever play by design. This is the same class of bug as character-geometry `triNormalArray` failing — a template-manager enumeration / registration path that isn't populating ambient templates on 64-bit. The OpenAL port itself is fine; the bug is at the template-registration layer. Scope revision: audio work is a TM-bridge investigation, not a platform port.
- Clarified that the earlier "3D engine startup complete → running game..." trajectory some nohup launches took was NOT user input — nobody clicked New Game. Still an unexplained auto-transition; parking for later as a likely port-exposed `ONgGameState->victory` garbage-read issue (triggers the win-splash path, which calls `ONrLevel_Load(next_level)`). Separate bug from the audio finding.

- FNtW crash root-cause narrowed. User drove gameplay; `startup.txt` captured 2,916 clean `MSrTransform_Geom_FaceNormalToWorld` calls (env geoms, `numVec` 54–353) then one bogus call on a later geom with `numVec=1063779958`. That decimal value is the IEEE-754 bit pattern for ≈0.886f — we're reading a float out of a `UUtUns32` slot. Not an overshoot; it's a struct-offset / TM-bridge miss on character geometry's `triNormalArray`. Environment geoms bridge correctly; the character-geometry path (probably `TRtBody`-nested `M3tVector3DArray*`) is missing its 32→64 widening step. Next step: widen the breadcrumb with a 16-byte hex dump around `triNormalArray` to identify which struct we're actually pointing at.
- `SIGSEGV handler verified end-to-end`. Induced SIGSEGV on a running playable process; handler fired, SDL tore down cleanly, zero UE-state zombies remained. No more daily reboots confirmed.
- `[obs commit pending]` per-call breadcrumb at `MSrTransform_Geom_FaceNormalToWorld` entry — logs `inGeom / triNormArr / vectors / numVec / block8 / outPtr` uncapped. This commit is the obs: landing; diagnostic stays in until the character-geometry bridge is fixed.
- Audio scoping: confirmed OpenAL output is silent at menu (user correctly points out menu music should be audible from memory). Tracked the path into `BFW_SS2_Platform_OpenAL.c` — the port isn't a streaming pump; it batch-decodes whole sounds via libav MSADPCM and attaches a single buffer per source. That architecture should work for looping music with `AL_LOOPING`, so the silence is a different bug. Two visible code smells already in that file: a dead-code format check at line 186 with `&&` that never fires, and an empty-body `SS2rPlatform_InitializeThread` at line 488 (non-void return with no return statement — UB). Investigation continuing with breadcrumbs inside `SetSoundData` / `Play` / `DecompressMSADPCM` to find which step actually fails.
- HiDPI mouse correction saved to auto-memory: render lives in the bottom-left of the window, pointer coords land in the top-left. Clicks themselves work. Previous framing as "menu lockup" / "non-deterministic clicks" was wrong; it's a pointer-vs-render quadrant mismatch that makes aiming hard, not broken input.

### 2026-04-24 — Session 10: gameplay-drivable
- Flagged audio as untested — OpenAL `SSiSoundChannels_Initialize` has run cleanly since the session-2 channel-count-init fix, but we have never actually heard sound play through many sessions of debugging. No commit for this; added to the milestones so it doesn't keep slipping off the list.
- Removed inherited `.github/workflows/cmake.yml` — built Windows/MSVC + MinGW-cross-to-Windows targets, both of which are out of scope for this fork. CI was red on every push because our macOS/ARM64 port work is orthogonal to those jobs, generating email spam for no signal. A macOS-ARM64 CI workflow can be written fresh later.
- `e8c85d7` SIGSEGV/SIGBUS/SIGFPE/SIGILL/SIGABRT handler calls `SDL_Quit()` then re-raises. Stops new crashes from creating unkillable `UE`-state zombie processes (macOS kernel pins the process on driver teardown otherwise). Existing zombies still need one reboot to clear.
- `64e0e68` `MS_Geom_Transform.c`: `block8 = (numX + 7) >> 3` (ceiling) made the main loop overshoot vertex / normal arrays by up to 7 slots while the remainder loop never ran. Switched to floor.
- `de9fdcf` `BFW_Akira.c` `ARiPointInBSP`: `inPlaneEquArray[curNode->planeEquIndex]` dereferenced the raw encoded index without `AKmPlaneEqu_GetIndex()` masking. On 64-bit `base + 0x80000000 × 16` doesn't wrap, so it indexed 32 GB off into unmapped space.
- `816314b` `BFW_Decal.c` `P3iDecal_ClipToPlane`: empty input buffer caused `num_points - 1` to underflow an unsigned 32-bit to `0xFFFFFFFF`. Multiplied by `sizeof(M3tPoint3D) = 12`, this yields a ~48 GB forward offset that no longer wraps. Added a zero-check at function entry.
- User result: walked around, mouselooked, went up stairs. Walks *through* doors rather than opening them — interactive object / trigger system isn't firing. Strong candidate: the `UUtUns32 inUserData` callback-truncation class (`OT_Door`, `OT_Trigger`, `OT_TriggerVolume`, `Oni_AI2*`, `OT_Combat`). Crash moved further into `MSrTransform_Geom_FaceNormalToWorld` on a later character geometry — floor-div fix didn't cover every overshoot path.

### 2026-04-24 — Session 9: first gameplay frame
- `103496b` env-clipper over-allocates `gqVertexData.textureCoords`. The software env clipper was writing clip-vertex UVs past the exact-sized env buffer; on ARM64 the overflow landed on `AKtOctTree->interiorNodeArray` and corrupted it. Scratch copy sized `numTextureCoords + M3cExtraCoords` fixes it.
- `3e00c86` narrowing tripwires that located the stomp site.
- `ONI_AUTOSTART=1` env-var path in `Oni_Windows2.c` to skip the main menu while the HiDPI mouse bug makes clicks non-deterministic.
- First gameplay frame on screen — level loads, warehouse textures render, HUD and player visible.

### 2026-04-22 → earlier (sessions 1–8)
Template-manager 64-bit bridge, 32→64 struct layout bridging at level-load, `tm_raw` pointer resolution in `BFW_Totoro.c`, SIGBUS walks in instance-descriptor traversal, camera / AKOT corruption bisection, OpenAL channel-count init, memory allocator pointer-width fixes. See `docs/handoff-2026-04-*.md` for session-by-session narrative.

